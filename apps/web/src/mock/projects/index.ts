/**
 * Mock data for the projects index (#722) — owned by #729, imported only by its own page; nothing shared re-exports it.
 * Empty until then (#725).
 */
export const MOCK_PROJECT_CARDS: readonly unknown[] = [];
