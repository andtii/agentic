/**
 * Mock data for the Plan views (#722) — owned by #754, imported only by its own page; nothing shared re-exports it.
 * Empty until then (#725).
 */
export const MOCK_PLANS: Readonly<Record<string, readonly unknown[]>> = {};
