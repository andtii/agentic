/**
 * Mock data for Settings: General, Members, Folders, Connectors and New project (#722) — owned by #733, imported only by its own page; nothing shared re-exports it.
 * Empty until then (#725).
 */
export const MOCK_PROJECT_SETTINGS: Readonly<Record<string, unknown>> = {};
