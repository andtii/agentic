/**
 * Mock data for the project Overview (#722) — owned by #730, imported only by its own page; nothing shared re-exports it.
 * Empty until then (#725).
 */
export const MOCK_PROJECT_OVERVIEW: Readonly<Record<string, unknown>> = {};
