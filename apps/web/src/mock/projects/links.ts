/**
 * Mock data for /projects/links (#722) — owned by #765, imported only by its own page; nothing shared re-exports it.
 * Empty until then (#725).
 */
export const MOCK_PROJECT_LINKS: readonly unknown[] = [];
