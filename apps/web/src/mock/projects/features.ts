/**
 * Mock data for Settings › Features: the add-a-feature catalogue (#722) — owned by #736, imported only by its own page; nothing shared re-exports it.
 * Empty until then (#725).
 */
export const MOCK_FEATURE_CATALOGUE: readonly unknown[] = [];
