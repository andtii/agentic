/**
 * Mock data for the project Chats (#722) — owned by #731, imported only by its own page; nothing shared re-exports it.
 * Empty until then (#725).
 */
export const MOCK_PROJECT_CHATS: Readonly<Record<string, readonly unknown[]>> = {};
