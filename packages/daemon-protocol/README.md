# @agentic/daemon-protocol

Envelope validators and conformance suite for the daemon <-> platform WebSocket protocol.

Design: `docs/architecture.md`. What may move into the sigx estate later: `docs/promotion.md`.
