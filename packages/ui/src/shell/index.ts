/** The responsive app shell: one Drawer, docked as the sidebar from md up and a modal sheet below, + topbar + content. */
// `AiShell`: the name an api-declaring design system's generated `./components` imports for the `ai-shell` scope.
export { AppShell, AppShell as AiShell } from './AppShell';
export { aiShellAnatomy } from './anatomy';
export type { AppShellProps, NavItem, NavGroup, NavSwitcher, NavLinkSlotProps, BackSlotProps } from './AppShell';
