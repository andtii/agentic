import type { RecipePatch } from '@sigx/zero-kit/define';

// Filled by a later issue (#589 / #591); registered in the barrel now so that issue edits only this file.
const patch: RecipePatch = {};

export default patch;
