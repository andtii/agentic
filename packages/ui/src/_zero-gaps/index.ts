/**
 * Local stand-ins for parts `@sigx/zero` does not have yet (AGENTS.md →
 * "Zero feedback"): each file names the zero-wip issue it waits on and is
 * deleted once zero ships the part.
 */
export { FileTree, FileTreeLegend, ancestorsOf } from './FileTree.js';
export type { FileTreeProps, FileTreeLegendProps, FileTreeLoad } from './FileTree.js';
