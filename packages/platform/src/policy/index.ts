/** Approval policy compilation — `ApprovalRule`s and `ToolGrant`s as a `@sigx/ai-agent` `Policy`, constrained across delegation (AGT-04, COL-10, AC-12). */
export { agentPolicy, compilePolicy, constrainPolicy, grantPolicy, ruleMatches, sessionPolicy } from './compile.js';
