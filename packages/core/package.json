{
  "name": "@agentic/core",
  "version": "0.0.0",
  "private": true,
  "description": "Edge-safe platform contracts: ids, agent config, chat entries, task lifecycle, environments, capabilities, plugin manifests, memory/learning interfaces, daemon frames, principals. Zero dependencies.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "development": "./dist/index.js",
      "production": "./dist/index.prod.js",
      "import": "./dist/index.js"
    }
  },
  "sideEffects": false,
  "scripts": {
    "build": "vite build && vite build --mode prod-dist && tsc -p tsconfig.build.json"
  },
  "devDependencies": {
    "@sigx/vite": "catalog:",
    "typescript": "^7.0.2",
    "vite": "^8.0.3"
  }
}
