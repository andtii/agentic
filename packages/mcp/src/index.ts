/** @agentic/mcp — MCP client (consume servers as platform tools) and the platform MCP server (orchestration surface for external clients). */
export const PACKAGE = '@agentic/mcp';

export * from './client/index.js';

export * from './server/index.js';
