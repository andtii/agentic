# Acceptance suite — requirements §17

Every acceptance scenario of `docs/requirements.md` §17 (`AC-01` … `AC-15`; the table skips `AC-03` and `AC-04`) maps to one scripted scenario under `apps/web/__tests__/acceptance/` and, where a part cannot run offline, to a manual checklist entry below. One file per scenario, each running end to end through the real actor host with mock daemons, drivers and models — never a re-import of a unit test. Where a deeper test proves the same path, the scenario file names it.

```sh
pnpm --filter @agentic/web test:acceptance
```

runs the whole suite: first the workerd half (`ac-NN.workers.test.ts`, `vitest.acceptance.config.ts` — the real `ActorHost` Durable Objects on Miniflare, the Worker in `__tests__/acceptance/worker.ts`), then the in-process half (`ac-NN.test.ts(x)`, the root vitest config). CI runs it in the `test` job on Node 22 (`.github/workflows/ci.yml`, "Acceptance suite"); the in-process half also runs under `pnpm test`.

## Where each scenario runs

| Host | Files | What it is |
| --- | --- | --- |
| workerd (`*.workers.test.ts`) | AC-01, AC-02, AC-07, AC-08 | The scenarios that need Durable Objects: daemon WebSockets on the Machine object (`connectDaemon` bridges an `InMemoryDaemon` from `@agentic/daemon-protocol/testing` onto the real `/_agentic/daemon/{machineId}` socket) and Schedule alarms (`runDurableObjectAlarm`). Everything goes over the HTTP actor mount as a signed-in user (`workers.ts`). |
| in-process, app registry (`host.ts`) | AC-05, AC-09, AC-10, AC-11, AC-12, AC-13, AC-14 | `platformActors()` from `src/actors.app.ts` — the app's own definitions and ports — on the platform test host over memory storage; the `anthropic-api` runtime is the real local path (`createSessionFactory` → `createPlatformModelAgent` over the platform tool ports) on a scripted `mockModel`, so `delegate`, `memory_search` and the approval policy run as with a real model. |
| in-process, live pages (`../pages/live-harness`) | AC-06, AC-15 | The pages mounted as a browser tab mounts them, over the real actor wire, a `mockAgent` behind the Session actor. |

## Map

| ID | Scenario | Evidence (spec) | Deeper proof | Manual |
| --- | --- | --- | --- | --- |
| AC-01 | Two registered machines appear with independently reported environments and availability | `ac-01.workers.test.ts` — two paired machines, two daemons on real sockets, each Machine reports its own `hello`, one dropping leaves the other online | `packages/platform/__tests__/machine/actor.test.ts`, `apps/web/__tests__/workers/daemon.test.ts` | M1 |
| AC-02 | Three accounts of one runtime on one machine; each selectable; execution changes no other account | `ac-02.workers.test.ts` — three environments in `hello`, three tasks each choosing one, every `session.open` names the chosen environment, accounts and doctor verdicts unchanged after | `packages/platform/__tests__/routing/routing.test.ts` (AC-02 block), `packages/runtimes/__tests__/claude-code/isolation.test.ts` | M2 |
| AC-05 | The assistant delegates; the task has owner, objective, traceable origin, status, result | `ac-05.test.ts` — a chat post through the composer's own path (`runActivation`), the assistant's model calls `delegate`, the child's record is the AC-05 shape, the result returns as the tool result and the answer lands in the chat | `packages/platform/__tests__/routing/delegation.test.ts`, `packages/platform/__tests__/task/task-actor.test.ts` | M1 (with a real CLI agent) |
| AC-06 | The user disconnects and returns on mobile; history and task status remain | `ac-06.test.tsx` — a tab posts, is torn down mid-turn, the platform finishes the turn with nobody connected, a fresh tab finds the history, the answer and the recorded task | `apps/web/__tests__/pages/chat-live.test.tsx`, `apps/web/__tests__/pages/session-live.test.tsx`; the phone regime: `pnpm --filter @agentic/web e2e:mobile` | M3 |
| AC-07 | A selected machine is offline; the configured policy is followed; no silent change of account or environment | `ac-07.workers.test.ts` — `queue` waits for ITS machine (another machine with the same environment id is never used) and resumes on its redial; `fail` fails naming the policy; `fallback-api` runs on `anthropic-api` only because the config says so, with a transition that says why | `packages/platform/__tests__/routing/routing.test.ts` (AC-07 block) | — |
| AC-08 | A reminder due while every machine is offline is delivered centrally | `ac-08.workers.test.ts` — the user's machine was online and went away; the Schedule object's own alarm delivers the reminder to the Inbox through the actor mount alone | `packages/platform/__tests__/schedule/trigger.test.ts`, `apps/web/__tests__/workers/schedule.test.ts` | M4 (push to a device) |
| AC-09 | A correction is retained with provenance and made available to a similar task | `ac-09.test.ts` — the correction through `Session.correct`, the lesson in the agent's scope with the user's provenance, the next similar session's record carries it in `spec.memories` and the platform memory block of `spec.system`; the `it.fails` case pins that the model does not yet see it on the routed path (#135) | `packages/platform/__tests__/session-learning.test.ts`, `packages/learning/__tests__/plugin.test.ts` | — |
| AC-10 | Chat membership gives no access to another agent's private memory | `ac-10.test.ts` — two agents in one chat; the other's identity is refused on the private scope and on a shared scope without an ACL; its own `memory_search` on the real local path searches only its own scope; the user owns every scope; an ACL opens a shared scope per access | `packages/platform/__tests__/memory.test.ts` | — |
| AC-11 | A memory implementation is replaced; data migrates through a defined path with limitations reported | `ac-11.test.ts` — an agent's scope on the Memory actor (a correction's lesson and every other kind) → dry run into the `flat` plugin reporting the dropped fields per kind → migrated → back into another actor scope losslessly | `packages/memory/__tests__/migrate.test.ts`, `apps/web/__tests__/workers/memory-conformance.test.ts` | — |
| AC-12 | A delegated action requires approval; delegation preserves it and exposes the request | `ac-12.test.ts` — the child asks under its own policy, the child waits `{approval}` and the parent `{child}`, the Inbox holds the request with a session ref, the user's decision resolves it in the child; a parent `deny` rule constrains the child | `packages/platform/__tests__/routing/delegation.test.ts`, `packages/platform/__tests__/policy/compile.test.ts` | — |
| AC-13 | A plugin is disabled; dependents identified; future use prevented per lifecycle | `ac-13.test.ts` — a connector plugin with a sealed secret, agents depending by connector and tool namespace, a schedule through an agent; `disable` names them all, `requireEnabled` and `openSecret` refuse with typed errors, dependents' records untouched, `remove` refuses unless forced | `packages/platform/__tests__/registry/` | — |
| AC-14 | A compatible remote A2A agent's declared capabilities and operations are available through the adapter | `ac-14.test.ts` — `a2aAgent` over an in-process A2A server as an `a2a` runtime of the app registry: capabilities from the card recorded on the Session, a message turn to its answer, `cancel` on a turn in flight reaches the remote (`CancelTask`); resume declared unsupported | `packages/a2a/__tests__/client.test.ts`, `packages/a2a/__tests__/conformance.test.ts` | — |
| AC-15 | An integration cannot resume or cancel; the limitation is visible; no capability is claimed | `ac-15.test.tsx` — a runtime declaring `resume: false`, `cancel: false` behind a running session: `/sessions/:id` lists both as unsupported with the runtime's reason, renders no Cancel control (the topbar contribution as `App` mounts it) and none for migration | `apps/web/__tests__/pages/session.test.tsx`, `apps/web/__tests__/pages/session-live.test.tsx` | M2 |

## Manual checklist

The parts no offline test can stand in for: a real Claude Code daemon, a real GitHub login, real push delivery, a real phone. Each entry names the exact commands; the deployment side is `docs/runbook.md`.

- **M1 — real machines and a real Claude Code agent (AC-01, AC-05).** On two machines with Claude Code installed and signed in: `pnpm --filter @agentic/daemon start` after pairing each through `/pair` (the code from `/machines` → "Pair a machine"). Check `/machines` lists both with their environments and `online`; stop one daemon and check only that machine goes offline. Then, on the preview deployment (`docs/runbook.md` → "Preview deploy"), create an agent on the `claude-code` runtime with one of the environments as its default and a second agent on `anthropic-api` with the `delegate` tool, and ask the second to delegate to the first; open `/tasks/:id` on the parent and check the child's owner, objective, origin (agent · task · call), status and result.
- **M2 — several accounts on one machine (AC-02, AC-15).** Sign Claude Code into two or three accounts, one config dir each (`docs/multi-account.md`); `pnpm --filter @agentic/daemon start` reports them as separate environments with their doctor verdicts (`/machines/:id`). Run one task per environment and check each session's environment line and that `/machines/:id` shows every account unchanged. Open `/sessions/:id` of a Claude Code session and check the capability list says what the driver declared (`packages/runtimes/src/claude-code/capabilities.ts`) and that no control exists for an unsupported operation.
- **M3 — return on a phone (AC-06).** From a laptop, post to a chat on the preview deployment and close the tab mid-answer; on a phone, sign in (GitHub, `docs/runbook.md` → "Secrets and bindings"; the walk-through's dev login is preview-only) and open the same chat: the history, the finished answer and the task's status on `/tasks/:id` are there. The 400 px layout of every route is `pnpm --filter @agentic/web e2e:mobile` (Playwright, mock data; CI's `e2e` job).
- **M4 — push delivery (AC-08).** Once the Web Push channel is wired with VAPID keys (`docs/runbook.md` → "Not wired yet"): subscribe a phone from `/settings`, create a reminder one minute ahead on `/schedules`, stop every daemon and close every tab; the notification arrives and `/` shows it in "Needs you". Until then the central path is the scripted scenario only.
- **Demo smokes.** `smoke:demo1` (`docs/runbook.md` → "Demo 1 smoke") walks sign-in → agent → config → chat → answer against a deployed Worker with the real model; run it before signing off M1–M3 on a fresh preview deploy.

## Known gaps the suite pins

- **#135** — on the routed local path the retrieved memories stop at the session record: `createSessionFactory` never hands `spec.memories` to the model agent, and `Routing.placeLocal` opens the session without the task's objective. `ac-09.test.ts` carries an `it.fails` case that flips when it lands.
- **#137** — `@sigx/actors` keeps the current host in one global; with several `ActorHost` objects in one isolate an ambient `actor()` hop can run another object's actor locally ("Cannot perform I/O on behalf of a different Durable Object"). `__tests__/acceptance/worker.ts` scopes each object's entry points with an `AsyncLocalStorage`; the production `ActorHost` does not yet.
- The Routing actor opens local sessions for `anthropic-api` only, so a remote A2A agent is not yet a runtime a task can be routed to (the `a2a` plugin kind, architecture §12); `ac-14.test.ts` opens the Session directly, as the router would.
