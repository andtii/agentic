# Architecture — Unified Agent Platform

Status: v1 design, 2026-09-17. Requirements: [`requirements.md`](requirements.md). Decisions: [`decisions.md`](decisions.md). Promotion candidates: [`promotion.md`](promotion.md).

Every sub-issue of the tracking issue names the section here it implements. A section is the contract an implementing agent works to; change the section in the same PR when a seam changes.

## 1. Package layout

pnpm monorepo, `@agentic/*`, all private.

```
apps/web               sigx SSR + serverFn + actors host on Cloudflare (Worker + ActorHost Durable Object in one bundle); UI on @sigx/zero + zero-daisyui
apps/daemon            Node 22 bin `agentic-daemon` (pair | run | doctor); Windows first
packages/core          edge-safe types + tiny helpers, zero deps: ids, AgentConfig, ChatEntry, TaskState/WaitReason/TaskContract, EnvironmentDescriptor, CapabilityReport, PluginManifest, MemoryPlugin/LearningPlugin interfaces, daemon frame types, Principal kinds
packages/platform      defineActor definitions + services: Workspace, Agent, Chat, Task, Session, Machine, Schedule, Memory, Inbox, Ledger, Registry, Audit; auth helpers
packages/runtimes      runtime adapters: 'anthropic-api' (modelAgent), 'claude-code' (daemon driver); platform tools (memory.*, delegate, chat.post, task.report, ask_user)
packages/memory        default MemoryPlugin (Memory-actor backed, BM25-ish + recency, no embeddings) + memoryConformance
packages/learning      default LearningPlugin (corrections → lessons; proposals needing review)
packages/daemon-protocol  envelope validators + daemonConformance (types live in core)
packages/ui            @agentic/ui: zero fragment (ai-thread, ai-message, ai-composer, ai-tool-call, ai-reasoning, ai-approval, ai-question) + layout shell (Stack/Row/Col on data-l-*) + StreamingMarkdown on @sigx/markdown
packages/mcp           MCP client (Streamable HTTP, edge-safe; stdio on the daemon) + platform MCP server (orchestration surface)
packages/a2a           A2A 1.0 JSON-RPC server (agent card + tasks) + client adapter implementing the @sigx/ai-agent Agent contract
```

Rules:

- `packages/*` never import the `sigx` umbrella (it drags `@sigx/runtime-dom/platform`). Peer on `@sigx/runtime-core` / `@sigx/reactivity` / `@sigx/actors` only, so packages stay edge-safe and renderer-neutral. Only `apps/web` and `packages/ui` touch the DOM.
- Import order (downward only): core ← daemon-protocol ← memory / learning ← runtimes ← platform ← apps.
- Source layout as in signalxjs/ai: one folder per concern, `index.ts` is the public surface, entry points are folders.
- Every sigx-estate version comes from the `catalog:` in `pnpm-workspace.yaml`.

## 2. What we build on (do not redesign)

- `@sigx/ai-agent`: `Agent` / `AgentSession` / `AgentTurn`; `AgentEvent` stamped `(sessionId, epoch, seq)`; `AgentCapabilities` (branch on capabilities, never on agent id); policy engine (`resolveRequest`, `allowAll`, `denyAll`, `allowReadOnly`, `allowTools`, `denyTools`, `firstMatch`, session grants); transcript reducer (`reduceAgentEvent`, `createReducer`); `TranscriptStore` / `EventLogStore` seams; `modelAgent`; `agentTool`; `./wire` `serveSession` / `connectSession`; `./harness` `createJsonRpcPeer`, `createMcpToolHandler`; `./app` `useAgentSession`; `./testing` `mockAgent`, `agentConformance`, record/replay. Adapters: `@sigx/ai-agent-claude-code`, `-copilot-cli`, `-codex-cli`, `-acp`; `@sigx/ai-agent-node` for process supervision.
- `@sigx/actors`: `defineActor({type, state, applyEntry, persistence, methods, streams, reads, watches, subscriptions, tasks, resumeTasks, authorize})`; `ctx.state` (deep signal proxy), `ctx.save()`, `ctx.append(entry)` (O(entry) durable log replayed on activation, compacted on save), `ctx.reminders` (durable, ≥ 60 s), `ctx.tasks` (detached, at-least-once restart), `ctx.actor(Def, key)` (principal and call chain propagate; cycles throw), `ctx.publish`, `ctx.changes()`; `authorize(principal, rq, op)` with `op.resource = {kind, type, key, method}` at every entry point; `useActorState(Def, key, method, ..., {live: true})`; `@sigx/actors-ws`; `@sigx/actors-cloudflare` (`createHostDurableObject`, `createWorkerHandler`, `durableObjectStorage`, `durableObjectReminders`).
- `@sigx/zero` + `@sigx/zero-daisyui`: 50 headless components with anatomy attributes; design systems as swappable CSS; ecosystem protocol (`"sigx-zero"` fragment + `sigx zero:extend`).
- sigx core: `serverFn` / `serverStream`, `useData` / `useStream`, `@sigx/cloudflare` adapter, `@sigx/router`, `@sigx/markdown`.

## 3. Hosting topology

- One Cloudflare Worker (`apps/web`) serves SSR pages, serverFns, static assets, the actor HTTP mount (`/_sigx/actor`), the browser live socket (`createWorkerHandler({socket: {terminate: 'object'}})`), the daemon socket (`/_agentic/daemon/{machineId}`, accepted by the Machine Durable Object), MCP (`/_agentic/mcp`), A2A (`/_agentic/a2a/{agentId}` and `/.well-known/agent-card.json`), webhooks (`/_agentic/hooks/{id}`), OAuth callbacks.
- One Durable Object namespace `ACTORS`, one object per actor, SQLite-backed (`migrations: [{tag: 'v1', new_sqlite_classes: ['ActorHost']}]` — never `new_classes`, it is irreversible), `durableObjectStorage()` + `durableObjectReminders()` (alarms). `wrangler.jsonc` needs `compatibility_flags: ["nodejs_compat"]` and `define: {"__DEV__": "false"}`.
- **Eviction is not deactivation.** `onDeactivate` never runs on Workers. Every mutation ends in `ctx.save()` or `ctx.append()` inside the turn.
- R2 bucket for artifacts, exports and chat attachments (`files/`, #207). Workers Secrets for the OAuth client secret, `WORKSPACE_KEK` (AES-GCM key for stored API keys), VAPID keys are per workspace, not deployment secrets: the Web Push plugin's config and a Registry secret (#244). No D1 in v1.
- Implementation (#33): `apps/web/src/actors.app.ts` holds the registry (`platformActors(ports)`), `createActorHost()` (the `ActorHost` class; `createHostDurableObject` derives storage and reminders from the object's own state, `socket: {}` accepts object-terminated sockets) and `createActorWorker({ fallback })` (the Worker half; server functions and SSR are its fallback). Both halves call `ensureServerApp(env)` first — `createServerApp({ ...serverAuth({ sessionSecret }) })` — so `authorize` sees the same principal in the Worker and after the hop into the object. No `SESSION_SECRET` → every caller is anonymous, never a fallback secret. Ports not wired yet refuse loudly: the Session `factory` returns `null` (daemon path) until #35, the platform `tools` a daemon session calls back are answered `unsupported` until #37. The Schedule trigger is the platform's `scheduleTrigger()` (#42; §4 Schedule) with no environment probe wired yet, so a scheduled task that needs an environment waits `environment-offline` for the router (#37). Routing order in `entry.cloudflare.ts`: auth routes → the actor worker (`/_agentic/daemon/{machineId}` forwarded to the Machine object, actor mount, actor sockets) → server functions → document. The auth routes are resolved by `apps/web/src/auth/mount.ts` (`createAuthMount`, the same resolver the workers harness mounts, #180) from what the env has: `SESSION_SECRET` alone mounts `POST /auth/pair`, `GET /auth/me`, `POST /auth/logout`; `GET /auth/login` / `GET /auth/callback` need the GitHub app's secrets and an origin; the OAuth 2.1 server (#50) needs an origin — `APP_ORIGIN`, or on localhost the request's own. A dev-login-only `pnpm dev` therefore pairs a machine. `test:workers` (`@cloudflare/vitest-pool-workers`) drives the real object on SQLite storage; deploy steps are in `docs/runbook.md`.
- **Several objects per isolate, one global host (#137).** `@sigx/actors` resolves an ambient `actor(def, key)` through `globalThis.__SIGX_ACTOR_HOST__` (last `start()` wins), and `createHostDurableObject` starts one host per object — so with several `ActorHost` objects in one isolate (always in the workers test pool, in production whenever objects of the class share an isolate) a hop resolved through the last-booted object's host, whose placement answers `isSelf` for ITS actor: the callee ran locally inside the caller's object on the callee's storage ("Cannot perform I/O on behalf of a different Durable Object"; the callee reset, its unsaved turn lost). The platform hops ambiently wherever a call carries its own principal (`.with({ context: asPrincipal(p) })`: the Routing driver's clients, `routing().machineOnline`, the tool and learning ports, the Session's command sink). `apps/web/src/host-scope.ts` makes the seam an accessor over an `AsyncLocalStorage` (workerd under `nodejs_compat`): `ActorHost` runs `fetch`, the hibernation handlers and `alarm` under its own host (`runWithHost`), the Worker enters the scope ONCE at the top of `entry.cloudflare.ts`'s `fetch` around every route — auth routes included, since `POST /auth/pair`, the token lookup and the MCP mount hop too (#172: unscoped, the pair for a machine whose object the Pair page had just booted ran inside the Worker on that object's storage) — under its own host (`createActorWorker().host`, a thunk: the host boots once per isolate from `env` alone through `createActorWorker().boot(env)`, awaited before an auth route runs and by the actor mount otherwise — #182: booted only by the first mount request, an auth route hopping as the very first request of a cold isolate found no host at all), and a read outside any scope answers what `stampHost` last wrote — `@sigx/actors` is unchanged. Upstream: signalxjs/actors#456 (the seam should scope itself); `__tests__/workers/host-scope.test.ts` pins the two-machines-then-a-session-turn shape on the production class, and the two-machines-then-`POST /auth/pair` shape on the Worker.
- **Chat files (#207, `apps/web/src/files`).** `r2ChatFileStore` is the `ChatFileStore` on the `ARTIFACTS` bucket: `files/<ws>/<chat>/<fileId>`, custom metadata `name` / `mediaType` / `posted` / `at`; R2 cannot edit metadata, so `markPosted` rewrites the object; `sweepOrphans` reads one listing page of `files/` per call (with `customMetadata`) and resumes from its cursor. One instance per isolate (`platformFiles` in `actors.app.ts`) is `PlatformPorts.files`, passed to the five places §7 "Wiring the file store" lists, and serves the Worker's routes. `createFilesMount` resolves `POST /files/chats/:chatId` and `GET /files/chats/:chatId/:fileId` after the auth routes, in the `actors.boot(env)` branch (they hop). Both authenticate with `authenticateRequest` (the session cookie; an agent token may read; machines never) and address the chat in the CALLER's workspace, so another workspace's ids name nothing (404). Upload: a same-origin `Origin` (the CSRF guard for a cookie credential), `Content-Length` and a streaming cap at `CHAT_FILE_MAX_BYTES` (413), a type that parses and is not HTML / XHTML / an executable (415), `X-File-Name` URI-decoded to its last path segment without control characters, ≤ 200 characters; R2 first, then `Chat.registerUpload` as the caller — a refusal deletes the object and passes the Chat's status on; 201 `{ file, uri }`; then, at most once an hour per isolate, `sweepOrphans(1 day)` under `waitUntil`. Download: `Chat.fileAccess` as the caller (`null` → 404), the object streamed with `X-Content-Type-Options: nosniff`, `Content-Security-Policy: default-src 'none'; sandbox`, `Cache-Control: private, max-age=3600` and `Content-Disposition` `inline` for png / jpeg / gif / webp, `attachment; filename*=UTF-8''…` for anything else (SVG included). The live chat (`pages/chat`) uploads from the composer's `files` event, posts `agentic-file:` parts with the text, and renders them through `/files/chats/…` (`entryTranscript`).
- Follow-up, not v1: split the DO host into a second Worker so UI deploys cannot evict live sessions.

## 4. Actor catalogue

Keys are workspace-prefixed so every `authorize` chain starts with `sameWorkspace(op.resource.key)`.

| Actor | Key | Persistence | State essentials | Methods | Reqs |
|---|---|---|---|---|---|
| Workspace | `ws:{userId}` | save | owner, ids of agents/chats/machines/schedules, settings (tz, notification prefs, default env, retention), pending pairing codes, `recentWorkdirs` `{environmentId, path, at}[]` (≤ 20, one per environment + path, most recent first, #190), `projects: ProjectRecord[]` (≤ `PROJECTS_MAX`, creation order, #330 contract / #332), `lastProjectId?` (the project last chosen for a chat) | get, noteWorkdir(ref) (moves the folder to the front; the router notes a task's own `workdir` over a one-way hop) / recentWorkdirs() (#190), createAgent, createChat({ title?, projectId? }) (a title becomes the new chat's first `rename` entry over a hop, #124; a project is written the same way after it — `Chat.setProject` — and then noted as `lastProjectId` in a second save; an unknown project is a 400 before the index save, and a failed hop leaves a project-less chat with `lastProjectId` untouched, never an orphan, #332), projects() (reentrant `'always'`: `Chat.setProject` reads it back over a hop inside `createChat`'s own turn), upsertProject(patch: ProjectPatch) → ProjectRecord (no `id` → `createId('project')`, `name` required, ≤ 120 chars; every folder the patch sets must be absolute and inside its environment's `cwdRoots` as a PAIRED machine reports them — read over `Machine.get()` hops on the index, an environment nobody reports is a 400, never a folder nobody can run; members must be agents of the index and the coordinator one of them; each `features[id]` goes through `Registry.checkProjectSettings` over a hop; the `PROJECTS_MAX + 1`th is a 400, an unknown `id` a 404; one save; audited `project.changed`), removeProject(id) (404 unknown; clears `lastProjectId` when it matched; chats keep the dangling id — decisions 2026-09-20), noteProject(id \| null) (400 unknown). Every project method is the owner's like the rest of the actor (`methodAuthorize` ANDs after `authorize`, so no method can be wider): the router reads projects as the driver, the tool ports (#334) as the workspace user, as they read the machine index. registerMachinePending → {machineId, pairingCode, expiresAt} (also files the code in the global PairingDirectory over a hop), claimPairing(code) → {machineId} \| null (single use, 10 min; called by Machine.pair over a hop), listMachines, removeMachine, createSchedule, removeSchedule, updateSettings, exportAll (task → `ArtifactSink`, progress in `ops.export`), deleteAll (task → `WorkspaceStore`, children first, own record last); `defineWorkspace({ sink, store })` binds the ports (#48, `docs/retention.md`) | USR-01/02, OPS-10 |
| Agent | `{ws}:agent:{id}` | append config versions | name/description/role, `config` (instructions, skills[], tools[] grants, connectors[]), `configVersion`, memoryPolicy {scope, shared[], autoLearn}, approvalPolicy rules, execution {runtime, defaultEnvironmentId, model, limits}, collaborators `'all'` \| ids, status | get, update(patch, reason) → new version, rollback(v), listVersions, snapshotForSession() → FrozenAgentConfig | AGT-01..09, LRN-08 |
| Chat | `{ws}:chat:{id}` | append entries (a `msg` note may carry `project: {id \| null}` — the chat's project from then on, #330; `setProject` writes it, #332); window of last 200 in state + `{seq, at}` index; older entries archived 100 per `ChatPage` actor (`{chatKey}:p{n}`, not wire-callable: its `authorize` denies every entry point — the wire endpoint and server-side `actor()` — while `ctx.actor()` hops from Chat run no `authorize`, verified on `@sigx/actors` 0.9.2 and pinned by the archive round-trip test) | members {since, historyFrom, workdir?}, coordinator?, sessions {agentId → {sessionId, since, seenSeq}} (#392, §6), title? (the last `rename` entry, #124), projectId? (the last `project` note, #332) | post(input, mentions \| 'all') → {messageId, activated[]}, addAgent(id, access 'all' \| 'from-now'), removeAgent, setCoordinator, rename(title) (users / external clients; trimmed, ≤ 120 chars), setWorkdir(agentId, ref \| null) (#190; members only, 404 otherwise; users / external clients; the member's sticky folder for this chat, written as a user `msg` note "Working folder for … → … on …" / "… cleared" that carries `workdir {agentId, ref}` and activates nobody — the fold copies it onto the member; the activation that builds the task contract copies it into `workdir` + `environmentId`, so it applies from the next activation), setProject(projectId \| null) → summary (#332; users, external clients and MEMBER agents — a non-member agent is 403, a machine too; an unknown project is 400 after one `Workspace.projects()` hop; idempotent; written as a user `msg` note "Project → <name>" / "Project cleared" carrying `project {id}` and activating nobody, so the fold keeps the last one; audited `chat.project-set {chatId, projectId, name?}` under the caller), get (→ `ChatSummary` with `projectId?` and `project? {id, name}` — the name looked up on the Workspace over a hop while the project exists; a removed project leaves `projectId` alone), history(cursor, limit), search(q) | CHT-01..11, MEM-11 |
| Task | `{ws}:task:{id}` | append transitions | objective, context, constraints/limits, expected, origin (user message or agent call), owner, assignee, environmentId, workdir? (#190), projectId? (#330: copied from the chat by the activation contract, inherited by children; the router resolves the project's folder for the environment when `workdir` is absent), depth, status, `wait: WaitReason`, children, result {output, text, artifacts, verified}, cancel {requestedAt, stopped}, usage/cost, configVersion | create (also writes the workspace's TaskIndex row over a hop, #146; a `workdir` without an `environmentId` is a 400, #190), start(by, sessionId?), reportWaiting(reason, by, sessionId?), resolveWaiting(by, why?, sessionId?), complete, fail, cancel (cascade), delegate(spec) → childId, get, tree, stream result | COL-03..12, EXE-11/12, OPS-03 |
| TaskIndex | `{ws}:task-index` | save | one denormalised row per task (id, objective, assignee, owner, status, wait, origin kind, chatId, parentId, depth, environmentId, sessionId, createdAt, updatedAt, `n` transition count), capped at 2000 by dropping the oldest settled rows | upsert(row) (hop-only, from the Task actor on create and every status / wait change; forward-only by `n`), list({status, assignee, parentId, limit}) newest first (the Tasks page's and Home's live read), size | COL-05, OPS-01 |
| Session | `{ws}:session:{id}` + `SessionPage` `{ws}:session:{id}:p{n}` | `ctx.append` per AgentEvent, folded by the definition's `applyEntry: applySessionEntry` (= EventLogStore; O(entry) on Durable Objects since signalxjs/actors#375, @sigx/actors 0.10) + a full save at turn end with the transcript snapshot, which compacts the log (= TranscriptStore); followers (`tail`, `resolution`) read `ctx.changes({ throttleMs: 50 })` so a long turn is not cloned once per event (#194). Under a Durable Object value's 2 MB (#198): the record keeps a WINDOW of the log (`WINDOW_BYTES` 512 KB, `windowBytes` kept per event); past it the oldest ~`PAGE_BYTES` (256 KB) go to the next write-once `SessionPage` and a `roll` entry drops them, so a replay folds the same window. An `index` (request, request-resolved, turn-start, turn-end, the tool-call a request asks about) serves grants, `request(s)`, `resolution`, recovery and learning without a page; `events(from)`, `tail(from)` and the turn-end transcript fold read pages first. The record is bounded for the life of the session (#391): a `turn-start` is indexed as `IndexedTurnStart` — its cursor, turn id and byte count, never the prompt (a chat prompt inlines images up to `CHAT_FILE_INLINE_BUDGET`, 700 KB) — and a roll keeps only the last `INDEX_TURNS` (64) turns in the index, plus any request still open and its call; a replied `CommandRecord` keeps its id, type and reply and forgets the `WireCommand` (`MAX_COMMANDS` 256 of them weigh KBs, not MBs); single-record readers use `findEvent` / `requestById` (window newest-first, then the index) and only `get().grants` and `requests()` materialise `knownEvents`. Budget at a turn end, worst case: window ≤ 512 KB + one oversize event, index and commands in the tens of KB, transcript ≤ 1 MB — under 2 MB. The stored transcript is `boundTranscript`ed to `TRANSCRIPT_BYTES` (1 MB): oldest messages lose tool outputs, reasoning and inlined image / file bytes first (an image part becomes a text note), then large arguments, each replaced by a marker | `spec` (agentId, chatId?, taskId?, runtime, environmentId?, machineId?, cwd? (the folder the daemon session runs in, #190), frozen config), `mode` local/remote, `ref: SessionRef`, capabilities, status idle/running/awaiting/closed/error/disconnected, head {epoch, seq}, events[], transcript?, `running` {turnId, commandId, input}, openRequests, commands by commandId (capped 256), gap? | open(spec), prompt(input, turnId, output?, commandId = turnId), respond(requestId, decision), cancel(agentId?), configure(patch), close, get, events(from?), transcript(), stream tail(from), forwardFrames (internal, machine principal), commandReplied (internal) | EXE-01, CHT-09/11, OPS-05/06, AGT-09 |
| Machine | `{ws}:machine:{id}` | save | name, os, tokenHash, revoked, online, lastSeen, daemonVersion, capabilities[], environments {name, runtime, account {label, authStatus}, cwdRoots, concurrency, isolation}, activeSessions, queued[], pending replies, fs {requestId → pending \| done \| error} (≤ 16), envRequests (≤ 16), policy {webManaged, allowedRoots}, quota {envId → QuotaSnapshot} (merged from `quota` frames, pruned with the environment, kept while offline) | pair(code, info) → token, heartbeat, quota(envId?) → MachineQuotaView, openSession(sessionId, envId, spec, {taskId?}) → opened \| queued, closeSession, sendCommand(sessionId, WireCommand), fsRequest(envId, FsOp) → {requestId}, fsResult(requestId) → FsResultView, putEnvironment(EnvironmentInput) / removeEnvironment(envId) → {requestId} and envResult(requestId) → EnvResultView (owner only), revoke, get; WebSocket message handler; tells the Routing actor (one-way) machineOnline / sessionOpened / sessionClosed | USR-04, EXE-02..09, OPS-03/04 |
| Routing | `{ws}:routing:main` | save | one `Route` per task in flight: runtime, environmentId + machineId + cwd (fixed at the first resolution; cwd #190; a project feature plugin's `beforeSession` may replace the cwd at placement, #332), projectId? + projectFolder? (the task's project and its folder on the environment as resolved at `run`, #332), offline policy, frozen config, sessionId, status waiting-offline \| waiting-capacity \| opening \| running, latest `task_report` per task | run(taskId) (the §7 driver), machineOnline / sessionOpened / sessionClosed (machine principal), report(taskId, report) (the task's agent), get; task `follow` settles the Task at the turn end | EXE-09/11/12, AST-05, COL-05 |
| PairingDirectory | `global:pairing` | save | pairing code → {workspaceId, machineId, expiresAt}; single use, 10 min | register(code, entry) (the Workspace's hop), resolve(code) → target \| null (anonymous: the `POST /auth/pair` door), count | USR-04 |
| Schedule | `{ws}:schedule:{id}` | save + reminder | kind reminder/recurring/agent-task, cron/at, IANA tz, next, agentId?, environmentId?, workdir? (needs environmentId; carried into the fired task's contract, #190), projectId? (exclusive with environmentId + workdir — a project says where the work lives; `null` clears it in a patch, as do `environmentId: null` / `workdir: null`; carried into the fired task's contract for the router to resolve per environment, #332), prompt?, offlinePolicy, lastRun, enabled | create/update/enable/disable/get; onReminder → Inbox notification or Task creation, re-arm | AST-02..07 |
| Memory | `{ws}:memory:{scope}` (`agent:{id}` or `shared:{name}`) | append + compaction | entries {kind working/fact/preference/assumption/lesson/record, text, tags, subject, provenance {source, sessionId, taskId, messageId, at}, confidence verified/stated/assumed, conditions, evidence[], supersedes, retired, ttl}, indexes | put, update, retire, get, query(MemoryQuery) → ranked, export (NDJSON), import(rows) → fidelity report | MEM-01..12, LRN-04/06/07 |
| Inbox | `{ws}:inbox` | save, capped at 500 (every mutation is a `reduceInbox` entry + `ctx.save()`, so it moves to `ctx.append` when `@sigx/actors` ships log appends) | notifications {id `n_<seq>`, kind reminder/task-done/task-failed/approval/input, title, body?, ref task/schedule/session/chat, at, read, deliveries[] {channel, at, ok, error?}}, push subscriptions {endpoint, keys, addedAt, label?} | append, push (append → deliver through every `NotificationChannel` → record attempts, drop expired subscriptions), list({unreadOnly, limit}) newest first, unread, ack(ids \| 'all'), subscribe, unsubscribe, subscriptions; `list`/`unread` are declared `reads` and serve live (`useActorState`) subscriptions | AST-06, OPS-04 |
| Ledger | `{ws}:ledger:{yyyy-mm}` (UTC month) | append (`applyLedgerEntry`; `ctx.append` where the runtime has it, else reducer + `ctx.save()`) | usage rows {key, at, sessionId, agentId, taskId?, turnId?, usage, costUsd?, estimated} idempotent by `key`; correction counters per agent / ISO week / kind (LRN-09) | append(row), rows(query), summary({by: agent \| task \| session \| turn \| day, timeZone?}) → totals with `costUsd: null` when no row carries a cost, `estimatedCostUsd` / `estimatedRows` / `unpricedRows` and `quality` reported \| estimated \| partly-estimated \| not-reported, recordCorrection(tally) → weekly total, corrections(agentId, week, what?) | OPS-07/08, LRN-09 |
| Registry | `{ws}:registry` | save | plugins {manifest, enabled, config, grantedPermissions, seenScopes?, registeredAt, updatedAt} — built-ins of the build's catalogue are stored only once touched —, active {memory?, learning?}, connectors {id, pluginId, transport, url \| command, machine, secrets[], auth {bearer?, headers?, env?} (secret NAMES bound to where they go), tools[] (`<id>__<tool>`), status}, secrets {sealed under `WORKSPACE_KEK`, AAD `{ws}:secret:{name}`} | list (read), overview (read: plugins, active slots, secret names, hasKek), get, isEnabled, requireEnabled (throws `PluginDisabledError`), checkProjectSettings(pluginId, settings) (#332, reentrant, every same-workspace principal: 400 unless the plugin exists, is enabled, is a `project-feature` manifest and the settings — `projectSettings` defaults filled in — pass `validateConfig`; what `Workspace.upsertProject` asks per feature), gate({runtime, connectors?}) → {runtime, memory, learning, channels, connectors?: [{id, state: ready \| disabled \| missing, pluginId, transport, url, auth, tools, status}]} (one hop for Routing; never throws), register(manifest, {enabled, config, grant}) (grants ⊆ declared, PLG-04; refuses a built-in id), enable, disable → {plugin, dependents}, remove (refuses `builtin`, and `plugin-in-use` unless force; drops its connectors), configure (validated: `bad-config`), activate(kind, id, { migrate? }) (single-slot kinds; `migrate` moves the memories first, #243), previewActivation('memory', id) (its dry run), grant, revoke, dependents(id) / dependentsAll() (hops: Workspace index → Agent.get / Schedule.get side by side; agents by connector \| tool namespace \| runtime \| fallback-api, schedules via their agent; the active memory / learning plugin is `workspaceWide`), connectors CRUD + setConnectorStatus, setSecret / deleteSecret / secrets (names only) / openSecret(name, pluginId) (enabled + `secret:` grant; the only way plaintext leaves), exportRows; `defineRegistry({ kek, catalogue })`; owner mutates, any same-workspace principal reads (#48, #229) | PLG-01..09, EXE-10, AC-13 |
| Audit | `{ws}:audit` (live window) + `{ws}:audit:{yyyy-mm}` (UTC month archives) | append (`applyAuditEntry`; `ctx.append` where the runtime has it, else reducer + `ctx.save()`); the live window rolls its oldest batch into month archives | events {key, seq, kind, at, by, summary, agentId?, taskId?, sessionId?, data} idempotent by `key`: approvals, delegations, environment choices (incl. fallback), transitions, config versions, proposal reviews, machine pair/revoke, plugin enable/disable/grant, secret access | record(event) (users over the wire, actors over one-way hops), list({kinds, agentId, taskId, sessionId, since, until, cursor, limit}) → {events newest first, next} as a live `read`, stats; archive(events) (the rollover hop only) | OPS-03, COL-09, AGT-06 |

Agent config log (#15): every version is one entry `{ t: 'config', v, patch, by, at, reason, rollbackOf? }` folded by the pure reducer `applyAgentEntry`; the log stays in state so `rollback(v)` replays it from `defaultAgentConfig()` and appends a new version equal to `v` — a rollback entry carries the whole target config and replaces rather than merges, so keys added after `v` do not survive it. `snapshotForSession()` is a detached copy stamped `configVersion`; version 0 (never updated) cannot start a session. `by` is `user:|agent:|machine:|external:<id>` from `ctx.principal`, which needs the app's principal `codec`.

`TaskStatus = queued | active | waiting | completed | failed | cancelled`. `WaitReason = approval {requestId, sessionId} | input {requestId, sessionId?} | environment-offline {environmentId, policy} | child {childTaskIds} | capacity {environmentId, position} | budget {limit} | project-feature {pluginId, message}` (#339: a project feature plugin's `beforeSession` threw; the router parks the task with the plugin's message).

### Audit

`packages/platform/src/audit` (issue #44, the platform half; the `/history` page is the UI track's). Every consequential action is one `AuditEvent` — `{ key, seq, kind, at, by, summary, agentId?, taskId?, sessionId?, data }` — recorded ONCE per occurrence by the actor that performs it, through `recordAudit(ctx, workspaceId, event)` (or the `AuditPort` the Session and Routing actors take in their ports; `auditPort()` is the default, `capturingAuditPort()` the test double). The helper is a ONE-WAY hop to `{ws}:audit`: it resolves at acceptance and swallows every failure, so the history never stalls or fails the turn that did the work. Idempotency is the emitter's: `key` derives from the source record (`{taskKey}:transition:{n}`, `{sessionKey}:{epoch}:{seq}`, `{agentKey}:v{n}`, `{machineKey}:paired`, …), so a retried turn or a replayed frame folds once. `by` is `user:|agent:|machine:|external:<id>` or `system:<name>` (`system:routing`, `system:policy`).

**Keys.** `{ws}:audit` is the live log: `record` stamps each event with the next `seq` and keeps the newest `AUDIT_WINDOW` (500) in state; once the window holds `AUDIT_WINDOW + AUDIT_ROLL_BATCH` events the oldest batch (100) rolls over into `{ws}:audit:{yyyy-mm}` archives — one per UTC month of the event's `at`, written through an idempotent `archive` hop (like Chat's pages; a crash between the hop and the save repeats it) — so a workspace's history never rewrites one ever-growing record and a month can be retained or dropped as a unit. The window-plus-monthly-archive shape was chosen over one actor per month (the Ledger's) because the history page wants ONE live entry point and one cursor: `list(query)` on the live key is a `read` (`useActorState(AuditActor, auditKey(ws)).list(q)` re-runs after every record), newest first, and walks the archives itself when the cursor passes the window (newest month first; `since` / `until` prune the months it cannot match). `list({ kinds?, agentId?, taskId?, sessionId?, since? (inclusive), until? (exclusive), cursor? (exclusive seq, `null` = newest), limit? (≤ 200) })` → `{ events, next }`, `next` being the seq to pass back or `null` when nothing older can match; `taskId` also matches a `delegation.created` by its child. `stats()` reports `inState`, `recorded`, `archived`, `months`. Over the wire only a user records; `archive` is never wire-callable — hops run no policy.

**Emitters and shapes** (`data` per kind; the common fields are always set as noted):

| Kind | Emitted by | `data` | `by` / ids |
|---|---|---|---|
| `approval.requested` | Session, after a `request {kind: 'permission'}` event is durable (both paths; input requests are the chat's) | `{ requestId, toolName?, callId?, message?, permissionKey? }` | `agent:<id>`; agentId, sessionId, taskId? |
| `approval.resolved` | Session, after the matching `request-resolved` | `{ requestId, outcome allow \| deny \| input \| cancel, resolvedBy client \| policy \| timeout \| cancel, scope?, reason?, ruleId? }` | `user:<ws>` for `client`, else `system:<resolvedBy>` |
| `delegation.created` | **`Task.delegate` only** — the DelegateTool (#39) calls it and emits nothing; key `{parentTaskKey}:child:{childId}` | `{ parentTaskId, childTaskId, assignee, owner, objective, callId, constraints (after the split), environmentId? }` | `agent:<parent assignee>`; agentId = parent assignee, taskId = PARENT, sessionId = the delegating session |
| `environment.chosen` | Routing `run`, before placement (key `{taskKey}:environment`), and again from the `fallback-api` branch (key `…:environment:fallback`) | `{ runtime, environmentId?, policy, fallback, from? {runtime, environmentId, machineId?}, why }` | `system:routing`; agentId, taskId |
| `task.transition` | Task, every edge (key `{taskKey}:transition:{n}`) | `{ from, to, why, wait? }` | the transition's `by`; agentId = assignee, taskId, sessionId? |
| `config.versioned` | Agent `update` / `rollback` / an accepted proposal (key `{agentKey}:v{n}`) | `{ version, reason, rollbackOf? }` | the version's `by`; agentId |
| `proposal.reviewed` | Agent `reviewProposal` (key `{agentKey}:proposal:{id}:review`) | `{ proposalId, decision, reason?, version? }` | the reviewer; agentId |
| `machine.paired` / `machine.revoked` | Machine `pair` / first `revoke` (keys `{machineKey}:paired` / `:revoked`) | `{ machineId, name, os?, daemonVersion? }` / `{ machineId, name }` | the caller |
| `plugin.enabled` / `plugin.disabled` / `plugin.granted` | Registry, only when the call changes something (`granted.scopes` = the NEW scopes) | `{ pluginId }` / `{ pluginId, scopes }` | the owner |
| `plugin.activated` | Registry `activate`, only when the active plugin of the kind changes | `{ pluginId, kind, previous? }` | the owner |
| `secret.opened` | Registry `openSecret`, every call (key = instant + counter) | `{ name, pluginId }` | the caller (owner or the agent principal) |
| `workdir.worktree-created` | Machine, when the daemon's `fs.response` reports a worktree it added (#189; key `{machineKey}:worktree:{requestId}`) | `{ machineId, environmentId, repo, branch, path, base? }` | the owner who called `fsRequest` |
| `environment.put` / `environment.removed` | Machine, when the daemon answers `putEnvironment` / `removeEnvironment` — accepted or refused (#237; key `{machineKey}:env:{requestId}`) | `{ machineId, environmentId?, name, runtime, cwdRoots, outcome }` / `{ machineId, environmentId, outcome }` | the owner who asked |

Not yet recorded: the Workspace's `exportAll` / `deleteAll` neither export nor purge the audit keys (follow-up with #48's store), and `Registry.revoke` / `rename` are not in the catalogue.

### Schedule

`packages/platform/src/schedule` (issue #28). One actor per entry, keyed `{ws}:schedule:{id}`, `defineScheduleActor({trigger: TriggerPort})`. Recurrence is `{kind: 'at', at}` (one-shot, epoch ms) or `{kind: 'cron', cron, tz}` — a 5-field cron subset (`*`, `n`, `a-b`, `a,b`, `*/n`, `a-b/n`; no names, no `L`/`W`/`#`) evaluated on the wall clock of an IANA zone. `next` is computed in `recur.ts` from `Intl.DateTimeFormat` parts only (no dependency, edge-safe); an unknown zone or bad expression is rejected at `create`/`update`, never stored.

Each occurrence is armed as a ONE-SHOT reminder (`ctx.reminders.set('fire', {due: next - now})`), re-armed from `onReminder` — the runtime's 60 s period floor is never requested, and on Cloudflare the entry's own alarm fires it with no machine or browser online (AST-03/04). `onReminder` posts a `ScheduleFired {workspaceId, scheduleId, key, kind, title, scheduledFor, firedAt, occurrence, skipped, agentId?, environmentId?, prompt?, offlinePolicy}` to the `TriggerPort` together with a `TriggerHop` (the actor's own `ctx.actor`, so the port reaches other actors as trusted hops — a reminder runs with no principal, and an in-process `actor()` from the port would be refused by `sameWorkspace`), then re-arms. A rejecting port is retried one floor (60 s) later up to 3 attempts, then the occurrence is logged `dropped` and the calendar moves on. Every mutation ends in `ctx.save()` inside the turn.

**Trigger (#42)** — `scheduleTrigger({environments?})` in `schedule/trigger.ts` is the platform's port, registered by `apps/web/src/actors.app.ts`. A `reminder` entry, or any entry without an `agentId`, becomes one Inbox notification of kind `reminder` (`body` = the prompt, `ref {kind: 'schedule'}`) through `Inbox.push`, so every configured channel delivers it from the entry's own alarm (AST-04, AC-08). An entry with an agent becomes a Task owned by and assigned to that agent, `origin {kind: 'schedule', scheduleId}`, objective = prompt (title when absent), with the id `scheduledTaskId(scheduleId, scheduledFor)` — deterministic and `Task.create` idempotent, so the actor's retry of a firing that threw half-way finds the task it already routed (exactly once per occurrence). AST-05: with an `environmentId` the trigger asks the `EnvironmentProbe` (the Machine actor's seam, #36; absent = every environment is offline). Offline under `queue` or `fallback-api` → `reportWaiting({kind: 'environment-offline', environmentId, policy})` — recorded for the router (#37) to resolve, never routed here; under `fail` → `fail({code: 'environment-offline'})` plus a `task-failed` notification. Online, or no environment → the task stays `queued`.

**DST rules (AST-07)**, fixtures in `__tests__/schedule/recur.test.ts` for Europe/Stockholm 2026:

- *Spring gap* — a wall time that does not exist (02:30 on 29 Mar, when 02:00 CET → 03:00 CEST) is skipped; `30 2 * * *` fires 28 Mar 01:30Z, then 30 Mar 00:30Z.
- *Fall overlap* — a wall time that exists twice (02:30 on 25 Oct, when 03:00 CEST → 02:00 CET) fires on its FIRST occurrence only (the CEST instant, 00:30Z); the repeated hour never fires a second time, whatever the minute pattern, so `*/30 * * * *` goes 00:30Z → 02:00Z that morning.
- *Catch-up policy `skip`* — a reminder that runs late (host down, alarm delayed) fires once for the occurrence it was armed for with `skipped = ` the number of later occurrences that fell before "now", writes a `skipped` log entry, and re-arms strictly after "now". Never a burst.
- Reminders are "at or after due" with the host's tick as resolution (60 s on the sharded table; exact on DO alarms); an entry due sooner than a tick fires on the next tick, never early.

## 5. Session execution paths

### 5a. Platform-managed API agent (`anthropic-api`)

The Session actor is built once per app over its ports (`packages/platform/src/session/ports.ts`): `defineSessionActor({ factory, commands })`. The `SessionFactory` maps a runtime id to an `OpenedSession` (`{ session: AgentSession, agentId, capabilities }`) or `null` for a daemon-hosted runtime; `@agentic/runtimes` supplies the `modelAgent` one, tests plug `mockAgent`. The actor never imports an adapter.

Which runtimes exist is the app's `RuntimeCatalogue` (#230, `packages/platform/src/routing/factory.ts`): runtime id → `RuntimeImpl`, `{ host: 'local', open(context, plugin) }` or `{ host: 'daemon' }`. The router and `createSessionFactory({ runtimes, registry })` share it: a `local` runtime is placed in-process and needs no environment, a `daemon` one goes to a machine, an id the catalogue lacks fails `unknown-runtime`. `anthropicApiRuntime(options)` is the built-in local one. Before any of it `Routing.run` asks `Registry.gate({ runtime })` once (§9): missing or turned off → the task fails `plugin-disabled` and nothing opens; the answer is recorded on the route and on the session spec (`SessionOpenSpec.plugins`), and the plugin's `config.defaultModel` is the session's model where the agent names none. A `fallback-api` (§7) asks the gate again for `anthropic-api`.

```ts
// open: record the spec — replaced on every re-open (#393) — open the runtime session (resume from the recorded ref), serve it in-process
const opened = await factory(spec.runtime, { spec, resume: state.ref, signal: ctx.abortSignal, transcripts, currentTaskId: () => currentTaskId(state) });
const served = serveSession(opened.session, { agentId, capabilities });   // command validation, busy/closed, one reply per commandId
// prompt(input, turnId, output?, commandId = turnId, { taskId }): idempotent by commandId (persisted), then the turn runs as a detached task
const reply = await served.handleCommand({ v: 1, commandId, type: 'prompt', turnId, input });
if (reply.kind === 'ack') { state.running = { turnId, commandId, input, taskId: taskId ?? spec.taskId }; await ctx.tasks.start('drive', { turnId }); }
// drive: subscribe from the durable head, one turn per event, until this turn's turn-end
for await (const ev of session.subscribe(state.head)) await ctx.turn((c) => appendEntry(c, { t: 'ev', ev }));
```

- **The turn owns the task** (#390; COL-04, OPS-08, MEM-07, AGT-06). A session serves many tasks over its life (§7), so `spec.taskId` is only the task it OPENED with. The prompt names the turn's task (`prompt(…, { taskId })`, the route's task from the router); on the daemon path it rides the command record (`CommandRecord.taskId`, never the `WireCommand`), so the ack that arrives later through `commandReplied` still starts the turn under it; `RunningTurn.taskId` holds it while the turn runs and `currentTaskId(state)` (`session/state.ts`) — `running?.taskId ?? spec?.taskId` — is what everything task-attributed reads: the minted agent principal, the audit rows of a request and its decision, `SessionRequestView.taskId`, the follow-up of a detached answer, the Ledger row and the budget verdict (`recordUsage`), the learning hook (`learnFromTurn`, whose plugin is built for that task), the chat message published at turn end, and a correction's origin. A `turn-end` drops `running`, so the driver reads the task before the fold and hands it to `finishTurn`. The tool ports read it per call, never once at open: `SessionFactoryContext.currentTaskId` → `createActorToolPorts({ taskId })` → `principalNow()` overlays it on the minted identity for every hop (`delegate`'s parent, `task_report`, a memory's provenance, `chat_post`'s task, the mentions a post activates). A sealed agent token (`auth/agent-token.ts`) therefore carries only the opening task; it is never the source of the task.

- `createPlatformModelAgent(cfg, { ports, anthropic: { apiKey }, store, skills, memories })` (`packages/runtimes/src/anthropic`) builds that `agent`: the roster is the platform tools the config grants (`deny` and skills never add one, AGT-04), `system` is assembled stable-first (identity, role, instructions, skills, tools, then the memory block), `pricing` is the `claude-*` table with `estimated: true` for an unlisted id, and it returns a `CapabilityReport` (AGT-09) plus `usageRow(event, …)` for the Ledger. Platform tools are `defineTool`s over abstract ports (`MemoryPort`, `TaskPort`, `ChatPort` in `packages/runtimes/src/tools/ports.ts`) the Session supplies under the agent principal; on the wire they are `memory_search`, `memory_remember`, `delegate`, `chat_post`, `chat_file_read`, `task_report`, `ask_user` (provider tool names allow `[A-Za-z0-9_-]` only). `chat_file_read` (#206) reads a chat attachment by its `agentic-file:` URI through the optional `PlatformPorts.files` (`ChatFilesPort.read`, the access decision is the platform's): text for a text file, cut at `CHAT_FILE_TEXT_MAX_BYTES` and flagged `truncated`, only a note with name, type and size otherwise; a host without the port fails the call. `chat_post` takes optional `attachments` URIs, passed on as `ChatPost.attachments`. `ask_user` goes through `ChatPort.ask` — the engine never raises an input `request` itself; the platform does (#122, below).
- Every event is durable before the next is read; the reducer (`applySessionEntry`) is pure and idempotent by `(epoch, seq)`, so a frame replayed twice folds once. At a turn end the driver folds the new events into the transcript snapshot, refreshes `ref`, publishes the final assistant text on `SESSION_EVENTS_TOPIC` keyed by the chat's actor key, and saves.
- **Where a ref comes from** (#389). The record holds only a ref a runtime actually gave it, so a placeholder can never be replayed as a resume token. Local path: `finishTurn` reads it from the live `AgentSession` at every turn end (and `ensureLive` when it opens one). Daemon path: only the daemon's `session.ref` frame, through `Machine.onSessionRef` → `Session.noteRef` (§5b) — never the synthesized `hello`, whose `sessionRef` is what `session.opened` carried before the first prompt (a `cc_…` placeholder for Claude Code). `open()` seeds `ref` from `spec.resume` when the spec is first recorded: that value is an earlier record's ref, so it is real. Nothing else writes it.
- **Re-open** (#393; EXE-12, EXE-13, MEM-07). `open()` on a record that is already open and not closed re-opens it: the spec is replaced by this placement's — the task, its objective and context, the memories `withRetrievedMemory` retrieves for them (retrieval ranks on the current objective, once per re-open), the machine, environment and folder (so `assertHostingMachine` admits the daemon the session comes back on after a restart on another connection) — and the status is `idle` again. A local runtime session that is gone is opened again by `ensureLive` from the recorded ref; on the daemon path the router passes that same ref as `OpenSpec.resume`. The ref itself is seeded from `spec.resume` on the first open only; a re-open never touches one a runtime reported. While a turn runs the record is left as it is (#395 decides what a mid-turn message does). `session-started` goes to the chat once, at the first open: the chat's binding (§6) is for the session's life, and the two statuses bracket it — a chat member's session is not closed when its task settles (§7).
- **The record's budget** (#198, #391; the Session row in §4 has the numbers). A live chat session (one per chat member, never parked) runs for weeks, so nothing in the record may grow with the number of turns: the window is paged, the index holds the last `INDEX_TURNS` turns and no prompt (`IndexedTurnStart`: cursor, turn id, bytes), a replied command keeps only its id, type and reply, and the transcript snapshot is bounded. What a reader needs from further back it reads from a page: `events(from)` / `tail(from)` through `eventsSince`, a `resume()` whose interrupted turn streamed its own start out of the window through the one page holding it (`eventAt`). The only whole-set readers are `get().grants` and `requests()`; `request(id)`, `respond`, `resolution`, `raiseInput` / `detachInput`, the decision's chat / Inbox / audit bookkeeping, `finishInterrupted` and learning look one record up (`findEvent`, `requestById`) without materialising the log — a tool call's approval path is O(window), not O(session lifetime).
- Eviction mid-turn loses nothing already saved. The runtime's task ledger restarts `drive({ turnId })` on the next activation; it finds no live session for that turn and closes it honestly: `tool-update cancelled` for calls still open, `request-resolved {outcome: cancel}` for open requests, `error {code: 'process_exited', recoverable: true, data: {interrupted: true}}`, `state idle`, then `turn-end {stopReason: 'error'}` — `isInterruptedTurnEnd(ev)` names it. The Task driver reads that and marks the Task `waiting {input}` with "Resume?". A model call is never replayed (OPS-05/06). A live session opened afterwards resumes from `ref` in a new epoch, so the log stays gapless per epoch.
- Approvals: `request` event → Task `waiting {approval}` + Inbox notification + Chat status entry → user responds → `Session.respond` reaches the live `AgentSession`. A decision that arrives after an interruption is a no-op ack: the interrupted turn is closed, not resumed.
  - As implemented (#40, `packages/platform/src/session/actor.ts` `appendEvent`, `src/policy/requests.ts`): the Session announces every request itself, on both paths — an `approval` / `input` Inbox notification with a `{ kind: 'session', sessionId, requestId }` ref (`SessionPorts.inbox`, one-way) and a chat status `request` with `ref` `approval:{id}` | `input:{id}`; the `request-resolved`, whoever caused it (`Session.respond` from the chat card, the inbox page or an external client; a policy rule; a timeout; a cancel), marks that notification read (`Inbox.ackRef`) and appends the pairing `request-resolved` status; the audit `approval.requested` / `approval.resolved` are emitted once each as before. `Session.request(id)` and `requests({ openOnly })` are live reads of `SessionRequestView` — the request joined with its `tool-call` (input, category, annotations), the decision, the agent / chat / task, and `rule: describeRule(ruleFor(approvalPolicy, …))` — so every client renders one record and the first `respond` wins (a second is an idempotent ack). A `session`-scoped allow is a grant: `SessionInfo.grants` lists them from the log (`sessionGrantsOf`); revoking is not offered — `@sigx/ai-agent` checks its private `SessionGrants` before the policy runs, so nothing of ours can undo one, and a daemon harness keeps its own; the control is not rendered until an adapter exposes them (AC-15). On the daemon path the same policy is compiled by the daemon from `OpenSpec.policy` (#121, §5b); what the harness still asks on its own terms (`permissions.every-call` unsupported for Claude Code) goes through the same inbox / chat / respond flow.
  - **Platform-raised input requests** (#122, `Session.raiseInput`, `packages/platform/src/routing/tools.ts` `chat.ask`): `ask_user` has no way into the runtime's own request flow, so the tool port (in-process on the local path, through the Machine's `tool.call` on the daemon path) asks the Session to raise `request { kind: 'input', requestId: 'ask:{callId}', callId, toolName: 'ask_user', message, options }` for the call — idempotent by call id, so a daemon re-sending an open `tool.call` after a reconnect re-finds the same question — then tails `Session.resolution(requestId)` until a decision lands; the `answers` are the tool result (`answerText`), a `cancel` a `cancelled` tool error. The event is the running turn's and is stamped by `platformCursor(head)` — strictly after the head, strictly before the runtime's next integer `seq` (fractional on purpose) — so it never collides with, nor skips, a runtime event: `applyEvent` folds by cursor, the local driver subscribes from the head, the Machine replays a daemon from its own cursor. On the local path the Session drains what the runtime already buffered first (the `tool-call` itself), so the question lands after its call. `respond` settles a platform-raised request itself (`platformRequests` on the state; a synthetic `request-resolved { outcome: 'input' | 'cancel', by: 'client' }`, a permission decision refused as `invalid`, a second decision an idempotent ack) — never sent to a runtime that does not know it. Everything else is the #40 flow unchanged: the chat `request` / `request-resolved` statuses, the `input` Inbox row and its ack, the router's `follow` parking the Task `waiting {input}` and releasing it, `Session.request(id)` rendering the card with the call's input; an input request is not audited. An eviction mid-question closes it like any open request (`finishInterrupted` → `request-resolved cancel`).
  - **Questions outlive the call** (#285). A blocking tool call cannot hold a question for hours: the engine's MCP tool timeout, the daemon's `toolTimeoutMs` and the router closing a chatless session at turn end all end it first. So in a chat `chat.ask` waits only `ASK_QUICK_WAIT_MS` (25 s, `askQuickWaitMs` on `createActorToolPorts` / `createToolCallPort` / `createSessionFactory`): a quick answer is still the tool result. After that it calls `Session.detachInput(requestId)` and answers `{ status: 'pending', questionId, note }` (`AskOutcome`, `packages/runtimes/src/tools/ports.ts`); the tool description tells the model to end its turn. `detachInput` is atomic with `respond`: an answer that beat it is returned instead, never lost. A detached request (`detachedRequests`, or any open platform request once the session is closed) stays open: `finishInterrupted` skips it, the close does not cancel it, `SessionRequestView.detached` says so (with `agentName`), and the router's `follow` releases the Task from `waiting {input}` at turn end so it completes. `respond` takes a platform request's answer before the `closed` check, so it is accepted on a closed session. An answer to a detached request is parked in `answeredDetached` and, once the session is closed or no turn runs on it (#393: a chat member's session is idle between turns and never closed at turn end, so `isDetached`, `respond` and `deliverAnswers` all gate on `closed || !running`, and `finishTurn` starts the delivery for answers that came mid-turn), handed one at a time to `SessionPorts.answered` by the `deliverAnswers` actor task, outside any turn (the follow-up reads this session back) and restarted after an eviction. `createAnswerFollowUp({ routing })` (`packages/platform/src/routing/answers.ts`) implements it: it posts the answer in the chat as the person who gave it, mentioning the asker (`@Ada — re: “…” → …`); then it creates the asker's task, whose id is `answerTaskId(sessionId, requestId)` so a replay starts nobody twice. The task comes from `answerContract`: its origin is that message, its context the question, the answer and the chat, `resumeFrom` the asking session, and its depth the asking task's. It is placed with `Routing.run` as the workspace user. A follow-up that throws is said in the chat (`task` status `answer-not-delivered:…`). In a chat the follow-up is a chat task, so it runs in the asker's own live session (#393: the chat's binding names it, and it is the `resumeFrom` session itself) — the engine still holds the question, and the prompt is the answer as the objective (`The answer to your question “…”: …`) plus whatever the chat said since, not the contract's context. Only when that session cannot go on (§7) does a fresh one open: then the router resumes the engine conversation (`OpenSpec.resume` = the earlier session's `ref`) only when a daemon ran it for the same agent, runtime, environment and machine; a local API session's transcript lives in its own Session record, so that path opens fresh on the contract's context. A chatless session (a delegated child) keeps the blocking wait: there is no chat to start it in. In the web app the Inbox card and a chat card for an open question whose session left `sessions` (`detachedQuestions`: the chat's `input` statuses, the session from the unread Inbox row) show who asked and, when stale, that answering starts that agent again (`QuestionPrompt` `stale`).
- `tail(from)` replays the log from `from` (exclusive) out of a state snapshot, then follows `ctx.changes()` until the session closes; `{ epoch: 0, seq: 0 }` gives a late joiner the whole log, and reducing it equals the driver's snapshot.
- Limits `{maxTurns, maxSteps, maxTokens, maxCostUsd, maxWallMs, maxDepth, maxConcurrentChildren}` enforced in the driver; every `usage` event → Ledger (estimated flag when pricing is unknown). As implemented (#45, `packages/platform/src/ledger`): `defineSessionActor({ factory, usage: ledgerRecorder() })` — after each **turn-scoped** `usage` event is durable (session-scoped events are cumulative totals, never counted) the driver prices the row with the `OpenedSession.usageRow` the factory returned (`createPlatformModelAgent(...).usageRow`; `estimated: true` for an unlisted model), else records the event's own `costUsd` as reported, else an unpriced row (provider data unavailable); the row's `key` is `{sessionId}:{epoch}:{seq}` so a replay folds once. `ledgerRecorder` appends it to `{ws}:ledger:{yyyy-mm}` one-way (the books never stall a turn) and, when the session works a task, charges `Task.recordUsage`; a `failed {budget}` task comes back as the verdict and the driver cancels the running turn — on the served session locally, through the `CommandSink` on the daemon path. `maxCostUsd`, `maxTokens` and `maxWallMs` are what the Task can measure today; `maxTurns` / `maxSteps` are runtime-side (`modelAgent({ maxSteps })`, `claudeCode({ maxTurns })`).
- API keys are user-provided (BYO), encrypted in Registry with `WORKSPACE_KEK`. As implemented (#230): a local runtime reads a secret its manifest declares through `RuntimePluginAccess.secret(name)` → `Registry.openSecret(name, pluginId)`, called as the workspace owner (the session's agent principal does not exist yet), so the plugin must be enabled and hold `secret:<name>`, and every open is audited `secret.opened` — once per session open (and once more if an evicted session is re-opened), never per turn. The plaintext lives for the length of the `open` call and is never cached in the isolate or written to a record. No `anthropic-api-key` → the open fails `no-api-key` naming `/plugins/anthropic-api` (task `session-open`); a plugin turned off between the gate and the open → `plugin-disabled`. Until #231 wires the catalogue into `apps/web`, the deployment still runs the deprecated `anthropic` resolver (no `registry` option → no secret lookup).

### 5b. Daemon-hosted Claude Code

The daemon runs one `claudeCode({ settingSources: [], env: { CLAUDE_CONFIG_DIR: env.profileDir }, models })` per environment. Sessions open with `cwd` from `cwdRoots`, `system` = instructions + skills + retrieved memory block with `systemPromptPreset: true`, `maxTurns` / `maxBudgetUsd` from limits, policy compiled from the agent's approval rules — as implemented (#121): the router fills `OpenSpec.policy = { rules: config.approvalPolicy, grants: config.tools, constraints?: route.constraints }` in `placeRemote`, the Machine passes it through opaquely, the daemon's `openSession` compiles `sessionPolicyOf(spec.policy)` (the SAME `sessionPolicy` the local path uses, moved down to `packages/runtimes/src/policy` and re-exported by `@agentic/platform`) and hands the `Policy` to `driver.open(env, spec, { policy })`; the Claude Code driver forwards it as the session's `policy` with `interactive: true`, so the harness asks the platform only what the rules say to ask, and a delegated child on a machine is never wider than its ancestors. Without a `policy` on the spec the driver opens with none and the harness asks on its own terms. Per session: `serveSession(claudeSession, { agentId, capabilities, eventLog: diskLog })` and pump `served.events(from)` upward.

**One hibernatable WebSocket** from the daemon to its Machine DO carries the daemon-protocol envelope (types in `@agentic/core` as `DaemonFrame<F, R>` / `PlatformFrame<C>`, generic over the `@sigx/ai-agent/wire` frame, reply and command types so core stays dependency-free; `@agentic/daemon-protocol` instantiates and validates them):

```
daemon → platform: hello {machineId, daemonVersion, os, environments[], capabilities[], resume: {sessionId → cursor}, policy?}
                   env {environments[], policy?} · heartbeat · session.opened {sessionId, ref, capabilities, head}
                   session.ref {sessionId, ref}   (#388: the runtime's own id, once it names the session and on every change — the only ref a record resumes from)
                   session.frame {sessionId, frame: WireFrame} · session.reply {sessionId, reply} · session.closed {sessionId, reason}
                   tool.call {callId, sessionId, tool, input} · pong
                   fs.response {requestId, exactly one of result: FsResult | error: {code, message}}
                   env.response {requestId, exactly one of result: {environmentId} | error: {code, message}}
                   quota {environmentId, snapshot: QuotaSnapshot}   (unsolicited, #261: provider limits; stream snapshots are partial → mergeQuota)
platform → daemon: welcome {serverTime, wanted: {sessionId → cursor}} · session.open {sessionId, environmentId, spec}
                   session.command {sessionId, command: WireCommand} · session.close {sessionId}
                   tool.result {callId, exactly one of output | error} · ping
                   fs.request {requestId, environmentId, op: list {path} | worktree {repo, branch, base?, path} | locate {origin, depth?}}
                   env.request {requestId, op: 'put', environment: EnvironmentInput} | {requestId, op: 'remove', environmentId}
```

**Folder browsing** (#185, frames #187, locate #331): `fs.request` asks a daemon for the immediate subfolders of a folder, to `git worktree add` a new branch, or for every checkout of a repo's origin, always inside the named environment's `cwdRoots`. `fs.response` answers with one of three things:
- a listing `{ path, parent?, git?, entries: [{ name, path, git? }] ≤ FS_LIST_MAX_ENTRIES, truncated }`, where `parent` is absent on a root and `git` is `{ kind: repo | worktree, branch? | head?, origin? }` — `origin` is the `origin` remote's URL as written in the repo's config, the repo's identity across machines (compared with core's `sameOrigin`);
- the added worktree `{ path, branch }`;
- the located checkouts `{ origin, matches: [{ path, git }] ≤ FS_LOCATE_MAX_MATCHES, truncated }`, roots first, shallowest first.

Refusals are named: `outside-roots`, `not-found`, `not-a-repo`, `branch-exists`, `invalid-branch`, `exists`, `timeout`, `unknown-environment`, `unsupported` and `internal`. The shared lexical check is core's `pathWithin`, and the daemon additionally resolves symlinks. `daemonConformance` has `fs-list` and `fs-locate` cases for daemons that declare the `fs` feature (`fs-locate` proves a match when the harness names a `knownOrigin`). As implemented in `agentic-daemon` (#188, `apps/daemon/src/fs.ts`): every path is checked lexically first (answered `outside-roots` before the disk is touched), then again after `realpath` of the path and of every root, and all reads go through the resolved path; `session.open` runs the same check on its `cwd` and refuses a missing one. Git badges are read from `.git` / `HEAD` / `config` files, never by running git — `origin` from the common config (`<gitdir>/config`, or for a worktree the config its `commondir` points to). A `locate` walks each root breadth-first at most `min(depth, FS_LOCATE_MAX_DEPTH = 3)` levels down, skipping `node_modules` and dot-folders, following a symlink or junction only when it resolves to a folder inside the roots and never visiting a resolved folder twice, and stops at `FS_LOCATE_MAX_MATCHES = 20` with `truncated: true`; a root missing on disk holds nothing. `worktree` runs `git -C <repo> worktree add -b <branch> -- <path> [base]` through `execFile` with no shell and a 60 s timeout, after `git check-ref-format --branch`.

**The `agentic-daemon` command** (#354, `apps/daemon/src/launcher.ts`): the daemon ships as an unpacked folder started by a service, so nothing puts its CLI on `PATH` — yet every instruction the product gives (Machine page, Pair page, both READMEs, the runbook) is spelled `agentic-daemon …`. The install scripts call `launcher install`, which writes a launcher that hard-codes the Node and entry the install resolved (so it runs where no Node is on `PATH`) and then takes the least invasive route onto `PATH`: its own folder already there → nothing, `~/.local/bin` or `~/bin` on `PATH` → a symlink, otherwise a fenced block in the profile `$SHELL` names, or the Windows **user** `PATH` via `[Environment]::SetEnvironmentVariable`. `launcherPlan` decides all of it from injected `platform` / `env` / `home` and touches no disk, so every OS's decision is tested from any one of them; `launcher remove` (the uninstall scripts) undoes each route. The web end is the Machine page's "command not found?" disclosure (`pages/machines/manage.ts` `fallbackCommand`), which spells the same command as `node <install folder>/bin/agentic-daemon.mjs …` for a machine installed before this.

**`agentic-daemon open`** (#336, `apps/daemon/src/open.ts`): the command-line entry into a chat, and no daemon frame — the daemon resolves the folder locally and the web resolves everything else from the query. `open [path] [--env <id>] [--no-browser]` takes the folder (default the current directory) and, over the daemon's own `environments.json`, finds the environment whose `cwdRoots` contain it with `checkWithinRoots` (lexically, then with links resolved, so the answer is the one `fs.request` would give); no environment → exit 1 with every environment's roots listed; several → exit 2 naming them until `--env <id>` picks one (checked the same way, exit 1 when the folder is not under its roots). It reads the folder's `gitInfo` for the `origin` (absent when the folder is not a checkout), prints `<platform>/chats/new?env=<id>&path=<lexical native path>[&origin=<url>]` (each value `encodeURIComponent`-ed; the platform URL from `credentials.json`, normalized as `pair` normalizes it) and opens it with the OS opener (`cmd /c start "" "<url>"`, `open`, `xdg-open`) unless `--no-browser`; the link is printed before the opener runs, so a headless box still gets it, and an opener that fails is a note, not a failure. Unpaired, or an unloadable `environments.json`, is exit 1 like `run`. The web end is §10 "Entry points".

**Web-managed environments** (#224, frames #236; decisions 2026-09-19 (c)): `env.request` asks a daemon to create or change (`put`, an upsert) or to `remove` an environment. `EnvironmentInput` is `{ id?, name, runtime, cwdRoots, concurrency?, accountLabel? }`.
- It has **no `profileDir`**. The daemon allocates one per environment, and its schema is strict: a frame whose input carries any key the contract does not name fails validation instead of having the key stripped.
- `env.response` answers with `{ environmentId }` or a named refusal: `policy-disabled`, `outside-allowed-roots`, `unknown-runtime`, `in-use`, `unknown-environment`, `invalid`, `io` (and `timeout`, which only the platform sets).
- The descriptors themselves travel in an `env` frame that comes with a `result`, before or after it.
- `hello` and `env` carry the optional `policy: { webManaged, allowedRoots }`, the machine-local policy. It is edited only on the machine and nothing on the wire changes it; a daemon that predates it sends none.
- `daemonConformance` has `env-put`, `env-remove` and `env-policy` for daemons whose harness declares the `env-manage` feature. `agentic-daemon` does not declare it until it answers the frame (#238); the Machine actor's end is below.

The Machine actor is the platform end (#189). The daemon's reply arrives as a separate `socketMessage` turn on the same actor, so the answer is a **stored result**, never an awaited promise:
- `fsRequest(environmentId, op)` → `{ requestId }` (`fs_<uuid>`) validates the op against the protocol schema (400), refuses a revoked machine (403), an unknown environment (404) and an offline machine or one with no open socket (503), sends `fs.request` and stores `fs[requestId] = { environmentId, op, status: 'pending', requestedAt, deadline, by }`. `list` is open to session drivers (a user, an agent, an external client with `sessions`); `worktree` changes the machine and `locate` walks every root of it (#332), so only its owner (a user principal) may ask for those — checked inside the method, since `methodAuthorize` sees only the method. The router's driver is the workspace user, so a project feature plugin's `fs` (§7) may ask for both.
- `fsResult(requestId)` → `FsResultView { requestId, environmentId, op, status: pending | done | error, requestedAt, finishedAt?, result?, error? }`, 404 for an unknown, evicted or pruned id; one string argument, so the UI reads it live with `useActorState(defs.Machine, () => [k, 'fsResult', id], { live: true })`. Same principals as `fsRequest`.
- The `fs.response` case stores the result or error. An unknown id is ignored, and so is a second answer, except over a `timeout`: a late answer says what the daemon did. A result of the wrong kind is stored as `internal`.
- Bounds: a pending request fails `{ code: 'timeout' }` at its deadline (`fsTimeoutMs`, 30 s) through the `liveness` reminder, which stays armed while any request is pending; `socketClosed` fails every pending one with `timeout` "machine went offline" (`revoke` with "machine revoked"). At most `MAX_FS_REQUESTS` (16) entries are kept, the oldest evicted first, and finished ones are pruned `FS_RESULT_TTL_MS` (2 min) after they finished (on the next `fsRequest` or reminder tick).
- A successful `worktree` records `workdir.worktree-created` (§4 Audit) under the principal that asked, keyed `{machineKey}:worktree:{requestId}`.

Environment requests use the same machinery (#237):
- `putEnvironment(input: EnvironmentInput)` / `removeEnvironment(environmentId)` → `{ requestId }` (`env_<uuid>`) send `env.request` and store `envRequests[requestId] = { op, status: 'pending', requestedAt, deadline, by }`; `envResult(requestId)` → `EnvResultView { requestId, op, status, requestedAt, finishedAt?, result?, error? }`, read live like `fsResult`. The input is parsed by the strict `environmentInput` schema (400, so a `profileDir` never leaves the platform); 403 revoked; 503 `machine-offline` with no daemon or no open socket. `removeEnvironment` also refuses an environment the machine does not report (404) and one this machine hosts or queues a session in (409 `in-use`); the daemon checks its own side too.
- **Owner only, and on no tool surface.** An agent must not widen where agents may work (decisions 2026-09-19 (c)): `methodAuthorize` admits a user principal only, and a source scan (`packages/platform/__tests__/machine/tool-boundary.test.ts`) fails if the platform tools, the daemon `tool.call` port or the platform MCP server ever call the methods.
- The daemon decides. Its refusal (`policy-disabled`, `outside-allowed-roots`, `unknown-runtime`, `in-use`, `invalid`, `io`, `unknown-environment`) is stored unchanged; the descriptors arrive with the daemon's `env` frame. Timeout, disconnect, revoke, the late-answer rule and the bounds (`envTimeoutMs` 30 s, `MAX_ENV_REQUESTS` 16, `ENV_RESULT_TTL_MS` 2 min) are the folder requests'.
- Each answered request is recorded once — `environment.put` / `environment.removed`, keyed `{machineKey}:env:{requestId}` — with the roots that were asked for and the outcome (`ok` or the daemon's code). An unanswered one is not: whether the daemon applied it is unknown until it answers.
- `MachineView.policy` is the `MachinePolicy` from the last `hello` (dropped when a daemon sends none) and any `env` that carries one, so the Machine page can explain "web management is off on this machine" and offer the local command.

- Machine DO routes `session.frame` → `actor(Session, id).forwardFrames(frames)` (one-way; `hello` records capabilities and lifts `disconnected` — not the ref it carries, which is the open's placeholder; `event` folds, `gap` marks `disconnected` and moves the head); `session.ref` → `Session.noteRef(ref)` after updating the hosted session's own `ref` (#389: the runtime's id, appended only when its identity — `id`, `data.epoch` — changed; a session the machine does not host is ignored); `session.reply` → `Session.commandReplied` (a prompt ack marks the turn running, its `turn-end` frame snapshots the transcript and posts to the chat); `tool.call` → the platform tool under an **agent principal** `{kind: 'agent', ws, agentId, sessionId, taskId}` so Memory/Task authorize correctly. The Machine mints that principal from the task the session was OPENED for (`HostedSession.taskId`, fixed at `openSession`) — identity only: the turn owns the task (#390, §5a), so `createToolCallPort` (`routing/tool-call.ts`) reads the Session once per call and rebuilds the principal with `running.taskId ?? spec.taskId` before the tool runs; the same read gives `chat_post` / `ask_user` their chat and the memory tools their gate. One Session hop per daemon tool call, on a path that is already detached from the socket's turn. A Session's refusal of a `session.frame` or a `session.reply` (403 from `assertHostingMachine`: a session this machine does not host on the record — stale after a daemon restart, re-opened elsewhere, never opened here) is swallowed by the Machine (#393): the frame is dropped and the socket stays up, since the host's `webSocketMessage` would otherwise take every other session on it down with the throw.
- Session → daemon: `Session.prompt` records the command as pending (idempotent while the reply is out) and hands it to the `CommandSink` port — the app binds it to `actor(Machine).sendCommand(sessionId, cmd)` → socket. Pending replies are stored in Machine state with a deadline so evictions do not leak promises.
- Reconnect: the daemon redials with backoff; `welcome.wanted` cursors drive gapless replay from the daemon's per-session NDJSON log (`%LOCALAPPDATA%/agentic/sessions/{id}.ndjson`); beyond that a `gap` marks the Session `disconnected` with "events lost" (OPS-04, distinct from failure).
- **Driver seam** (`@agentic/core` `RuntimeDriver<S, P>`): the daemon reads `LocalEnvironment` rows from `environments.json`, asks the driver to `inspect(env)` (auth status, isolation, `CapabilityReport`) and runs its `doctor(envs)` for `hello` / `env` via `toEnvironmentDescriptor` (the per-environment verdict `EnvironmentDescriptor.doctor = environmentVerdict(report, id, checkedAt)`; a throwing driver gives each of its environments an error verdict `driver-doctor-failed`), `open(env, OpenSpec, { sessionId, callTool, policy })` → `{ session, capabilities }` on `session.open`, and runs `doctor(envs)` for `agentic-daemon doctor`. `callTool` is how a driver's platform tools reach the socket as `tool.call`. The daemon never branches on the runtime id; it picks the driver whose `runtime` matches the row.
- Multi-account isolation = `CLAUDE_CONFIG_DIR` per environment (the adapter's env allowlist passes it through). `claudeCodeDriver` (`@agentic/runtimes/claude-code`, #20) sets it from `profileDir` and removes the daemon's own `CLAUDE_CONFIG_DIR` and `ANTHROPIC_*` from every child, so an environment can only ever use its own account; `inspect` reads auth status from `<config dir>/.credentials.json`; every session runs with Claude Code's cross-session tools (`ListAgents`, `SendMessage` — they reach the operator's other Claude Code sessions, not platform agents, and run without a permission request) in the SDK's `disallowedTools` (#194, `withoutCrossSessionTools`); the memory block reaches Claude Code as `## Platform memory` and the `CapabilityReport` lists `memory.runtime` (CLAUDE.md, settings — not loaded, `settingSources: []`) as unsupported (MEM-10). macOS Keychain behaviour is unverified, so v1 validates Windows only; `doctor()` implements EXE-07 with the codes in `CLAUDE_CODE_DOCTOR_CODES` (`shared-config-dir` is the error). Isolation is pinned by `packages/runtimes/__tests__/claude-code/isolation.test.ts` (three profiles: every child env equal to the others except its own `CLAUDE_CONFIG_DIR`, a session in one profile changing nothing about another, a shared config dir refused and its verdict failing exactly the two) and AC-02 by the routing test "multi-account environments on one machine" (three environments selectable through Routing, every `session.open` naming the chosen one, accounts and verdicts untouched by execution); `docs/multi-account.md` has the mechanism, the verified / unverified matrix and the manual checklist for the real CLI (#43).
- Shared harness pieces (#318, `packages/runtimes/src/harness/`): `bridgedPlatformTools`, `openDaemonConnectors` / `withConnectorPolicy`, `profileEnv` (strip the parent's account variables, set the profile's home), `withPlatformMemoryLabel` / `withUnavailableConnectors`, `harnessCapabilityReport(subject, caps)` and the session guards (`assertCwdInRoots`, `assertRuntime`, `closingWith`). Claude Code uses them; the Copilot CLI (`copilot-cli`, `COPILOT_HOME`) and Codex (`codex-cli`, `CODEX_HOME`) drivers reuse them (#317).
- Copilot CLI (#319, `@agentic/runtimes/copilot-cli`): `copilotCliDriver` runs one `@github/copilot-sdk` client per environment, with `COPILOT_HOME = profileDir` and the parent's GitHub token variables stripped. Its `copilotCli` agent maps Copilot session events onto the event wire. Copilot's permission requests go to the daemon-compiled policy. Platform tools and connectors are native client tools, ruled on in their handler. The system prompt is appended, with repository instructions off (MEM-10). Resume is `local`, by Copilot session id. `copilotCliQuota` probes `account.getQuota`, which gives monthly premium, chat and completion requests. The doctor codes are `COPILOT_CLI_DOCTOR_CODES`, and `shared-login` flags a profile running on the machine's `gh` keyring login.
- Codex (#320, `@agentic/runtimes/codex-cli`): `codexCliDriver` drives one `codex app-server` per environment over stdio JSON-RPC (`CODEX_HOME` = `profileDir`, the daemon's `OPENAI_*` removed) and one Codex thread per session. The system prompt goes in as developer instructions, with memory labelled as the platform's (MEM-10). Session tools are served on a loopback MCP server named in the thread's config as `mcp_servers.agentic`, and each call is gated by the session policy. Approval requests map to `accept` / `acceptForSession` / `decline`, and cancel is `turn/interrupt`, steer `turn/steer`. `inspect` and the quota probe (`codexCliQuota`: `account/read` plus `account/rateLimits/read`, primary → Current session, secondary → Current week) run on a short-lived app-server and never read token files. Streamed `account/rateLimits/updated` is re-emitted as `ext codex-cli/rate-limits`. Doctor codes are in `CODEX_CLI_DOCTOR_CODES` (`shared-home` is the error).
- Platform tools reach Claude Code through the adapter's local MCP tool server; their `execute` sends `tool.call` over the same socket and awaits `tool.result`.
- **Daemon** (`apps/daemon`, #19): `agentic-daemon pair <code> --url` posts to `POST /auth/pair` and stores `{url, workspaceId, machineId, token}` in `%APPDATA%/agentic/credentials.json`, owner-only (`icacls /inheritance:r /grant:r user:F` before the token is written; `0600` elsewhere). `run` dials `/_agentic/daemon/{machineId}` with `Authorization: Bearer <token>` (never in the URL), redialling with exponential backoff and jitter; every log line is redacted of the token. The session log is keyed by the **platform** session id (`ndjsonEventLog(dir).forSession(sessionId)` as the `serveSession` `eventLog`) — a runtime names its sessions its own way. `session.opened.head` is the served head, `(0, 0)` before any event; `hello.resume` is the later of the served head and the last frame sent. `session.opened.ref` is whatever the runtime calls the session before its first prompt — the Claude Code adapter's `ref` is a live getter whose id is a `cc_…` placeholder until the CLI's `system/init` event names it — so the daemon remembers the ref the platform last heard per session and, after every frame its pump sends (and at each pump start, which covers a reconnect), reads `session.ref` again and sends `session.ref {sessionId, ref}` when the identity (`id`, `data.epoch`, never the object) changed (#389). It is never tied to `turn-end`: a turn that errors before it ends still named the session. Because `serveSession` replays whatever the store holds when its buffer has moved on, the daemon's pump checks each frame follows the last (next seq, or seq 1 of a later epoch) and sends a `gap {from: wanted, resumeAt}` before a hole. A wanted session the daemon no longer runs (restart) is replayed from its log, then `session.closed`. `session.open` is refused with a reason for an unknown environment, a runtime without a driver, a `cwd` outside `cwdRoots`, or a full environment (`concurrency`). Background running on Windows is a per-user Scheduled Task (`apps/daemon/scripts/install-service.ps1`), not a LocalSystem service, because the token and every `CLAUDE_CONFIG_DIR` belong to the user. The daemon ships as a self-contained zip (#52, `pnpm --filter @agentic/daemon package` → `scripts/package.mjs`): built `bin/` + `dist/`, the production dependency closure laid out as a plain `node_modules/` (workspace packages as `dist/`, the Claude Code SDK with the native CLI of the building platform), `install.ps1` (Node ≥ 22.12 check → `pair` → `doctor` → the scheduled task) / `uninstall.ps1` / README at the root; the machine needs only Node. `agentic-daemon --version` prints `DAEMON_VERSION`; `__tests__/package.test.ts` unpacks the zip and runs it on plain Node. Install, upgrade, uninstall: `docs/runbook.md` §5. **Quota** (#271): `QuotaMonitor` (`apps/daemon/src/quota.ts`) runs the build's `QuotaSource`s (`builtinQuotaSources()`, one per runtime). It taps each live session event once per cursor (a reconnect replay is not news): `ext` → `fromSignal`, and `error` as `{ns: 'error', name: code}`. Unless `--quota-probe off`, it also probes after `welcome`, polls idle environments every `quota.pollMs` (5 min), and probes 30 s after a `turn-end`, one probe at a time. It merges with `mergeQuota` and sends `quota {environmentId, snapshot}`. An unchanged snapshot is dropped unless the last send is older than 15 min, since the platform judges staleness from `observedAt`.
- **Machine actor** (`packages/platform/src/machine`, #36): `defineMachineActor({ socket, sessions, tools?, heartbeatWindowMs?, commandTimeoutMs? })`, keyed `{ws}:machine:{id}`, save persistence. `pair(code, info)` redeems the code through `Workspace.claimPairing` over a hop and stores only the token hash (`tokenRecord()` is what `authenticate`'s `machines` lookup and the socket handshake verify against; `revoke()` refuses the token from then on and closes the socket). The host owns the socket: on Cloudflare (`apps/web/src/daemon`) the Worker forwards `/_agentic/daemon/{machineId}` to the Machine's own Durable Object, which verifies `Authorization: Bearer amt.…` against `tokenRecord`, accepts the server end with the hibernation API under the `agentic:daemon` tag, and hands every text message to `socketMessage(raw)` under the machine principal; a close or error calls `socketClosed()` (offline at once). Outbound frames go through the `MachineSocketPort` (actor key → `state.getWebSockets(tag)`). `hello` records os / version / environments / capabilities, answers `welcome {wanted}` from the cursor the machine last forwarded per hosted session (`{0,0}` when none), re-sends `session.open` for sessions the daemon never acknowledged and every pending command (replies are idempotent by `commandId`); `env` replaces the environments; `heartbeat` / `pong` refresh `lastSeen`. The stored descriptors carry the daemon's per-environment `doctor` verdict, and `doctor(environmentId?)` → `MachineDoctorView { ok, unverified, environments: [{ environmentId, name, runtime, account, isolation, verdict? }] }` returns them as stored (never recomputed — the platform cannot see a config dir): `ok` only when every environment has a verdict and none is an error; this is the `environments.doctor` shape for the MCP surface (#50) and the Machines page (#90). `openSession(sessionId, environmentId, spec, {taskId?})` → `opened` when the environment has a free slot (`concurrency.max` minus hosted sessions) or `queued` until one closes — idempotent by id: a session the machine already hosts answers `opened` with no new `session.open` and no `session.opened` to follow, so the router prompts a reused one itself (#393); `closeSession` sends `session.close` (or drops a queued one); `session.opened` binds the session and forwards a synthesized wire `hello` (capabilities, since the daemon's pump skips it; the ref it carries is the open's placeholder and the Session ignores it) to the Session; `session.ref` updates the hosted session's `ref` and forwards it as `Session.noteRef` (#389); `session.frame` → `Session.forwardFrames`, `session.reply` → `Session.commandReplied`, `session.closed` frees the slot, answers that session's pending commands `closed` and dequeues the next; `tool.call` runs on the `ToolCallPort` under `mintAgentPrincipal({ws, agentId, sessionId, taskId?})`, detached from the socket turn, and answers `tool.result`. `sendCommand(sessionId, command)` (the app binds the Session's `CommandSink` to it) records `pending {sessionId, command, sentAt, deadline}` and sends while online; the `liveness` reminder (60 s floor) marks a machine offline after `heartbeatWindowMs` (90 s) of silence and answers commands past their deadline (120 s) with `error internal`. Session hops are fresh `actor()` calls under the machine principal (never `ctx.actor`): the daemon-path entry points admit only the hosting machine, and `sendCommand` may run inside the Session's own turn — the not-hosted `closed` reply is `oneWay` so it never waits on the turn waiting on it. With #37 the machine also tells the Routing actor — one-way, as itself, when `MachinePorts.routing` is set — `machineOnline` on every `hello`, `sessionOpened` when the daemon acknowledges a hosted session (a dequeued one included) and `sessionClosed` when one is gone, so a task parked on the machine resumes or fails with the daemon's reason (a session refused before it opened fails its task `session-refused`) — the router applies either to every route still waiting on that session, since a chat's session serves many tasks and the task the machine names is only the one it was opened for (#393); the anonymous `POST /auth/pair` resolves the code through the global `PairingDirectory` (filed by `Workspace.registerMachinePending`, single use) and then calls `Machine.pair(code, info)` as that machine. Open seam: the Session actor still has no entry point for a host-side closure of a session that already opened.
- Envelope contract (`@agentic/daemon-protocol`): one frame per WebSocket text message; `v` is checked before `t` before the schema, so a peer on another version is refused with `unsupported-version` and a malformed message with one of `too-large` (1 MiB, checked before parsing) · `not-json` · `not-object` · `unknown-type` · `invalid`. Every frame kind has a Standard Schema (zod) with bounded strings and lists (`LIMITS`); unknown keys are stripped. A daemon drops what it cannot decode and keeps the socket. `session.frame` traffic waits for `welcome`, so a replay driven by `wanted` always precedes live frames; `wanted` is the last cursor the platform holds and replay starts right after it; a cursor older than the log yields a `gap` frame, never silence. `daemonConformance(harness)` on `@agentic/daemon-protocol/testing` pins all of this from the platform seat (hello/welcome, malformed input, env, heartbeat, session, reconnect replay, gap, tool round trip); `inMemoryHarness()` is the reference daemon it is proven against and a stand-in for tests of the Machine actor.

## 6. Chat model

Entries: `msg {author: user | agent{agentId, sessionId?}, parts, mentions, replyTo?, sessionId?, taskId?}`, `member {op: add | remove, agentId, historyAccess}`, `status {agentId, kind: typing | session-started | session-ended | task, ref}`, `coordinator {agentId | null}`.

- Activation (CHT-06): activated = mentions ∩ members; none → coordinator if set; none and exactly one agent member → that agent; otherwise store only. `mentions: 'all'` addresses the group (every agent member). An agent's own post never activates itself. Agent-originated posts may mention other agents (COL-06) with the depth counter from the originating Task. As implemented (#222): a person's post is activated by the browser (`runActivation`); an agent's `chat_post` by the tool port (`createActorToolPorts().chat.post`), which starts a task only for the members the post explicitly mentions (never the coordinator fallback), gated by the poster's `collaborators` (COL-10), at the posting task's depth + 1 (`checkDepth`, so mention chains stop at `maxDepth`), contract from `mentionContract` (origin the message, the last 50 messages the member may read, the member's chat folder, else the posting route's environment for an assignee with no default); the tool result lists `activated` and `notActivated` with reasons.
- Each activation creates a Task (origin = the user's message, context = the entries visible to that agent since `historyFrom`, pre-rendered as the "Chat so far" block) and runs it in the member's **one live Session per (chat, agent)** (#393; CHT-01, CHT-11): the router takes the session from the chat's binding below (`sessions[agentId]`) and re-opens it for the task, or opens a fresh one — on the member's first activation, and whenever the bound one cannot go on (closed, its placement moved, or its runtime never named it; the rule is in §7). On a reused session the prompt carries the objective and the entries after the row's `seenSeq` (each message attributed by name, their chat-file parts once, at most the activation's 50), never the pre-rendered block: the engine keeps its own conversation. The session is not closed when the task settles, whatever the turn's outcome; it ends when the agent leaves the chat, the user resets it (#399), the placement moves, or the workspace is deleted. A coordinator and three members hold four live sessions that reach each other through `chat_post` / `delegate` exactly as before.
- Streaming deltas never pass through Chat: the UI subscribes to `Session.tail` for `chat.sessions`. Chat receives only final assistant messages and status through a topic subscription — `topic(SESSION_EVENTS_TOPIC, chatKey)` carrying a `SessionEvent` (`status` → a `status` entry that also maintains `sessions`; `message` → a `msg` entry attributed to the agent). `typing` is ephemeral and never made durable (CHT-11: a chat coordinates N sessions, never one provider conversation).
- **The session binding (#392, CHT-01/CHT-11):** the chat durably knows which execution session belongs to each agent member, so the router finds a member's session instead of minting one. `ChatState.sessions` (and `ChatSummary.sessions`) is `Record<AgentId, ChatSessionRow>` with `{ sessionId, since, seenSeq }` (`packages/platform/src/chat/state.ts`), fed by the reducer alone — no extra hop, so it is replay-safe and eviction-safe: a `session-started` status entry carrying a `ref` creates the row (`sessionId` = the ref, `since` = the entry's `at`, `seenSeq` = 0) or replaces the member's existing one (new id, new `since`, `seenSeq` back to 0); `session-ended` deletes it; a `member remove` deletes it with the member. These two statuses bracket the chat's lifetime of that session, not a single turn's. **The watermark:** when a `msg` entry whose `author` is an agent that owns a row, authored under the row's own `sessionId`, is folded, the entry's reducer-assigned `seq` becomes that row's `seenSeq` — that is the member's own final answer, so every entry after it is what its engine has not seen; another member's or a user's message never moves it, a message from an agent with no row moves nothing, and a late final answer from a replaced session leaves the new row alone. `seenSeq: 0` means "nothing seen in this session" — the router (#393) treats it as "send from the member's `historyFrom`".
- History access (CHT-04): `members[agentId].historyFrom` is the index of the join entry (`'all'` → 0); `history`/`search` filter `seq >= historyFrom` when the caller is an agent principal, and an agent that is not a member reads nothing; context assembly uses the same rule; removal closes the agent's session for that chat. Membership and the coordinator are changed by user or external principals only; machines never post.
- Attachments (#203):
  - **Storage:** a file is uploaded once into the chat and stored in R2 under `files/<ws>/<chat>/<fileId>` (`ChatFileStore`).
  - **References:** `msg` parts reference it as `agentic-file:<chatId>/<fileId>` in the `url` of an `image` or `file` part, and never carry the bytes, so actor state and daemon frames stay small.
  - **Access:** reading a file is allowed exactly when the caller can see the message it was posted in, by the same `historyFrom` rule as `history`. A pending, not-yet-posted upload is visible only to its uploader.
  - **Hydration:** when a turn starts, the router inlines the images the target agent may see as base64 `image` parts, within `CHAT_FILE_INLINE_BUDGET`. Every other file reaches the model as a text placeholder naming its URI, which the agent reads with `chat_file_read`.
  - **As implemented (#205, `packages/platform/src/chat`):** `Chat.registerUpload(file: ChatFile): Promise<ChatFile>` (users and external clients only; the web upload route calls it after its R2 write) records a pending upload keyed to the uploader (`principalKey`), at most `MAX_PENDING_UPLOADS` = 50 per uploader, forgotten after `PENDING_TTL_MS` = 24 h (the store's `sweepOrphans` must use at least that age); idempotent for the same uploader and id, 409 for another's or a posted id, 400 for another chat / a bad id / a missing name or type, 413 over `CHAT_FILE_MAX_BYTES`, 429 at the cap, 501 on a Chat without a file store. `Chat.post` checks every part whose url claims `agentic-file:` before storing anything: well-formed (400), THIS chat (400), the caller's own live pending upload or a file already posted in a message the caller can see — a re-share (404 unknown or another's pending, 403 not visible), and the file's own `mediaType` (400); it then calls `ChatFileStore.markPosted` for each pending upload it posts (a failure fails the post, nothing stored) and appends the entry. The reducer `applyChatEntry` indexes the chat-file parts of a `msg` into `ChatState.files[fileId] = { entryId, seq, name, mediaType, bytes, at }` — a pending upload moves there, a re-share moves `entryId`/`seq` to the newer message (visibility is "from a seq on", so the newest posting is the widest) — so the index replays and, living in the actor's state rather than the window, outlives archiving to `ChatPage`. `Chat.fileAccess(fileId): Promise<ChatFile | null>` (reentrant, closed to machines): a pending file for its uploader only; a posted file when `visibleFrom(state, principal) <= files[id].seq` — users and external clients always, a member agent from `historyFrom` on, anyone else `null`. An agent's final message arriving as a session event keeps a chat-file part only when the agent can see that file; any other becomes `[file unavailable]`, so no path but `post` makes a file readable. The store reaches the Chat actor as `defineChatActor({ files })` (`Chat` is the store-less default; both have type `'Chat'`, so every `actor(Chat, …)` client reaches whichever the app registers).
- Memory privacy (MEM-11): retrieval runs under the assignee's principal against its own scope + declared shared scopes; `Memory.authorize` enforces it.

## 7. Task and delegation

`queued → active → (waiting ⇄ active) → completed | failed | cancelled`; `waiting` always carries a `WaitReason`.

Driver (`Task.tasks.run`): resolve environment (fixed at creation; offline → `waiting {environment-offline}` and the agent's policy `queue | fail | fallback-api`, the last only when explicitly allowed — never silent) → `Agent.snapshotForSession` → `Session.open` → retrieve memories → prompt → mirror session state → result (`verified: false` unless a verification step set it, LRN-03) → learning hook.

**A session may serve many tasks** (#390). A task owns a turn, not a session: the router prompts a session with the task the turn works (`Session.prompt(…, { taskId })`), and from then on the turn — its `delegate` parent, its `task_report`, its Ledger rows and budget check, its audit rows, its memories' provenance, its chat message — is attributed to that task (§5a "the turn owns the task", §5b for the daemon's `tool.call`). The task a session opened with (`spec.taskId`) is the fallback for a turn prompted without one. A chat's tasks share the member's live session (#393, below): each message is a new task and a new turn in the one session, so `spec.taskId` is only ever the task of the latest (re-)open, and everything task-attributed reads the turn's.

**DelegateTool** (`packages/runtimes/src/tools/delegate.ts`, a `defineTool({name: 'delegate', annotations: {openWorld: true}})`): `execute` calls `Task.delegate(spec)`, which checks collaborators, depth, concurrency and budget split, creates the child with the deterministic id `(parentTaskId, callId)`, moves the parent to `waiting {child}`, then awaits the child's `Task.result` stream (idempotent across restarts). In-process `agentTool` is used only for `modelAgent` named sub-agents within one session, never across machines.

Stop cascade (COL-12): `Task.cancel` → `Session.cancel` → each child (one-way) → wait for acks with a deadline → unacked children recorded `stopped: false` and listed as "could not be stopped".

As implemented (`packages/platform/src/task/`): `cancel(by, {timeoutMs})` transitions to `cancelled`, then calls `stop(by, deadline)` one-way on every unsettled child; a child acknowledges its parent through `childStopped(id, report)` (a reentrant method, so it lands while the parent's turn is parked on the deadline) after settling its own subtree, and each hop keeps a 20 % margin of the remaining time so the child's report precedes the parent's deadline. A task with a session attached also waits for `sessionStopped()` from the session driver (the `Session.cancel` hop above); until that arrives it lists itself in `notStopped`. The driver must hear of the cancel while that turn is still parked, but a change feed yields only after a turn ends, so the router follows `result({ eager: true })`: it also yields as soon as the `cancel` entry is committed (an in-process wake per activation), and after that the router reads nothing more from the Task — a FIFO `get()` would queue behind the parked turn — before it sends `sessionStopped` (#168). Children report terminal states to the parent with `childSettled(id, status)`, which frees the child's budget reservation and resumes a parent parked on `waiting {child}` once its last waited-on child settles. Limits default to `maxDepth 5` and `maxConcurrentChildren 8` when the contract sets none; the budget split clamps each of `maxCostUsd`/`maxTokens`/`maxWallMs`/`maxTurns`/`maxSteps` to the parent's remaining share after its own spend and live reservations, never widening. Budget exhaustion (#45, COL-11/OPS-08): `recordUsage` and `delegate` run `checkBudget(constraints, spent)` from `packages/platform/src/ledger`; once a budget is spent (`spent >= max`, estimates counted in full) the task ends `failed` with `error.code 'budget'`, cancels every unsettled child one-way (a queued child is cancelled before it ever starts), and `delegate` throws `TaskLimitError('budget')` before creating anything — distinct from the split's "nothing left to delegate", which refuses the child but keeps the parent alive. Every mutation is one `TaskEntry` through the pure reducer `applyTaskEntry` — `ctx.append` where the runtime ships it, the reducer plus `ctx.save()` otherwise — so the record is snapshot + log either way.

Approvals across delegation (AC-12): children run under `firstMatch(childPolicy, parentConstraints)` (never wider); a child `request` bubbles `waiting {child}` to the parent; the user's decision goes to the child session.

**Delegation, as implemented** (#39, `packages/platform/src/routing/tools.ts`, `packages/platform/src/policy`, `packages/runtimes/src/tools/delegate.ts`): `createActorToolPorts().task.delegate` serves the `delegate` tool on BOTH paths (in-process inside `modelAgent`, and through the Machine's `tool.call`): it checks the parent agent's `collaborators` (COL-10, `forbidden`), calls `Task.delegate` (depth, concurrency, budget split, child id `childTaskId(parentTaskId, callId)`, parent → `waiting {child}`; a `TaskLimitError` answers `limit`), hands the child to `Routing.run` — a 409 (already routed, or already settled because a restarted parent re-issued the call) is not an error — and awaits the child's `Task.result` stream, so the same call id always finds and re-awaits the same child. The tool result is `{ taskId, status, text?, output?, artifacts, verified, error?, notStopped? }`; on the local path the tool also emits an `agent-start` / terminal `agent-update` pair bound to its call (`AgentToolContext.emit`) so the UI renders a sub-agent card linking to the child task — the child's own events are never nested into the parent transcript (it may run on another machine). The tool takes an optional `environmentId` and `workdir` (#190; a `workdir` needs its `environmentId`, refused as invalid input otherwise) and an optional `projectId` (#332; the parent task's when absent, as with the environment) that `Task.delegate` puts in the child's contract (the child's environment defaults to the parent task's; when neither names one, the router falls back to the assignee's default, then the parent route's environment, #220); without a `workdir` the router's inherit rule applies — the parent's folder when the child lands in the parent's environment. A child runs on ITS agent's runtime and environment with no chat (`origin.kind === 'agent'`): background collaboration whose record is the Task tree and its transitions (COL-08/09). Approvals (AC-12): the child's `Route` carries `constraints` — the parent route's chain plus the parent's own `approvalPolicy` — into `SessionOpenSpec.approvalConstraints`, and `createSessionFactory` opens every local session under `sessionPolicy(spec)` = `constrainPolicy(agentPolicy(config), compilePolicy(constraints))`, where `agentPolicy` is `firstMatch(compilePolicy(approvalPolicy), grantPolicy(tools), allowAll)` and `constrainPolicy` lets the stricter side win (deny > ask > allow) so a child is never wider than its ancestors (the daemon path's compiled policy is #121). A child `request` parks the child `waiting {approval | input, requestId, sessionId}` (the parent already `waiting {child}`) and, like every request, reaches the Inbox and the chat through the child's own Session (#40, §5a); the user answers with `Session.respond` on the CHILD session. Stop (COL-12): the router's `follow` is the Task's session driver — a task settled from outside while its turn runs (cancelled by a user or a parent's cascade, failed on budget) has its session cancelled, and `Task.sessionStopped` is sent when the turn actually ends; a late confirmation (`sessionStopped` or a child's ack after the cascade settled) is re-acknowledged upward so every ancestor's `notStopped` is corrected. An aborted parent turn answers `cancelled` with `notStopped` naming the child when its stop is not yet confirmed. `follow` itself is one supervisor task per router (`ctx.tasks` is single-flight per name) that follows every `running` route at once and is woken by `prompt`.

**The `projects` tool** (#334, `packages/runtimes/src/tools/projects.ts`, `PlatformPorts.projects`; COL-02/04, PLG-01), beside `delegate` and `ask_user` and granted like them (`ToolGrant`, both paths — in-process and through the Machine's `tool.call`, the daemon bridge serving the same definition): `{ action: 'list' }` returns the registered projects as `{ id, name, description?, environments }` (the environments with a folder); `{ action: 'set', chatId, projectId | null, force? }` is `Chat.setProject` under the agent's principal (a member of the chat, else `forbidden`; an unknown project is `invalid`), which the Chat records as `chat.project-set` under the agent, so the tool audits nothing itself. The platform does no matching: the tool's description tells the coordinator to `list` only when the chat has no project and the user's message names a repo or project, to set the single registered project whose name or description matches, to ask with `ask_user` offering the candidates when none or several match (never guess), and never to pick a folder on disk — only a registered project. The one rule the tool enforces: a chat already in a project is left unchanged (the error names the current project) unless `force` is passed, which the description reserves for an explicit request from the user to switch the chat's project; setting the project the chat is already in is a no-op. `createActorToolPorts().projects` reads `Workspace.projects` as the workspace's user (the root actor admits its owner only, as `usage_limits` reads the machine index) and the Chat under the agent.

**Routing, as implemented** (#37, `packages/platform/src/routing`): the driver is the Routing actor, `{ws}:routing:main`, `defineRoutingActor({ sessions, machines })` (thunks — Session, Machine and Routing reference each other). `run(taskId)` on a `queued` task takes `Agent.snapshotForSession`, resolves the runtime and the environment ONCE (the task's `environmentId`, else the agent's `defaultEnvironmentId`, else — for a delegated task — the delegating task's route environment, #220: a chat-started parent's environment lives on its route, never on its Task record) and records both on a `Route`; nothing later changes them (EXE-12) — a retry always goes to the machine that reported the environment at resolution, never to another machine reporting the same id. **The working folder** (#190, #332, EXE-06/12) is resolved once too, in this order: the task's `workdir`; the project's folder for the environment (`projectFolderFor(project, environmentId)`) when the task carries a `projectId`; for a delegated task without one that lands in its parent route's environment, the parent route's `cwd`; the agent's `execution.defaultWorkdir` when the environment is the agent's `defaultEnvironmentId` (never elsewhere); else the environment's first `cwdRoots` entry (the one piece that needs the machine's report, so a route no machine reported at `run` takes it at placement). The `cwd` is stored on the route, in the `environment.chosen` audit data (`cwd`, and the source in the summary) and on the Session record (`SessionOpenSpec.cwd`), and sent as `OpenSpec.cwd`. Before any Session is opened — and on every placement, against the roots the machine reports then — it is checked with core `pathWithin(cwd, env.cwdRoots, machine.os)` (a machine that never reported its OS is taken for Windows): outside the roots, a sibling prefix or a relative path fails the task `{ code: 'workdir-outside-roots', recoverable: false }` and `Machine.openSession` is never called. A task's own `workdir` is noted on the Workspace (`noteWorkdir`, one-way). `anthropic-api` runs in no local folder: a `workdir` is ignored and the `environment.chosen` entry says so (`ignoredWorkdir`), as does the `fallback-api` entry for the folder it leaves behind. **Projects in the router** (#332, `routing/features.ts`): a task's `projectId` is read from `Workspace.projects()` at `run` — a project the Workspace no longer has fails the task `{ code: 'project-missing', recoverable: false }` before any route is written, never a guessed folder (a Workspace that cannot be read is `workspace-unavailable`, recoverable) — and again at every placement, including before a `fallback-api` is gated or recorded. The gate is asked for the agent's connectors AND the project's (`connectorIds(config, project)`, one per id; the `fallback-api` gate too), so the project's ready connectors ride `spec.plugins.connectors` into the `OpenSpec` beside the agent's. After the folder is final and the machine is there — before `Session.open` on both paths — the enabled feature plugins the build ships (`RoutingPorts.projectFeatures`, by plugin id; an enabled feature with no plugin here is skipped) run in the project's order: `beforeSession({ project, settings, taskId, chatId?, environmentId, cwd, fs })` with `settings` = the manifest's `projectSettings` defaults under `features[id]`, and `fs` = `machineFs(machine, environmentId)` — `Machine.fsRequest` then `fsResult` polled every 250 ms until the daemon answered (or `timeout` after 35 s; a refused request comes back as the daemon-shaped error: `unknown-environment`, `unsupported`) — on the daemon path, and `noDaemonFs` (always `unsupported`) on the local path, where `beforeSession` is only called when the route has an environment and a folder (a `fallback-api` route; a platform-hosted runtime runs in no folder). A returned `cwd` replaces the route's after the same `pathWithin` check (`workdir-outside-roots` otherwise); `instructions` plus every plugin's `instructions({ project, settings })` join with blank lines into `SessionOpenSpec.projectInstructions`, rendered as the prompt's `## Project` section — inside `system` on the daemon path (`buildSystemPrompt({ project })`), and by the API factory on the local one (`createPlatformModelAgent({ project })`). A hook that throws parks the task `waiting { kind: 'project-feature', pluginId, message }` (one line, ≤ 500 chars) with the route kept and no session opened; a later `Routing.run` on that task runs the hooks again on the same route (EXE-12: never a silent fallback). The chat section names the project (`ChatRoster.project`, from `Chat.get().project`). `anthropic-api` opens a local Session through the concrete `SessionFactory` (`createSessionFactory({ routing, anthropic })` → `createPlatformModelAgent` over the actor tool ports, BYO key per workspace, `no-api-key` when none) and prompts `objective + context` — on a reused session, the objective + the chat's unseen entries (#393, below) — under the turn id `{taskId}:turn:1` (idempotent), with the route's task (`prompt(…, { taskId })`, #390). A daemon runtime opens the Session record first (mode remote), then `Machine.openSession(sessionId, envId, OpenSpec, { taskId })`: `opened` → Task `active`, prompted when the machine reports `sessionOpened`; `queued` → Task `waiting {capacity, position}` (the position in the machine's queue), prompted on `sessionOpened` after a dequeue with a `waiting → active` transition that says so; machine offline → Task `waiting {environment-offline, policy}` then the agent's `offlinePolicy`: `queue` parks the route until `machineOnline` from THAT machine, `fail` ends the task `failed {environment-offline}` naming the environment, machine and policy, `fallback-api` — only when the config sets it — opens a NEW local session through a `waiting → active` transition whose `why` starts with `fallback-api:` and names the environment (the task's own `environmentId` stays what it was; the record says where it actually ran). An environment no machine of the workspace reports yet is offline under the same policy (`queue` waits for the first machine that reports it and binds the route to it then; `fail` ends the task `failed {environment-offline}` saying no machine reports it); an agent without an environment, or one whose runtime differs from the environment's, fails the task with a code (`no-environment`, `runtime-mismatch`). The `follow` actor task tails the Session and settles the Task at the turn end: `completed` with the final assistant text (or the `task_report` summary) and the `task_report` output when one was given, `failed` on an error, `waiting {input}` "Resume?" on an interrupted turn (OPS-05/06), `waiting {approval | input}` / back to `active` on `request` / `request-resolved`; then a chatless session is closed so the environment slot frees — a chat member's session is not (#393, below). Routing drives the other actors as the workspace user (v1: `workspaceId === userId`; `RoutingPorts.driver` overrides). `run` also adopts a task the schedule trigger (#42) left `waiting {environment-offline}` — the app hands every task a firing creates to `Routing.run` from `scheduleTrigger({ onOutcome })`, and the trigger's `EnvironmentProbe` is `createEnvironmentProbe({ machines })` over the same machine scan. Platform tools (`memory_search`, `memory_remember`, `chat_post`, `task_report`, `delegate`) run over `createActorToolPorts` under the agent principal on both paths — in-process inside `modelAgent`, and through the Machine's `ToolCallPort` (`createToolCallPort`, the same tool definitions, `SchemaValidationError` → `invalid`); `ask_user` raises a platform input request on the Session (#122, §5a) — `createActorToolPorts({ sessions })` / `createSessionFactory({ sessions })` name the Session definition; without it the tool answers `unsupported`. The local session policy is compiled from the config (`sessionPolicy`, #39); memory retrieval and the learning hook are #41's. **Chat roster** (#194, CHT-07): for a chat-originated task the router reads `Chat.get()` and each member's `snapshotForSession` into `SessionOpenSpec.roster` (`ChatRoster`: members by id, name and role, the coordinator, `self`) on both paths; `buildSystemPrompt` renders it as `## This chat` — every member by platform id, who coordinates, and that they are reached through `delegate` (the id as `assignee`) or `chat_post` @Name, never a runtime's own agent messaging. `delegate` refuses an assignee that is no configured agent with `invalid` before a child exists.

**One live session per chat member, in the router** (#393; CHT-01, CHT-07, CHT-09, CHT-11, COL-04, EXE-12, EXE-13, MEM-07). A route with a `chatId` — set only for `origin.kind === 'user'`: chat members, the coordinator, and members an agent `@mention`s — is a chat route; everything else (a `delegate` child, a schedule, a trigger, an external client) is chatless and unchanged: one throwaway session per task, closed at the turn's end. **Binding** (`bindSession`, once the placement is final, the folder included): a chat route takes `ChatSummary.sessions[agentId].sessionId` from the chat and, with it, the row's `seenSeq` onto the route; a chatless route, or a chat route with no binding, mints a fresh id. **Refusal** (`reusable`; never a migration): the bound record is not reused — it is ended with `Session.close` (its `session-ended` drops the chat's row; the fresh session's `session-started` replaces it either way) and a fresh id minted — when it is closed or was never opened, when the placement moved (`runtime`, `environmentId`, `machineId` or `cwd` differ from the record's spec), or when a daemon-path record has no `ref` (its runtime never named it, so nothing could be resumed; #389). A record that cannot be read is not reused. The git feature's `beforeSession` still runs per placement — it decides the folder the reuse is judged on — and is idempotent (`branch-exists` → the same worktree), so the worktree is made once per chat. **Re-open**: `Session.open` with the new spec (§5a) and, on the daemon path, `Machine.openSession` with `resume` = the record's `ref`; a session the machine still hosts answers `opened` without a frame, so the router prompts it at once, a session the daemon lost (a restart: it says `session.closed` for the one the platform wanted) is opened again on the daemon with that ref. **Prompt**: the objective, the triggering message's attachments, then — instead of the task's context — the chat's messages after `seenSeq` that the member may read (`Chat.history` as the agent, so `seenSeq: 0` starts at `historyFrom`), the triggering message left out, oldest first, each `Name: text` with attachments noted and their chat-file parts following once for `hydrateChatFiles`; nothing when nothing is new. **Follow**: the route records the head `open()` returned and `follow` tails from it, ending itself at its turn's `turn-end` — the log outlives the turn and nothing before it is replayed. **Busy**: a second route on a session whose first is still `running` fails `prompt-busy` before anything is sent, on both paths alike (a daemon's `busy` comes back after the pending ack and would strand the route); #395 steers it into the running turn instead. `sessionOpened` and `sessionClosed` from the Machine apply to every route bound to the session that still waits for it to open. A chat route's session is not closed when the task settles, a failed turn included; `Route.sessionId`, `seenSeq` and `head` are the state (`routing/state.ts`).

**Chat attachments on the router and the tools** (#205, `packages/platform/src/routing/files.ts`, CHT-04 / MEM-11): before `prompt`, the router builds the turn's input as `[objective, …the triggering message's chat-file parts the context does not already carry, …context]` — the triggering message is read with `Chat.history` as the task's agent (the last 50 entries; best effort) — and `hydrateChatFiles` resolves every chat-file part through `Chat.fileAccess` asked as the TARGET agent (`mintAgentPrincipal` with the route's session and task): an image of a `MODEL_IMAGE_TYPES` type is read from the store and inlined as `{ type: 'image', mediaType, data }` (base64) while `CHAT_FILE_INLINE_BUDGET` base64 characters last, the triggering message's images first and then the newest (last) ones; an image over budget, or any image when no store is wired, becomes `[image "<name>" (<type>, <KB> KB) <uri> not inlined]`; every other file `[file "<name>" (<type>, <KB> KB) <uri> — read it with chat_file_read]`; a file missing, denied or without bytes `[file unavailable]`. The parts keep their order. `createActorToolPorts({ files })` adds the `files` port only when a store is given: `files.read(uri)` asks `Chat.fileAccess` as the agent (`invalid` for a malformed URI, `forbidden` when missing or not visible), then for a text-like type (`isTextLikeMediaType`) reads the store and returns the UTF-8 text cut on a character boundary at `CHAT_FILE_TEXT_MAX_BYTES` with `truncated: true` (`not-found` when the bytes are gone), for any other type the record alone without touching the store. `chat.post` turns `attachments` into parts the same way — each URI through `fileAccess` (`forbidden` if not visible), then an `image` part for a model image type or a `file` part with the name, typed by the chat's record — and `Chat.post` checks them again, so an agent re-shares only what it can see. `Workspace.deleteAll` deletes every chat's files (`ChatFileStore.deleteChat`) before purging the records. **Implicit grant:** `run` adds `{ name: 'chat_file_read' }` to the route's config for every chat-originated task (`withChatFileRead`) — the tool is read-only and every read is decided by `Chat.fileAccess`, like reading the chat itself — so it reaches the model's roster, the system prompt, `SessionOpenSpec.tools` / `OpenSpec.tools` and the compiled policy on both paths without the agent's config naming it; a config that names the tool (`ask`, or `deny` outright) keeps its own grant, and a task from outside any chat gets nothing implicit.

**Wiring the file store** (for the web app, #207): the store is one `ChatFileStore` (from `@agentic/core`, the R2 implementation) passed as the optional `files` option everywhere it is used — `defineChatActor({ files })` registered in place of `Chat` (`markPosted`), `defineRoutingActor({ …, files })` (`RoutingPorts.files`, hydration), `createSessionFactory({ …, files })` (the local path's `files` tool port), `createToolCallPort({ …, files })` (the daemon path's), and `defineWorkspace({ …, files })` (`WorkspaceOptions.files`, the purge); the MCP mount gets it too — `createAuthMount({ …, files })` → `createOAuthRoutes(…, { files })` → `createPlatformMcpHandler({ files })` (`chats_file_get`, #209). Absent anywhere, that piece degrades: the Chat refuses uploads (`registerUpload` and a `post` of a pending upload answer 501), notes instead of images, no `chat_file_read`, no purge of bytes.

## 8. Memory and learning

Interfaces in `@agentic/core`:

```ts
interface MemoryPlugin { id: string; version: string; capabilities: { semantic: boolean; export: 'full' | 'partial' }; open(scope: MemoryScope, ctx: PluginContext): MemoryStore }
interface MemoryStore { put(e: NewMemoryEntry): Promise<MemoryEntry>; update(id, patch); retire(id, why); delete(id): Promise<boolean>; get(id); query(q: MemoryQuery): Promise<RankedMemory[]>; export(): AsyncIterable<MemoryEntry>; import(rows: AsyncIterable<MemoryEntry>, opts): Promise<ImportReport> }
interface MemoryQuery { text?: string; tags?: string[]; kinds?: MemoryKind[]; subject?: string; limit: number; since?: number }
interface LearningPlugin { id; version; onTaskEnd(ev: TaskOutcome, mem: MemoryStore): Promise<Proposal[]>; onCorrection(ev: Correction, mem: MemoryStore): Promise<Proposal[]> }
type Proposal = { kind: 'memory'; entry: NewMemoryEntry } | { kind: 'instruction'; patch: string; requiresReview: true }
```

Default memory (`@agentic/memory`, #26): a pure core — `MemoryState` + `applyMemoryLog` reducer, BM25 over text/tags/subject/`conditions` × kind weight (preference 1.3 > lesson 1.2 > fact 1.1 > working 1.0 > record 0.9 > assumption 0.8) × confidence weight × recency decay (30-day half-life, floor 0.5) — and `createMemoryStore({ state, now, commit })` over it. `query` filters on `tags` (any-of; `conditions` tokens count as tags), `kinds`, `subject`, `since`, ranks on `text`, and never exceeds `limit` (default 20) or `maxBytes` (default 4096 bytes of `text`; a non-fitting entry is skipped). `working` entries expire `ttl` ms after `provenance.at` and are compacted on the next working put; a new `record` for a task retires the previous ones (`supersedes`). `retire` is soft: hidden from `query`, kept for `get` and export. Export is NDJSON with a header `{ format: 'agentic-memory', version: 1 }`; `import` reports `imported` / `skipped` / `droppedFields`. `memoryConformance(make)` (`@agentic/memory/testing`) runs against the in-memory store and the Memory actor over `memoryStorage()`; the Durable Object run is a follow-up (#33).

The Memory actor (`@agentic/platform/src/memory`) is that store over `ctx.state` with `ctx.save()` per mutating turn (`ctx.append` once `@sigx/actors` ships signalxjs/actors#312). Methods: put, update, retire, delete (MEM-05 / MEM-08: gone for good, `false` for an unknown id — `retire` keeps the history), get, query, exportPage, importBatch, compact, setAcl/getAcl, stats. Access: `memoryAuthorize` (same workspace; a user or a `memory`-scoped external client owns every scope; an agent only `agent:{self}`) and, inside the turn, the shared-scope ACL `{ read, write }` — no ACL, no agent access (MEM-04/11, AC-10). Embeddings later as a plugin via export/import (MEM-09) with the fidelity report.

Migration (`@agentic/memory`, #49, MEM-09): the export/import contract is the path between plugins, and `migrate(from, to, { dryRun, onConflict })` is the helper that walks it — every entry of `from` (retired included) imported into `to` kind by kind, returning a `MigrationReport` with `entries` / `imported` / `skipped` / `droppedFields` in total and per kind; a field is listed only when an entry of that kind carried it. A dry run asks the target's `fidelity(entry)` seam (`MemoryFidelity`, on every store of the package: the fields `import` would drop, `null` for a row it would skip) and `get` for id conflicts, so it reports the same numbers without writing; a target without the seam migrates for real only. The **flat plugin** (`flatMemoryPlugin()`, `agentic.memory.flat`, `capabilities.export: 'partial'`) is the deliberately narrower second implementation the path is proven against: it holds id, kind, text, tags, subject, provenance, confidence and `retired`, drops `conditions`, `evidence`, `supersedes` and `ttl` (`FLAT_UNSUPPORTED_FIELDS` — reported by `import`, by `fidelity`, and logged at `warn` by a `put` / `update` that carried one), ranks by the number of query terms found as substrings of `text` with recency as the tie-break (no kind or confidence weight, conditions are not tags), never expires a `working` entry and never compacts records. A round trip default → flat → default therefore reports exactly those four fields and nothing else (AC-11). `memoryConformance` marks the four cases those features need with `requires` (`conditions`, `kindWeights`, `ttl`, `supersedes`); a plugin runs the suite `without` the ones it lacks — the flat plugin all four, the default plugin and the Memory actor none. Retirement reasons are not part of `MemoryEntry`, so no export carries them: a limitation of the format, shared by every plugin.

Learning: explicit "Correct" action on any assistant message (v1); agent-detected correction classification is off by default. Memory writes are automatic; instruction/skill patches require review and land as new Agent config versions (AGT-06, LRN-08). Lessons retire through `supersedes` / `retire` (LRN-06). Copying a lesson into a shared scope keeps provenance (LRN-07). LRN-09: Ledger counts corrections per agent per week.

As wired (#41, `packages/platform/src/task/driver.ts` + the Session actor's `learning` port; the memory page is #89): the steps are pure functions the Session actor runs and a Task driver can reuse. **Session start** — `open` (before the factory or a daemon sees the spec) runs `retrieveMemories` **as the minted agent principal** over `agent:{id}` + `memoryPolicy.shared` (so `Memory.authorize` and the ACL decide, MEM-11; a refused scope is listed in `spec.retrieval.skipped`), ranked on `spec.objective` + the last text part of `spec.context`, filtered on `spec.tags`, merged under one budget (8 entries / 4096 bytes by default, MEM-07); the hits are recorded as `spec.memories`. **One rendering per path** (#135): on the local path the router opens the session without a `system` and `createSessionFactory` hands `spec.memories` to `createPlatformModelAgent({ memories })`, whose `buildSystemPrompt` composes the whole prompt (identity, role, instructions, skills, tools, then the memory block) — the record composes no second prompt, so `spec.system` stays absent and what the model saw is what `spec.memories` says; on the daemon path the caller builds `spec.system` (`placeRemote`: `buildSystemPrompt` over the config, the granted tools and the chat roster — the same prompt the API path composes, #194) and `open` appends `## Platform memory` + an ownership note to it (MEM-10) — the heading `claudeCodeSystemPrompt` relabels to, so a daemon carries it verbatim. Both `placeLocal` and `placeRemote` put the task's `objective` and `context` on the `SessionOpenSpec`, so retrieval ranks on the task (`spec.retrieval.text` is the objective plus the latest context text) and a correction's lesson gets `Tasks like: <objective>` as its conditions. Pinned by `packages/platform/__tests__/routing/memories.test.ts` (the real factory over a `mockModel`: every request's `system` carries the lesson) and AC-09. **Turn end** — a task session's regular `turn-end` becomes a `TaskOutcome` (`end_turn`/`max_*` → completed, `cancelled`, else failed; a result with text is `claimed`, `verified`/`refuted` only from the `verify` port, LRN-03) for `plugin.onTaskEnd`; memory proposals are applied by the plugin, instruction proposals parked; an interrupted turn is no outcome. **Correct** — `Session.correct(messageId, text, what)` builds the `Correction` for the assistant message (`by: 'user'`, or `'agent'` for an agent principal; never a machine) and runs `plugin.onCorrection` as the agent, so the lesson lands in `agent:{id}` with the user's provenance. The plugin port may be a factory over `{ workspaceId, agentId, sessionId, taskId?, objective?, tags? }`, which is how `contextFor` gives a lesson the task's conditions. **Review** — instruction proposals park on the Agent actor (`propose` / `listProposals` / `reviewProposal`); an acceptance is a new config version with the patch appended to `instructions`, reversible by `rollback` (LRN-08). AC-09 is scripted end to end over `mockAgent` in `packages/platform/__tests__/session-learning.test.ts`.

**Through the active plugin** (#242, MEM-02, LRN-01, AC-13): `memory` and `learning` are single-slot plugin kinds, and a session uses the workspace's ACTIVE plugin of each, over its config — the Registry's answer `Routing.run` already recorded on the spec (`spec.plugins`, #230), so no second hop. The app hands `platformLearningPorts` the build's implementations by plugin id (`memoryPlugins: Record<id, (config) => PlatformMemory>`, `learningPlugins: Record<id, (config) => LearningPlugin | LearningPluginFactory>`, composed in `apps/web/src/plugins/catalogue.ts` as `memoryCatalogue` / `learningCatalogue`); `memoryAccess(ports, gate)` and `learningAccess(ports, gate)` resolve them. The default memory plugin is `memoryActorImpl()` — `memoryActorPlugin` over the Memory actor, reached as the calling principal so the ACL still decides (MEM-11); the flat one is `flatMemoryActorImpl()` — `flatMemoryActorPlugin` over the **FlatMemory actor** (#281), reached the same way (below). Config knobs: memory `retrievalLimit` (entries a session starts with, default `DEFAULT_RETRIEVAL_LIMIT`; `0` asks no scope), learning `repeatThreshold`. **Turned off** — the memory plugin: `open` retrieves nothing and records why in `spec.retrieval.skipped` (`memory is turned off for this workspace: …`), nothing is written, learning is off with it, and the stored memories stay where they are (the Memory page still browses them); the learning plugin: `onTaskEnd` is not called and `correct` refuses with the reason. An active id the build does not implement is off the same way. `memory_search` / `memory_remember` stay listed and answer the same reason (`ToolCallError('unsupported')`), so the agent learns memory is off; they reach the same store the session retrieved from — on the local path through `anthropicApiRuntime({ memory })`, on the daemon path the `tool.call` port reads the gate from the calling session's spec, only for a memory tool. No gate on the spec (no Registry behind the router, a test): the static `memory` / `plugin` / `retrieval` ports, as before. **Switching with migration** (#243, MEM-09, AC-11): `Registry.previewActivation('memory', id)` is the dry run and `Registry.activate('memory', id, { migrate: true })` the move, both over `defineRegistry({ memoryPlugins })` (the app passes `memoryCatalogue`; without both ends a move refuses `no-migration`). `switchMemory` (`packages/platform/src/memory/switch.ts`) walks every scope the workspace's agents imply (`workspaceMemoryScopes`: each agent's own, then every shared scope any of them reads) through `migrate(from.open(scope), to.open(scope))` as the owner — the AC-11 path; the actor-backed stores carry the plugin's `fidelity` seam (it depends on the entry alone, so a local store of the same kind answers it), so the dry run reports per scope and in total what would be imported, skipped and dropped without writing. For real, each scope is VERIFIED — the target must hold exactly `imported` more entries than before — and `active` flips last: any failure is `migration-failed` and the old plugin stays active. Ids the target already holds are skipped, so a switch back imports only what is new; what a failed move already copied stays in the target; the source store is never touched (retention removes it with the workspace). The move runs inside the Registry's turn, so other Registry mutations wait for it; a session still writing to the old plugin while the move runs can leave its newest entries behind. The audit summary of `plugin.activated` carries the counts. The plugin page's "Make active" on a memory plugin shows the dry run (what moves, what is dropped, by agent) in a confirmation and moves only on confirm.

**The flat plugin's durable backend** (#281): the FlatMemory actor, one per scope under the SAME key as the Memory actor (`{ws}:memory:{scope}`) but its own type — so it is its own object and the two plugins' stores never mix, and `memoryAuthorize` (a static check on the key) guards both unchanged. Its state is `createFlatMemoryState()` (the flat store's plain records, JSON-safe; `createFlatMemoryStore({ state })` reads and writes it in place) plus a `rev` the pages follow; `persistence: 'explicit'`, a `ctx.save()` at the end of every mutating turn. Its method table is the Memory actor's (`put` … `importBatch`, `compact` always `0`, `getAcl`, `stats`), so `actorMemoryStore` (typed over `MemoryStoreClient`), the pages and the workspace export reach either the same way. **One ACL per scope, the Memory actor's** (MEM-11): who reads a shared scope belongs to the scope, not the plugin, so an agent on a shared scope is checked against the ACL the FlatMemory actor reads from that scope's Memory actor, as the workspace owner — one hop, only for an agent on a shared scope; there is no `setAcl` here. Switching the active plugin therefore never changes who reads a shared scope, and #243 has no ACL to move. The flat plugin ships enabled, not active. `exportAll` writes its entries into `memory.ndjson` marked `plugin: 'agentic.memory.flat'` and `deleteAll` purges both records of every scope. The agent pages read the active plugin's store (`useActiveMemory` on `Registry.overview().active.memory`).

Default learning (`@agentic/learning`, #27) works over any `MemoryStore`. `onCorrection` writes a `lesson` — `text` the correction, `evidence` one line per occurrence, `conditions` from `contextFor(correction)` (the task objective; the correction itself when unknown), `confidence: 'stated'`, `provenance.source: 'user'`; agent-detected corrections are dropped unless `acceptAgentCorrections`. A correction of the same `what` with ≥ 0.6 word overlap supersedes the live lesson (merged conditions, the newest `evidenceCap` evidence lines behind an uncapped `corrections in total: N` line, old one retired). The `repeatThreshold`-th repetition (default 3, and each multiple) adds an instruction proposal with `requiresReview: true`; nothing applies it here. `onTaskEnd` writes a `record` per task — `verified`/`refuted` are `confidence: 'verified'` + `source: 'verification'`, `claimed`/`none` are `assumed` + `agent`, the tag says which (LRN-03) — plus a lesson when a claim was refuted. Memory proposals are applied by the plugin (`apply: false` returns them instead); `applyProposals` is the one writer and runs `assertPermissionFree` over the batch first: no permission key (`permissions`, `grants`, `allowedTools`, `policy`, …) at any depth and no field outside `NewMemoryEntry` / the instruction shape, mirrored at the type level by `PermissionFree<T>` (LRN-08). `relevantLessons(memory, { objective, tags })` is the pre-task read: lessons only, no retired entry, no lesson sharing no word with the context. Counters go through the `CorrectionLedger` port (`recordCorrection({ agentId, week, what, at })`, ISO week in UTC); the Ledger actor (#45) implements it.

## 9. Plugins, MCP, A2A, auth

- `PluginManifest {id, version, kind: runtime | connector | memory | learning | notification | trigger | a2a | project-feature, name, description, capabilities[], config: ConfigSchema, secrets?: [{name, title, description, required}], permissions: [{scope, reason}], compat}`. `ConfigSchema` is a typed JSON-Schema subset core can validate (`validateConfig`) and the UI can render; secrets are declared beside it, never inside it; `pluginReadiness(state, facts)` is the one readiness verdict the pages and the platform share (#226). v1 plugins are in-repo modules registered at build time. Registry holds enabled/config/grantedPermissions; `dependents()` lists agents/schedules referencing a plugin (AC-13). Nothing receives a secret unless `grantedPermissions` includes it (PLG-04). Notifications v1 (`packages/platform/src/notify`): the seam is `NotificationChannel {id, deliver(notification, {workspaceId, subscriptions}) → {ok, error?, expired?}}`; a `notification` plugin implements it as a `ChannelPlugin {open({config, secret}) → NotificationChannel}`, and the app passes `defineInbox({channelPlugins, registry})` (#244): per notification `Inbox.push` asks `Registry.gate()` once — however many subscribers — which notification plugins are on, and opens each this build implements with its merged config and a `secret(name)` that goes through `openSecret` as the workspace owner (enabled + granted, audited). A Registry that cannot be asked is a failed attempt on channel `plugins`; static `channels` still exist for tests. `Inbox.push` records first, then fans out through `deliverAll` — a channel that rejects or answers `ok: false` becomes a `DeliveryAttempt` on the record, never a throw (OPS-04). `webPushChannel({fetch, vapid, ttlSeconds})` POSTs one push per subscription with VAPID (RFC 8292) signed as ES256 over WebCrypto, so it is edge-safe; 404/410 endpoints are reported `expired` and the inbox drops them. **Gap:** v1 pushes are *contentless* — no RFC 8291 `aes128gcm` payload encryption — so the service worker treats a push event as "inbox changed" and re-reads `Inbox.list`; encrypted payloads (title/body in the push itself) are a follow-up. Web Push is the plugin `agentic.notify.web-push` (`webPushPlugin` + `webPushChannelPlugin`, off by default): config `{subject (required), publicKey}`, secret `vapid-private-key`. Its page's "Generate keys" makes the P-256 pair in the browser (WebCrypto), saves the public half with `configure` and seals the private half with `setSecret`; a new pair drops the Inbox's subscriptions, since browsers subscribed with the old key. The channel opens the private key only when a notification has a subscription to go to, and without a contact, public key or private key it records a failed attempt naming what is missing, never an unsigned push. Browsers subscribe in Settings → Notifications (`apps/web/src/push`): `/sw.js` (`apps/web/public`) shows a generic "something needs you" notification per push — it cannot read the inbox itself — and focuses or opens the app on click. Triggers v1: HMAC-validated webhook → Task. **Runtime kinds (#313):** a `runtime` manifest declares what it is with a capability: `harness` (a product with its own loop and sign-in, driven through its CLI or SDK, e.g. Claude Code), `model` (the platform runs the loop over a model API, e.g. `anthropic-api`) or `remote` (an A2A peer). Core's `runtimeKindOf(manifest)` reads it. This is independent of `daemon-hosted` / `platform-hosted`, and `/plugins` groups runtimes by it. **Usage limits (#261)** are a runtime capability, `usage-limits`: the runtime supplies a `QuotaSource {id, version, runtime, probe?(env, ctx), fromSignal?(signal, env)}`, which is not a plugin kind. The daemon runs the build's sources (`builtinQuotaSources()`). `claudeCodeQuota()` maps a session's `rate_limit_event` to one window and probes an idle account's `/usage`; `anthropicApiQuota()` answers `not-reported` with its reason. Snapshots are normalized (`QuotaWindow`: utilization 0..1, `resetsAt`, `status`), merged with `mergeQuota`, and stored on the Machine.
- **Project feature plugins (#330 contract, #332 wiring, #335 git).** `ProjectFeatureManifest` is a manifest of kind `project-feature` with a second schema, `projectSettings`: what a project stores under `features[id]` (its presence enables the plugin for that project); `config` stays the plugin's workspace-wide settings. The code half, `ProjectFeaturePlugin`, sits beside the manifest in the catalogue: `detect(folder)` over the daemon's git badge (the project form suggests the feature when a folder is added), `beforeSession({ project, settings, taskId, chatId?, environmentId, cwd, fs })` run by the router once per task after the folder is resolved and before the session opens — may return a new `cwd` and `instructions`; a throw parks the task with the message, never a silent fallback (EXE-12) — and `instructions(ctx)` merged into the system prompt. Plugins run on the platform only and compose the daemon's generic folder operations (`list`, `worktree`, `locate`); no plugin ships daemon code or UI. The repo's identity across machines is `FsGitInfo.origin`, compared with `sameOrigin`. As wired (#332): the Registry lists a `project-feature` manifest like every other kind (multi-enable, never a slot; `assertPluginManifest` requires its `projectSettings`), `Registry.checkProjectSettings(pluginId, settings)` is what `Workspace.upsertProject` holds each `features[id]` to (400: unknown, disabled, another kind, or settings that fail the schema), and the router runs the hooks as §7 says (`RoutingPorts.projectFeatures`). `dependents()` does not yet count projects that enable a feature — deferred; disabling a feature plugin skips it at the next placement, and `upsertProject` refuses to store settings for a disabled one.
- **Git project feature (#335, `@agentic/plugins-git`, `packages/plugins-git`).** The first project feature plugin, edge-safe over `@agentic/core` only; the web's `projectFeatureCatalogue` maps its id to `gitFeaturePlugin` and lists `gitFeatureManifest` in the Registry's catalogue (enabled by default: it acts only on a project that switches it on). Id `agentic.feature.git`; `projectSettings`: `origin` (string — the origin remote URL as git writes it, https or `git@host:path`, so no `format: 'uri'`; the project form fills it from the first folder's badge, `identityOf(folder)` reads it, `sameOrigin` compares it), `worktreePerChat` (boolean, default false), `branchPrefix` (default `chat/`), `base` (optional start point), `instructions` (default `''`). `detect(folder)` is the git badge's presence (repo or worktree). `instructions()` is the trimmed settings text. `beforeSession` acts only with `worktreePerChat` on and a `chatId`: `branch = branchPrefix + short chat id` (the last 8 characters after `chat_`, lowercased — the same on every environment and for every later task of the chat; `gitBranchFor` throws on a prefix that makes an invalid ref name), `path = suggestWorktreePath(cwd, branch, hostOsOfPath(cwd))`, one `{ kind: 'worktree', repo: cwd, branch, path, base? }` op to the environment's daemon, then `{ cwd: <the daemon's path>, instructions: "This chat works on branch `…` in `…`." }`. Nothing is remembered per `{chatId, environmentId}`: the next task sends the same op and the daemon's `branch-exists` / `exists` resolve deterministically to the same path; every other error throws `git worktree <code>: <message>` and the router parks the task `waiting { project-feature }` (EXE-12), so a daemon that cannot make worktrees never silently falls back. Push/pull, PR creation, clone and worktree cleanup are out of scope (follow-up).
- MCP client (`@agentic/mcp`): `createMcpClient({url, auth}).tools() → AnyTool[]` — Streamable HTTP over `fetch` (edge-safe, no SDK; JSON and SSE responses, `Mcp-Session-Id`, `notifications/cancelled` on abort); `@agentic/mcp/node` adds `createStdioMcpClient({command, args})` for servers the daemon spawns (via `@sigx/ai-agent-node` + `@sigx/ai-agent/harness` JSON-RPC). `tools/list` entries become `defineTool({ jsonSchema: inputSchema, annotations })` with the wire schema verbatim and hint-for-hint annotations; `isError` results throw `McpToolError` so the engine shows the model a tool error. `mcpConnector({id, transport, url | command, secret?, headerSecrets? | secrets?, envSecrets?})` is the `PluginManifest` (kind connector) with `network:<host>` / `machine:<id>`, `secret:<name>` and `tools:<namespace>` permissions declared up front; every credential is a declared `manifest.secrets` entry and never config (the config holds only `url`, or `command` / `args` / `cwd`). `mcpConnectorSetup(options)` → `{ manifest, connector }` is what the app stores to add a server: `Registry.register(manifest, { enabled, grant: 'declared' })`, `Registry.putConnector(connector)` (same id — one server is one plugin; `auth` binds each secret name to the bearer, a header or an environment variable), then `Registry.setSecret(name, value)` per secret. **Adding one from the UI (#241):** `/plugins` → "Add MCP server" (`apps/web/src/pages/plugins/{connector.ts,AddConnectorDialog.tsx,LiveConnectors.tsx}`) takes a name (→ the id), a Streamable HTTP URL and a bearer token or an API-key header; the credential is sealed as `<id>.token` and bound by name. "Test connection" runs `openMcpConnector` from the page with the values still in the form (so a server that refuses browser requests fails the test and may still work for agents), and adding records what it found with `setConnectorStatus`. Remove is `Registry.remove`: `plugin-in-use` names the agents and schedules that pick it and removes only on confirm (`force`), deleting the connector's own secrets with it. stdio servers are not offered yet (#280). **Connectors on a session (#240, as built):** `Routing.run` passes the agent's `connectors[]` ids to the same `gate()` call as its runtime and the answer rides on `spec.plugins.connectors`. On a LOCAL open (`anthropicApiRuntime`), each ready Streamable HTTP connector is opened by the app's `ConnectorOpener` — `openMcpConnector` of `@agentic/mcp`, injected in `apps/web/src/plugins/catalogue.ts` so `packages/platform` does not depend on `@agentic/mcp` — with its credentials opened through `Registry.openSecret(name, connectorPluginId)` (the connector plugin's own grants, audited) and a 10 s deadline for connect + `tools/list`; its tools join the roster as `<namespace>__<tool>` (`connectorToolPrefix`) and it is closed with the session. Nothing about a connector fails the session: missing, turned off, stdio, a secret not set, a name clash or an unreachable server leaves it out, and the system prompt's "Connectors not available" section names it and why. A change in what an open found is written with `setConnectorStatus` (status, discovered tool names), never on every open; credential values are scrubbed from any recorded error. Approval: a connector tool goes through the agent's rules, then its `tools[]` grant by the namespaced name, then allow — and its request is presented as `source: 'mcp'` with a category from its MCP hints (`destructiveHint` → `destructive`, `readOnlyHint` → `read`, otherwise `network`), so category rules reach it. **Connectors on a daemon-hosted session (#280, as built):** for a daemon runtime, `Routing` puts the gate's ready connectors on `OpenSpec.connectors` (`daemonConnectors`: `{id, transport, url | command/args/cwd, auth}`, secret NAMES only). A stdio connector bound to another machine (`machine`, `*` = any) is left out and, with every missing or turned-off one, named in the prompt's "Connectors not available". The spec holds no value because the Machine keeps it in durable state and re-sends it after a reconnect (EXE-09). While it opens the session, the daemon asks for the values over its own `tool.call` (`CONNECTOR_CREDENTIALS_TOOL`; never offered to the model). `createToolCallPort` answers only for a ready connector the calling session's recorded gate names, opening each secret with `Registry.openSecret(name, connectorPluginId)` as the workspace owner, which is audited. An unset secret is refused and recorded on the connector's status once; nothing else is written. On the machine, `claudeCodeDriver({ connectors })` opens each connector: Streamable HTTP with `openMcpConnector`; stdio with `openStdioMcpConnector` (`@agentic/mcp/node`), which spawns the server in the connector's `cwd` or the session's (inside the environment's `cwdRoots`, else left out) with the stdio client's allowlisted environment plus its credential variables. Both share a 10 s deadline. The tools join the session's client tools on Claude Code's tool server as `<id>__<tool>`. The daemon's compiled policy sees them as `source: 'mcp'` with a category from the hints (`withConnectorPolicy`, the same mapping as `connectorPolicy`), and the connectors are closed with the session. A connector that cannot be opened is left out, named in the prompt and in the capability report (`connector:<id>`), with credential values scrubbed from the reason; the session still opens. The values are held in memory for that session only, never logged or written by the daemon. **Gap:** a daemon open does not record the connector's status or discovered tools; only an unset secret is recorded. `capabilityReport()` and the manifest's `unsupported:<op>` capabilities enumerate what the client leaves out — resources, prompts, sampling, elicitation, roots, logging, completions, tasks, `list_changed`, progress, the GET stream, resumption, OAuth discovery — and which of those a given server actually offers (PLG-09). Server-initiated requests are answered `-32601`, never dropped.
- **Platform MCP server = orchestration surface** (`/_agentic/mcp`, `createMcpToolHandler`). Design constraint from day one: every platform capability is an actor method, and both the web UI and the MCP server are thin projections over the same methods, so an external MCP client (Claude Code on a laptop, Claude Desktop, any MCP- or A2A-speaking orchestrator) can drive every daemon on every machine. Tool families, annotated readOnly/destructive so `allowReadOnly` composes: `machines.list`, `environments.list/doctor`, `agents.list/get`, `sessions.open(agentId, environmentId, cwd)`, `sessions.prompt`, `sessions.respond`, `sessions.cancel`, `sessions.tail(from)`, `tasks.create/delegate/get/tree/cancel`, `chats.post/history`, `memory.search/remember`, `schedules.create`. Machine selection is explicit in every call (EXE-12). Shipped (#50, `packages/mcp/src/server` + `packages/platform/src/auth/oauth-server` + `apps/web/src/auth/oauth-server`): wire names are `<family>_<op>` (`sessions_open`, …; `defineTool` names cannot carry a dot), the family IS the OAuth scope that gates it (`hasScope`, refused as an `isError` tool result naming the scope), and every tool runs over a per-principal `PlatformPort` — in the app a fresh `actor()` call per capability under the external principal, so each actor's own `authorize` chain decides; only the Workspace index read and `Routing.run` execute as the workspace's user (platform machinery, as the schedule trigger does). `sessions_open(agentId, machineId, environmentId, cwd?)` = Task.create with `origin { kind: 'external', clientId }` + `Routing.run`, after checking the named machine reports the named environment (404 otherwise, never a silent switch) and that `cwd` lies within its `cwdRoots` by `pathWithin` on the machine's OS (400 otherwise — a sibling prefix such as `C:/src2` for root `C:/src` included); the `cwd` becomes the task's `workdir`, so the session runs in it (`OpenSpec.cwd`, #190); `sessions_tail(sessionId, from?, limit?)` is a bounded page over `Session.events` with a `next` cursor. `tasks_delegate(taskId, agentId, objective, …, callId?)` = `Task.delegate` on the parent under the external principal (limits, depth, parent parked `waiting {child}`, COL-03) + `Routing.run` on the child as the driver, returning the child for `tasks_get` / `tasks_tree` to follow; `environments_doctor(machineId, environmentId?)` = `Machine.doctor` (#43). `chats_file_get(chatId, fileId)` (#209, `chats` scope) = `Chat.fileAccess` under the external principal through the optional `PlatformPort.chats.fileAccess` (missing and not visible are one `not found` error), then the bytes from the optional `files: ChatFileStore` of `createPlatformMcpHandler` — the same store instance the actors get (#207): text for a text-like type (cut at `CHAT_FILE_TEXT_MAX_BYTES`, `truncated`), an MCP `image` block for a `MODEL_IMAGE_TYPES` type, the record otherwise or without a store (`file bytes unavailable on this host`); `chats_history` keeps chat-file parts as their `agentic-file:` URIs. `usage_limits(machineId?, runtime?)` (#272, `usage` scope, read-only): every account (a machine's environment) with its latest `QuotaSnapshot` and `ageMs`, and `null` before the first report. Both surfaces return the same `UsageLimits` shape (`usageLimitsOf`, `packages/platform/src/machine/usage.ts`):
  - **MCP:** `PlatformPort.usage.limits`, bound in `apps/web`. The tool gate checks `usage`, then the machines are read as the workspace's user, so a client granted `usage` alone sees limits without `machines`.
  - **Agents:** the platform tool `usage_limits` (`PlatformPorts.usage`, granted by `ToolGrant` like the others). `createActorToolPorts({ machines })` reads the index as the workspace's user and each Machine under the agent's own principal.
- **Projects on the MCP surface (#334):** `PlatformPort.projects { list, setChatProject }` beside `schedules`. `projects_list` (scope `projects`, read-only) = `Workspace.projects` read as the workspace's user (owner-only, like the machine index), mapped to `ProjectSummary { id, name, description?, environments }` — the environments with a folder, never the folders themselves. `chats_set_project(chatId, projectId | null)` (scope **`chats`**, write) = `Chat.setProject` under the external principal: the chat is what changes, and `scopeOfTool` takes the family before the first `_` as the gating scope, so the tool carries the `chats_` prefix rather than the issue's `chat_set_project` — a client granted `chats` alone may set it, one granted `projects` alone may only list. The Chat's 400 (unknown project) and 403 surface as `isError` results with the message. The agent-side counterpart is the `projects` platform tool (§7).

  The description tells the model to weigh `resetsAt` and `ageMs`. Nothing is switched automatically (EXE-12). Declared gaps (`PLATFORM_MCP_UNSUPPORTED`): MCP resources and prompts. OAuth grant/revoke events are not on the Audit catalogue yet (`AUDIT_KINDS` has no `oauth.*` kind) — a follow-up once the catalogue grows.
  - **External-client OAuth (RFC 8414 / 7591 / 9728, OAuth 2.1).** `createOAuthServer({ secret, issuer, resource, store })` is pure `Request → Response` over WebCrypto and an `OAuthStore` (`memoryOAuthStore` in tests, `actorOAuthStore` over the global `OAuthClients` directory — anonymous `register`/`get`, like the `PairingDirectory` — and the per-workspace `OAuthGrants` actor `{ws}:oauth:grants`, owner-only). Public clients only (`token_endpoint_auth_method: none`), PKCE S256 required, redirect URI exact-match (loopback port excepted, RFC 8252), scopes = core's `Scope` union (`ALL_SCOPES`). Consent is a minimal HTML form rendered only for a signed-in user (else 302 to `/auth/login?returnTo=…`), sealed as an `oatx` transaction bound to that user; the user may untick scopes — the grant is the ticked subset. Tokens are HMAC-sealed on the package's `seal` envelope: `oacd` code (10 min, single use through the store), `oaac` access (1 h; decodes to `{ kind: 'external', workspaceId, clientId, scopes }`, `verify` also checks the grant is not revoked), `oarf` refresh (30 d, rotates on every use; a replayed generation revokes the grant), RFC 7009 revocation. The 401 on `/_agentic/mcp` carries `WWW-Authenticate: Bearer resource_metadata="…/.well-known/oauth-protected-resource/_agentic/mcp"`, which is all an MCP client needs: **`claude mcp add --transport http agentic https://<origin>/_agentic/mcp`** → the first request is challenged → the client reads the protected-resource metadata → the authorization-server metadata → registers itself at `/oauth/register` → opens the browser at `/oauth/authorize` (login, consent) → exchanges the code at `/oauth/token` → calls tools with the bearer, refreshing silently; no token is ever pasted. The MCP SDK asks for every scope the resource advertises, so narrowing happens on the consent screen.
- A2A 1.0, JSON-RPC binding only (`@agentic/a2a`, #31): `createA2aHandler({port})` is a fetch handler over a `SessionPort` (`agents()` = the agents flagged `exposeA2A`, `session(agentId, contextId, request)` = the live session behind a context, optional `authorize`) and a `TaskStore` for snapshots — no actors in the package. A card per exposed agent at `{base}/{agentId}/.well-known/agent-card.json` (the single/default agent also at the root well-known path); SendMessage, SendStreamingMessage (SSE), GetTask, ListTasks, CancelTask. Mapping: contextId = session, task = turn (task id = turn id), text/reasoning parts = artifact chunks, `request` = INPUT_REQUIRED answered by a message on the task, other events (tool calls, usage, errors, the turn result) = data parts under the `agentic` extension (`required: false`, a plain peer sees text only). State mapping: SUBMITTED = queued, WORKING = active, INPUT_REQUIRED / AUTH_REQUIRED = waiting, terminals 1:1 (`turn-end` stop reasons: end_turn/max_* → COMPLETED, error → FAILED, cancelled → CANCELED, refusal → REJECTED). Unsupported and declared: gRPC/REST bindings, push-notification config (`-32003`), extended card and Subscribe (`-32004`) — JSON-RPC errors, never a 500. Client `a2aAgent(cardUrl | card, {fetch, auth})` implements `Agent` (capabilities `resume: false, cancel: true, tools: 'none'`, `promptParts` from `defaultInputModes`, `permissions: 'harness-filtered'` when the card declares the agentic extension else `'none'`; `agent.a2a.unsupported` lists what is not translated, PLG-09), streams or polls GetTask, and passes the conformance subset (AC-14). The full event table lives in `packages/a2a/README.md`. **A2A peers as runtimes (#246):** a remote agent a user adds on /plugins ("Add A2A peer") is a `kind: runtime` plugin minted by `a2aPeer({ id, cardUrl })` — id `a2a.<id>` (a dot, not a colon: Registry ids and secret names share `[A-Za-z0-9._-]`, and the id is a `/plugins/:id` segment), config `{ cardUrl }`, an optional bearer secret `a2a-<id>-token`, permissions `network:<host>` + `secret:a2a-<id>-token` — registered on and granted what it declares (the token is set first, so a refused one leaves nothing half-added). The runtime catalogue resolves the prefix: `withInstanceRuntimes(fixed, { 'a2a.': (id) => a2aPeerRuntime(id) })` (`packages/platform/src/routing/factory.ts`) keeps the build's own runtimes first, then makes a local `RuntimeImpl` per peer id; each open reads the card URL from the gate's plugin config, opens the token through `Registry.openSecret` (never kept) and opens an `a2aAgent` session, continuing the peer's context from the session ref's `contextId`. Whether the id may run is the router's `gate()` as for any runtime: no such plugin or turned off fails the task `plugin-disabled`. The agent form offers it with the other runtime plugins (#234). No approval policy is handed to the peer session: a plain peer asks nothing, and an agentic one's requests surface as the Session's requests.
  - **Mounted as a plugin (#245, `apps/web/src/a2a`).** The `agentic.a2a.server` plugin (`a2aServerPlugin`, kind `a2a`, in the catalogue `enabledByDefault: false`, config `{ exposedAgents: string[] }` — agent ids or names, matched without case) is the switch; `createA2aMount({ actors })` in `entry.cloudflare.ts` answers `/.well-known/agent-card.json` and `/_agentic/a2a/{agentId}[/.well-known/agent-card.json]` with `createA2aHandler`, one handler per workspace. Every request: a bearer token of the platform's OAuth server (the MCP clients' — same issuer, store and grants; the mount builds its own `createOAuthServer` over them) or 401 with the resource-metadata challenge → the plugin enabled in the token's workspace or **404**, the same answer as nothing mounted → the `tasks` + `sessions` scopes or 403 `insufficient_scope`. `agents()` reads the plugin and the agents as the workspace (exposure is the plugin's decision, not the client's scope); a card is `promptParts: 'text'`. A context is `platformA2aSession` (`a2a/session.ts`) keyed by client + agent + contextId: each A2A task is ONE platform task — `Task.create` as the client (`origin { kind: 'external', clientId }`) + `Routing.run` as the workspace through `createActorPlatformPort`, so the runtime gate, the approval policy and the audit apply — whose session's first turn is followed through `sessions.tail` (polled, 250 ms) and handed back as the A2A turn; a task that fails before a session opens is `turn-start` + `turn-end {error}` naming the platform code (`plugin-disabled`, `no-api-key`, …), i.e. a FAILED A2A task. `respond` answers the session's request, `cancel` cancels the task. Earlier exchanges of the context go into the next task's `context`. Limits, v1: live A2A task state and context history are per isolate (the handler's in-memory `TaskStore`) — a `GetTask`, or the answer to INPUT_REQUIRED, that lands on another isolate does not find the task; the platform task itself is durable. The card carries no `securitySchemes` yet.
- Principals: `{kind: 'user', userId, workspaceId}` (GitHub OAuth behind an `AuthProvider` interface → `__Host-session` cookie → `createServerApp({authenticate})`), `{kind: 'machine', workspaceId, machineId}` (pairing code: 6 chars, 10 min, single use → machine token, hash stored), `{kind: 'agent', workspaceId, agentId, sessionId, taskId}` (minted per session for tool callbacks), `{kind: 'external', clientId, scopes[]}` (OAuth 2.1 + Dynamic Client Registration, RFC 8414/7591/9728, so `claude mcp add --transport http agentic <url>` needs no manual token; scopes gate tool families). Every actor's `authorize` chain starts with `sameWorkspace`. Principal propagates unchanged across `ctx.actor()` hops; `Task.delegate` re-mints an agent principal for the child (COL-10).
  - Auth seams (#32, `packages/platform/src/auth`, bound in `apps/web/src/auth`): `serverAuth({ sessionSecret, machines })` → `{ authenticate, codec }` for `createServerApp<Principal>`. Precedence: `Authorization: Bearer amt.<ws>.<machine>.<secret>` → machine principal (SHA-256 looked up on the Machine actor through the injected `MachineTokenLookup`; `revokedAt` → anonymous), `Bearer agt.…` → sealed agent principal, else `__Host-session` (HMAC-SHA256 seal of `{ userId, workspaceId }`, 30 days) → user principal; anything else `null`, never a throw. Login is `beginOAuth` / `completeOAuth` over the `AuthProvider` seam (`githubAuthProvider` over `fetch`; state + PKCE S256 in a sealed `__Host-oauth` transient, 10 min). In-process callers with no request (task bodies) act as a minted agent via `actor(Def, key).with({ context: asPrincipal(principal) })`. v1 identity: personal workspace, `workspaceId === userId === gh_<github id>` (colon-free so `workspaceOfKey` parses every actor key). Pairing: `issuePairing` stores only the code hash on Workspace; `POST /auth/pair { code, name }` verifies (used → expired → mismatch), consumes, and returns the one-time machine token. Stored API keys: `encryptSecret(importWorkspaceKek(WORKSPACE_KEK), value, aad)`.
- Credentials: Claude Code OAuth stays on the machine under `CLAUDE_CONFIG_DIR` (EXE-10); the platform stores only `authStatus`. API keys encrypted with `WORKSPACE_KEK` (AES-GCM via WebCrypto). Retention (OPS-10, `docs/retention.md`): workspace settings `{sessionLogDays, artifactDays}`; `exportAll` writes one NDJSON file per actor kind plus `manifest.json` through the `ArtifactSink` port (R2 in `apps/web`), every row read through the owning actor's `get` / `export` as the owner (a task body inherits no principal, so the cascade calls `actor(Def, key).with({ context: asPrincipal(owner) })`), secrets by name only; `deleteAll` purges every child record the index implies (agents, their memory scopes, chats + pages, schedules, inbox, registry) plus whatever `WorkspaceStore.list` returns (tasks, sessions), then `clearState()`s itself (`apps/web` purges each record inside its own Durable Object via `POST /_agentic/purge`, `SESSION_SECRET`-authenticated, and has no `list`, #100) — tested to leave no record in storage; copies handed to runtimes, providers and peers are listed as outside platform control. Plugin lifecycle (AC-13): `Registry.disable` returns `dependents(id)` and `requireEnabled(ctx, ws, id)` refuses NEW use from then on; running work is never interrupted by it.

### Target — plugins track (#224)

What the bullets above describe is built but not load-bearing: nothing in production registers a plugin, calls `requireEnabled`, reads `PluginState.config` or opens a Registry secret (`docs/runbook.md` §10). This is the design the sub-issues of #224 implement (`docs/decisions.md`, 2026-09-19); each replaces its part of this subsection with the as-built text.

- **As wired in the app (#231).** `apps/web/src/plugins/catalogue.ts` exports `pluginCatalogue` (the manifests, memory default before flat so it is the slot's default) and `runtimeCatalogue(options)` (`anthropic-api` → `anthropicApiRuntime`, the harness runtimes `claude-code`, `copilot-cli`, `codex-cli` → daemon, #322); `platformActors` hands the first to `defineRegistry({ kek, catalogue })` and the second to both `createSessionFactory({ registry, runtimes })` and `defineRoutingActor({ registry, runtimes })` (overridable as `PlatformPorts.catalogue` / `runtimes`). The Worker has no `ANTHROPIC_API_KEY`: the only key is the workspace's `anthropic-api-key` secret. Memory and learning follow the active plugin (`memoryCatalogue` / `learningCatalogue`, #242; the flat memory plugin durable on the FlatMemory actor, #281); notification implementations are still wired statically (#244). The A2A server (#245) is `{ manifest: a2aServerPlugin, enabledByDefault: false }`; its implementation is the Worker's A2A mount, which asks the Registry on every request (see the A2A bullet above).
- **Catalogue = manifest + implementation, assembled at the composition root.** Manifests live beside their code (`@agentic/runtimes`: `anthropic-api`, `claude-code`, `copilot-cli`, `codex-cli`; `@agentic/memory`; `@agentic/learning`; `notify`: web push; `mcpConnector()` and an A2A peer template mint one manifest per instance a user adds). `apps/web/src/actors.app.ts` builds `{ manifest, impl }[]` and injects it: `defineRegistry({ kek, catalogue })` gets the manifests; Session, Routing and Inbox get the implementations — runtime `{ host: 'local', open } | { host: 'daemon' }` (replaces `if (runtime !== 'anthropic-api')` in `createSessionFactory`), memory → `MemoryPlugin`, learning → `LearningPluginFactory`, notification → `NotificationChannel`. `packages/platform` never imports a plugin package it does not already depend on.
- **Manifest.** `config` becomes `ConfigSchema`, a typed JSON-Schema subset with a pure `validateConfig` / `configDefaults` in core; `secrets[]` is declared apart from it, each entry with its `secret:<name>` scope. `pluginReadiness(state, facts)` — `ready | disabled | needs-config | needs-secret | needs-grant | needs-machine | no-kek` — is one pure core function the pages and the platform share; the caller supplies the facts (secret names, environments, whether a KEK is set), the function hops nowhere.
- **Built-ins are virtual until touched — as built (#229).** `defineRegistry({ kek, catalogue })`; an entry is a manifest or `{ manifest, enabledByDefault: false }` for one that ships switched off. `current(id) = stored record ?? catalogue default` (enabled, every declared scope granted, `registeredAt: 0`); state keeps the owner's config values only and every view merges them over `configDefaults`, so a new build's defaults flow through. Reads never write (pinned by a test on the storage's saves); the first mutation materialises the record and saves in the turn; a stored built-in's manifest is always overlaid from the build — undeclared grants lapse, a scope no build declared before is granted once (`seenScopes`), one the owner revoked stays revoked; `register` over and `remove` of a built-in refuse `builtin`; a plugin a later build drops falls back to its stored record. `openSecret` works for a built-in that was never materialised. `memory` and `learning` are single-slot kinds: `active[kind]` names the one plugin in use.
- **Secrets are never config — as built (#229).** In through `Registry.setSecret`, out only through `openSecret(name, pluginId)` (enabled + granted, audited). `configure` (and `register`'s config) validates against the manifest's `ConfigSchema` with defaults filled in — `BadConfigError` names every path, unknown keys included, and nothing is stored. A manifest's `secrets[]` entry without its `secret:` permission is a `bad-manifest`.
- **One hop.** The Registry side is built (#229): `gate({ runtime })` → `{ runtime: { id, enabled, config } | null, memory, learning, channels: { id, config }[] }` — `null` when no runtime plugin has that id, memory / learning the ACTIVE plugin of the slot, channels the enabled notification plugins, every `config` ready to use; it reports and never throws. `overview()` is one live read (plugins, active slots, secret names, `hasKek` — the `pluginReadiness` facts minus the machines) and `dependentsAll()` one walk of the workspace for every plugin. Still to come (#230): `Routing.run` calls `Registry.gate({ runtime })` once, before a route is written: `{ runtime: { enabled, config }, memory, learning, channels }`. The result rides on the `SessionOpenSpec`, so the session's memory and learning ports need no second call. Pages read `overview()` and `dependentsAll()` — one call each, not one per plugin.
- **Flow: a key set in the UI reaches a turn.** `/plugins/anthropic-api` → `Registry.setSecret('anthropic-api-key', …)` (sealed under `WORKSPACE_KEK`) → a message → `Routing.run` → `gate()` (applies `config.defaultModel` when the agent names none) → the session factory's local runtime → `Registry.openSecret('anthropic-api-key', 'anthropic-api')` → `createPlatformModelAgent`. No key → `no-api-key`, naming the plugin page. There is no deployment-wide key, and no plaintext is cached in the isolate.
- **Flow: a plugin is disabled.** `Registry.disable` saves, audits and returns the dependents → the next `Routing.run` fails the task `plugin-disabled` (recoverable); schedules follow, since a firing goes through `Routing.run`; the `fallback-api` path is refused; `openSecret` refuses as defence in depth; running sessions finish. Memory or learning disabled → new sessions skip retrieval and writes, the data is kept. Re-enabling restores everything.
- **Flow: an environment is added from the web.** Machine page → owner-only `Machine.putEnvironment(input)` (never a platform or MCP tool) → `env.request` frame, tracked like `fsRequest` → the daemon checks its machine-local `policy.json` (off by default, editable only on the machine), contains every root by realpath, allocates the `profileDir` itself, writes `environments.json` atomically, calls `setEnvironments` → `env` + `env.response` → the Machine folds the descriptors and the page shows the row, `signed-out`, with `agentic-daemon env login <id>`.

## 10. UI

Routes (`@sigx/router`): `/` (inbox + active tasks), `/chats`, `/chats/new` (#336, the daemon's deep link, before `/chats/:id`), `/chats/:id`, `/projects`, `/projects/new`, `/projects/:id` (#333), `/agents`, `/agents/:id` (overview, config + versions, memory, sessions), `/tasks`, `/tasks/:id` (tree), `/sessions/:id`, `/machines`, `/machines/:id`, `/schedules`, `/plugins`, `/settings`, `/pair`, `/history` (Audit), `/usage` (Ledger). The visual design is the handoff vendored at `docs/design/` (`HANDOFF.md` is the spec; `tokens.json`, `screenshots/`, `artboards/`).

- Design system (`packages/ui/src/design-system`, `@agentic/ui/design-system`): zero-daisyui's tokens and recipes re-tuned to the handoff and compiled by `@sigx/zero-kit`'s `runStandardBuild` (`scripts/build-design-system.mjs`) into `dist/ds/**` — `@agentic/ui/css`, `/css/tokens`, `/register`, `/manifest.json`. One theme, `control-room` (dark only in v1, `docs/decisions.md`), is both scheme defaults; `installThemes()` from `@agentic/ui/design-system` seeds zero's registry. The handoff's `--ag-*` properties (line, line-strong, text-muted, text-dim, link-hover, agent-1..4, radius-xl, control-h, input-h, control-h-touch, touch-min, pill-h, sidebar-w, topbar-h, space-8) are kit `custom` tokens; product states ride a `tone` axis (`data-tone`: muted | dim | live | working | needs-you | failed) and a `kind` axis (approval | input | interrupted | offline | machine | auth | runtime | task), presence flags ride modifiers (`hollow`, `outline`, `compact`, `selected`, `current`) — never `data-state`, whose vocabulary zero governs. daisy recipes are replaced per scope by `withOverride` (one recipe per scope, never two); daisy's `api` is not carried (its emitter would self-import `@agentic/ui`), so pages keep `@sigx/zero-daisyui/components`. The web build runs `sigx zero:validate` on this design system with the `ai-*` fragment merged (`--extra-manifest`). The kit contrast-checks role and base pairs only; `packages/ui/__tests__/design-system.test.ts` measures the `--ag-*` inks. Fonts (Schibsted Grotesk, JetBrains Mono) load from Google Fonts through `useHead` links in `App.tsx`.
- Ops pages (`apps/web/src/pages/{Machines,Machine,Pair,Schedules,Plugins,Settings,History,Usage}.tsx`, #90): each is a `*View` component over props plus a thin route component that reads `apps/web/src/mock/ops.ts`, so the actor issues (#36, #42, #48, #44, #45) swap the data source without touching the layout. `pages/ops/OpsPage` carries `data-page` for `styles/pages.css`, a visually hidden `h1[data-page-title]` (the breadcrumb is the title; Pair shows a hero) and one head row for page actions; `pages/ops/LinkButton` is a real `<a href>` stamped with the button anatomy. The Machine page's `SessionsTable` is the agent Sessions tab's table too. Rows that carry `data-*` are raw `<tr data-scope="table" data-part="row">` because zero's `Table.Row` forwards only `class`.
- Component kit (`packages/ui/src/kit`): the handoff's app components, one visual per domain state. Eight `ag-*` scopes join the fragment with anatomies and recipes — `ag-pill` (status pills and tags; `PILLS` maps every `TaskStatus`, machine presence, auth status and tool phase to a tone, a solid or hollow dot and a label), `ag-agent-tile` (square monogram in one of four identity hues, people are circles), `ag-env-line` (`machine / runtime / account`, never one part alone), `ag-needs-item` (`kind` approval | input | interrupted, `compact`), `ag-task-node` (depth rail, `selected`), `ag-connection`, `ag-version` (current | proposed | past), `ag-env-card` (`EnvironmentCard`: capacity meter, queued count, default-for tiles, auth fix line). `tokens.scopes` carries each scope's vocabulary claim (`kitScopes`). States (`kit/states`, #87) add `ag-failure` (`FailureCard`: the six named failure states of OPS-04 from one `FAILURES` table — name, icon, signal caption, tone, action; `data-kind` uses the declared axis spelling, `client-offline` → `offline`), `ag-banner` (`OfflineBanner`), `ag-empty` (`EmptyState` variants), plus `EventsLostRow`, the `ConnectionStrip` row helpers and the skeleton presets; with every declared axis value wired, the web build's `zero:validate` runs `--strict`. Compositions over zero carry no scope: `Button` maps the five intents to daisy `color` / `variant` / `mods` (primary = primary solid, default = neutral solid, wait = warning solid, danger = error outline then solid at the confirm step, icon = neutral square with a required label), `Segmented` is a `ToggleGroup` with `aria-pressed` and a per-segment tone through the group's accent tokens, `DataTable` turns the artboard's column template into `<col>` widths on zero's `Table`, `ConfirmDialog` is zero's alert-dialog preset listing every dependent before a button that states the consequence, `TimelineList` on `Timeline`, `Switch`, `ChipInput` (the multi-select gap), `SectionHeading`, `Label`, `Icon` (the artboards' geometry). The root exports `componentExportName(scope)` for every `ag-*` scope (`AgPill`, `AgAgentTile`, …).

- Inbox page (#40, `apps/web/src/pages/inbox`): "Needs you" is `NeedsYou` over a `NeedsSource` — rows from the Inbox `list` (unread `approval` / `input` with a session ref; the mock adds interrupted), each row's card over `Session.request(id)`, answers through `Session.respond` with `once` | `session` from the `ai-approval` card or `{ type: 'input' }` from the `ai-question` card (#194: one block per question of the request's `schema`, answers keyed by property — Claude Code's `AskUserQuestion` reads nothing else; `Session.respond` fits a plain string to the form). The chat renders the same card on the tool call that asked (`ToolCall`) and loose input requests at the thread's end. `liveNeedsSource(defs, workspaceId)` runs both reads `{ live: true }` (a decision anywhere drops the row and collapses the card everywhere), `memoryNeedsSource` is the same contract for the mock and the tests, `useNeedsSource` picks by `data-mode.ts` (`live`: `useActorDefs` — `Inbox` is in `ActorDefs` — and `useViewer`; else the mock). The chat's approval card (`Thread`'s `onRespond`) is wired the same way by the chat page.
- Live data via `useActorState(Def, key, 'get', {live: true})`; transcripts via `Session.tail` into `createReducer` / `reduceAgentEvent` (in-place mutation, one signal per part); markdown via `@sigx/markdown/dom`.
- Live data via `useActorState(Def, key, 'get', {live: true})`; transcripts via `Session.tail` into `createReducer` / `reduceAgentEvent` (in-place mutation, one signal per part); markdown via `@sigx/markdown/dom`. Wired for chats and sessions (#34, `apps/web/src/actors`, `pages/chat/{live,feeds,directory,LiveChat,LiveChats}`, `pages/session/{live,LiveSession}`): the platform's definitions are factory-built, not `*.actor.ts` modules, so the browser gets hand-built `__actorRef` stubs (`clientDefs()`) and SSR the registry's own objects (`platformDefs()`) through the `useActorDefs` injectable each entry provides; the page learns its workspace from the `whoami` serverFn (`useViewer`). Calls and the `$live` stream ride the Worker's HTTP actor mount (`actorsPlugin({ transport })`; the object-terminated socket is a follow-up). `Chat.get` and `Chat.history` are live reads; each `sessions` row becomes a `connectSession` feed whose transport is the Session actor (`get` → `hello`, `tail` → event frames, commands → the actor's idempotent methods), replayed from `{ epoch: 0, seq: 0 }` so a reload mid-turn resumes from the log; `composeTranscript` folds the chat's entries and every feed's in-flight rows into the one transcript the Thread renders. A post is `Chat.post(text, mentions)` → one `Task.create` per activated agent (origin = the message, context = the entries visible to that agent) → `Routing.run`, from the browser (`runActivation`). Mock mode (`data-mode.ts`) keeps the design track's workspace for the dev server and Playwright.
- Agents on the platform (#35, `pages/agent/{live,head,LiveAgents,LiveAgent,NewAgentDialog}`): `/agents` reads the Workspace index live and each agent's identity through the chat directory (`Agent.get`); "New agent" (topbar → `newAgentRequest`) is `Workspace.createAgent` then `Agent.update(newAgentPatch, 'Created in the web UI')` — v1 on `anthropic-api` with `offlinePolicy: fail` — and lands on `/agents/:id?tab=config`. `/agents/:id` folds `Agent.get` (live) and `listVersions` (re-read per `configVersion`) into the same `AgentProfile` the mock tabs render (`profileOf`); the Config tab takes a `ConfigStore` so a save is `Agent.update(configPatch(config), reason)` and a rollback `Agent.rollback(v)`, the rail showing the version the actor recorded; "Start chat" is `createChatWith(defs, ws, [id], null)`. Demo 1 is proven three ways: `__tests__/pages/demo1-live.test.tsx` (the pages on the live harness, mock model), `__tests__/workers/demo1.test.ts` (dev login + the actors in workerd), and `e2e/demo1.spec.ts` through `smoke:demo1` against a deployed Worker (`docs/runbook.md`). The preview / local-only `/auth/dev-login` (`src/auth/dev-login.ts`) exists only while `AGENTIC_DEV_LOGIN` is set and mints a `dev_<user>` session, its own id namespace beside `gh_<id>`: `POST` JSON `{ token, user }` for the smokes, `GET` a plain HTML form (the secret prefilled from `?token=` on localhost only) that POSTs itself and lands on `/` (#143). The Worker records which doors are open per request (`src/auth/sign-in.ts` ← `entry.cloudflare.ts`); the `signInOptions` server function tells the shell, whose sidebar foot shows "Dev login" / "Sign in with GitHub" to a signed-out visitor in live mode. `pnpm dev` (`scripts/dev.mjs`) generates `apps/web/.dev.vars`, builds when the sources are newer than `apps/web/dist`, starts `wrangler dev` and prints the link (`docs/runbook.md` §4).
- Provider limits (#270, part of #261): `QuotaMeter` / `QuotaPanel` (`ag-quota`, `ag-quota-panel`) draw a `QuotaSnapshot` the way Claude Code's `/usage` does. Status is the tone; the reset time is in the viewer's zone; a panel older than 30 min is `stale`; `not-reported` and "nothing yet" are explicit (OPS-07). The panels appear on every Machines environment card (`EnvironmentCard.quota` ← `MachineView.quota`), in the `/usage` "Limits" card (one panel per account, `pages/usage/Limits.tsx`: `LiveLimits` reads the machine index, then `Machine.get` per paired machine), and in Home's rail (the tightest window per account, `compact`). **Where work is assigned (#315):** `QuotaBadge` is a one-line `QuotaMeter compact`: a short label (`Week · Fable`), a 56 px bar and the percent, with the full label and reset time as a tooltip. It appears in three places:
  - **Chat members:** each member of a chat shows the badge for the environment it runs on there (its working folder's environment, else its default: `pages/chat/quota.ts`).
  - **Folder picker:** the working-folder picker's environment strip shows it per environment (`WorkdirEnvironment.quota`, from `EnvironmentEntry.quota` ← `MachineView.quota`).
  - **New chat:** the dialog picks members from cards showing where each agent runs and that account's limits, with the coordinator as a radio on the picked cards (`pages/chat/MemberPicker.tsx`, shared with the project form).
  - **Projects (#333, `pages/projects/{model,live,locate,head,ProjectsView,ProjectForm,LiveProjects}`, `pages/Projects.tsx`):** `/projects` lists `Workspace.projects()` live (name, members, one badge per environment with a folder, the enabled features); `/projects/new` and `/projects/:id` are one `ProjectForm` — name, description, the roster on the member cards, connectors as chips over the Registry's connector plugins, one folder row per daemon environment (a `WorkdirInput` scoped to that environment; the picker's `select` now carries the folder's git badge, `WorkdirSelection`), and every enabled `project-feature` manifest as a switch whose settings render from `projectSettings` on `SchemaForm`. **Find** on an empty row runs `Machine.fsRequest(env, { kind: 'locate', origin })` with the origin of another row's badge and reads `fsResult` live (`useLiveLocate`, the picker's deadline); the matches (path + branch) are offered in a dialog and a pick fills the row; a row whose checkout has another origin warns (`sameOrigin`) and never blocks. A plugin's `detect` (`projectFeatureCatalogue`, `plugins/catalogue.ts`; empty until #335) marks the row and preselects the feature when the project has none; a schema with an `origin` property is prefilled from the first badge. Save is `Workspace.upsertProject(patch)` — `projectPatchOf` sends every section whole with dropped folders / features as `null` — its 400 inline; delete is `removeProject`. New chat opens on a project picker preselected from `Workspace.get().lastProjectId`: picking one fills the roster and the coordinator from `members` and states its connectors and folders; `createChatWith(..., projectId)` is `Workspace.createChat({ projectId })`. The chat header shows the project chip (`ChatSummary.project` → `chatHead.project`, linking to `/projects/:id`) and the list column filters by project (`MockChatSummary.projectId`).

  A model-runtime member says "No plan limits · API key". Nothing here moves work (EXE-12).
- Machines on the platform (#144, `pages/machines/{live,head,environments,LiveMachines,LiveMachine,LivePair}`): `/machines` reads `Workspace.listMachines` live and, per paired entry, `Machine.get` live (`machineOf`: the daemon's name at `pair` over the index's, `online` false once revoked) — a socket drop flips the group without a reload; `Routing.get` supplies the EXE-12 queued counts (`waiting-offline` routes per environment); the chat directory supplies the "Default for" tiles (`defaultForByEnvironment`, from `identityOf`'s environment line) and the platform row's agents, whose key pill reads NOT CHECKED (the deployment's key serves every workspace). `/machines/:id` reads `Machine.get` and `Machine.doctor` live (`doctorChecksOf`: isolation across the environments, then per environment its sign-in and the daemon's verdict with its findings — an unverified environment is a failed "not checked" row), `Task.get` per hosted session for the objective (`sessionsOf`: `opening` reads AWAITING); Revoke is `Machine.revoke` (REVOKED in the hero, the card stops offering it); "Run again" re-reads the stored verdicts because the platform cannot run the daemon's doctor; an unpaired id gets a not-paired card; the topbar crumb reads `machineHead`. `/pair` mints through `Workspace.registerMachinePending({ name })` on the client once the workspace is known (`machine-N` by default; a rename mints again so the index entry and the daemon's `--name` agree; "New code" mints again), shows the runbook's `install.ps1` line and `agentic-daemon pair <code> --url <origin> --name <name>`, and watches the pending record live — `paired` moves the page to `/machines/:id`. The agent Config tab's environment picker (`useEnvironmentOptions`: the index live, then `Machine.get` per paired machine) offers every environment as `machine / runtime / account` and a save persists `execution.defaultEnvironmentId`. `MachineView`, `MachineGroup`, `EnvironmentGrid`, `PlatformRow`, `SessionsTable` and `PairView` take their facts (queued counts, default-for tiles, agent lookup, commands, revoked state) as props, so the mock pages pass the sample workspace's and the live pages the actors'. Tests: `__tests__/pages/machines-live.test.tsx` on the live harness, `__tests__/workers/machines-pages.test.ts` in workerd (the pair route mounted as the actor mount's fallback so its hop to the Machine object runs under the Worker's host scope, #137).
- Home, Tasks, History and Usage on the platform (#146, `pages/task/{live,LiveTasks}`, `pages/history/{live,LiveHistory}`, `pages/usage/{live,LiveUsage}`, `LiveHome` in `Home.tsx`): `/tasks` and Home's active tasks are ONE live read of the workspace's `TaskIndex` (`list()`; every Task actor writes its row over a hop on create and every status / wait change — §4), each row's assignee through the chat directory, "Stop all" = `Task.cancel('user')` per chain root (`chainRoots`: an active task whose parent is not itself active; the cascade covers the subtree). `/history` is `Audit.list(query)`: the kind chips, the agent select and the time window are the actor's `kinds` / `agentId` / `since`, "Load older" passes the page's `seq` cursor and the pages merge by event key; a read that takes an options object cannot be a `useActorState` key (primitives only), so the page subscribes to `Audit.stats()` live and re-keys its `useData` first page on `recorded`. `/usage` is `Ledger.summary({ by: agent | task | day, timeZone })` over the current UTC month (`ledgerMonthOf`) through `useData`, re-read on mount and on Refresh; a cost is printed as its reported part plus `~` its estimated part (`costPartsOf` / `costText`), `n/a` in the dim ink when no row was priced (OPS-07), the quality pill per group; Home's spend panel reads the same total (`useMonthSpend`). Home's "Today" panel does not read the schedules yet. `ActorDefs` carries `TaskIndex`, `Audit`, `Ledger`; the platform stays out of the client bundle (types only).
- Zero components per screen: shell `Navbar` + `Drawer`; chat `Chat` rows + `Avatar` / `Status` / `Badge`; composer `Textarea` (`modelModifiers`) + `Button` + `FileUpload` + `Kbd`; tasks `Table` / `TreeView` / `Timeline` / `Steps`; approvals `Dialog` / `Alert`; inbox `Toast`; settings `Field` + `Input` / `Select` / `Switch` / `Combobox`; cost `Stats`.
- `@agentic/ui` fragment scopes, styled to `docs/design/HANDOFF.md` → "`ai-*` fragment": `ai-thread` (root/earlier/list/row/anchor, `data-state` on | off; the "Showing the last N entries · Load earlier" chip), `ai-message` (root/avatar/meta/name/environment/time/body/image/file/file-name/file-size/tools/footer — an `image` part is a lazy ≤ 320 px thumbnail linking to the full size, a `file` part a download chip, placement start | end — a row on the kit's `AgentTile` and `EnvironmentLine`, the STREAMING pill mid-turn; who an author is comes from the page through `Thread.describe`), `ai-composer` (root/addressing/recipient/hint/attachments/attachment/thumbnail/attachment-name/attachment-size/spinner/attachment-error/input/mentions/mention/actions/keys, over zero `Textarea` and the kit `Button`; the page resolves `recipients` per CHT-06; files arrive by the Attach button's hidden `<input type=file multiple>`, by paste (`clipboardData.files`) and by drag-and-drop — the root takes the governed `highlighted` flag while a drag hovers, as zero's `FileUpload` does — and all three emit one `files: File[]` event: the composer is presentational and the host owns the upload (CHT-10, #207), feeding back `attachments` whose `status` `uploading | ready | error` is the chip's `data-state` `loading | complete | error`; Send waits while any chip uploads and may go with an empty draft; `prepareImage` downscales to 1568 px and re-encodes as WebP / JPEG before upload when that is smaller), `ai-tool-call` (root/header/icon/name/signature/meta/status/input/output/more/log/error/agent), `ai-reasoning` (root/summary/body), `ai-approval` (root/header/title/rule/request/description/context/actions/record, `compact` modifier; one card everywhere — chat, inbox, task, session, mobile). The tool lifecycle pending | running | done | error | denied is spelled in zero's governed states — `loading | active | complete | error | closed` — with the handoff's word on a `StatusPill` in `status` and the refined phase in `meta` (no lifecycle family upstream: andtii/zero-wip#483); the border takes a colour only for `active` and `error`. The pack stays on the recommended grammar (the `--ag-*` inks are read with fallbacks; state colour rides `data-state` and nested kit scopes, never a `tone` axis of its own), so it remains promotable as is. Declared with `"sigx-zero": { "fragment": "./dist/fragment.js" }` (+ `dist/fragment.json`) and a recipe pack; the app adopts it with `sigx zero:extend` against `@sigx/zero-daisyui` once a kit carrying the command is on npm (andtii/zero-wip#482) — until then `mergeManifests` + `recipes` by hand, and `packages/ui/__tests__/fragment.test.ts` runs the `zero:fragment` checks. The thread windows by PART (a message of 2 000 parts renders its last 150), keys rows by message id so a sliding window patches rather than remounts, and freezes the window while the reader is scrolled up. The composer reads keys on its `<form>` (zero `Textarea` forwards no key events: andtii/zero-wip#481).
- Responsive: the published zero (0.2.0-beta.1) has no layout tier (andtii/zero-wip#473 landed upstream after it), so `@agentic/ui` carries one — `Stack`/`Row`/`Col`/`Spacer` in `packages/ui/src/layout` render `data-l-*` attributes (gap/pad on the `--space-*` ramp, align/justify/wrap/grow, breakpoint in prefix position: `data-l-md-gap`) painted by one `layout.css` step table; `useMediaQuery` is the SSR-safe `matchMedia` signal. The shell (`packages/ui/src/shell`, `AppShell`) is the handoff's: a sticky 232 px sidebar (`--ag-sidebar-w`; brand, nav `groups` — Primary unlabelled, Workspace headed — each a `<nav aria-label>`, per-item `badge`, then the `connection` and `user` slots) beside a 60 px topbar (`--ag-topbar-h`; `breadcrumb` and `actions` slots on zero's `Navbar`) and `<main>` (padding 28, `flush` for Chat); below 768 px the sidebar becomes zero's modal `Drawer` (312 px, 50 px items, connection strip at the foot, 200 ms slide off under reduced motion) behind a 44 px menu button and the topbar a 60 px app bar on base-200 — menu or `back`, `title` with the `subtitle` slot, one right slot (`phoneAction`, else the last action); one hand-written `shell.css`; router-agnostic (the app renders `Link` through the `link` and `back` slots). Per-screen grids live in `apps/web/src/styles/pages.css`, one section per page, plus the two responsive blocks of the handoff's single 768 px breakpoint: 768–1279 the fixed rails drop under the main column (Home, Task, Session, Machine, Memory), Chat keeps list + thread with the context panel in an end drawer behind the topbar's tasks button, three-column card grids become two; below 768 every grid is one column, `DataTable` stacks its rows into captioned cards from CSS alone (`--ag-col-N` on its wrapper, no second markup), the composer docks with 48 px controls, buttons are 48 px and split a row evenly, the environment card folds to a 52 px row, the message meta drops its environment line. The topbar is the route's: `apps/web/src/crumbs.ts` builds the trail (the section root plus the page's entity name from its `defineTopbar` contribution) that is the one-line breadcrumb at ≥ 768 px and the app bar's title + back link below; pages register their actions, sub-line and phone action in `components/topbar.ts`, never in an in-content head row. Playwright runs at 400 / 1024 / 1280 (`apps/web/e2e`, the CI `e2e` job). `themeInitScript` reaches `<head>` through `useHead`; `installThemes()` runs once per entry; a per-request `ThemeProvider` serves `useTheme()` on the server. The app's `build` runs `sigx zero:validate` against the consumed design system first, so CI validates it. No virtualised list in zero: the thread windows its own rows.
- Forms (`packages/ui/src/forms`): every configuration surface edits a draft bound through zero's `model=` contract (`Field` + labelled control, `Field.Error` wired by `aria-describedby`) and posts the same field names pre-hydration; one model module per form (`agent-model.ts`, `settings-model.ts`) owns config ⇄ draft ⇄ `FormData` and validation, so the live and posted paths cannot drift. A valid submit writes back through the model and emits; persistence (the Agent actor's versioned config, Workspace settings) is wired by the pages. The approval editor owns one rule per tool category (`id: category:<name>`, first in the policy); every other rule is carried through untouched. Multi-selects use `_zero-gaps/multi-select.tsx` until zero ships a multiple mode (andtii/zero-wip#479). The Agent config page (`apps/web/src/pages/agent/ConfigTab.tsx`, #89) renders `AgentForm` in its `sections` layout with the segmented approval control and puts the save card (only while `dirty()`) and the versions rail (`VersionItem`: proposed / current / past) in the form's `rail` slot; the memory tab (`MemoryTab.tsx`) is the handoff's row grid over `MemoryEntry` with filter chips per kind; the sessions tab shares the Machine page's sessions table columns (`SESSIONS_COLS`). Agent pages read `apps/web/src/mock/agents.ts` until #35 / #41 wire the Agent and Memory actors.
- Schedules, Plugins and Settings on the platform (#145, `apps/web/src/pages/ops/{live,environments,head,LiveSchedules,NewScheduleDialog,LivePlugins,LiveSettings}`; `Schedule` and `Registry` join `ActorDefs`, `clientDefs` and `platformDefs`): the routes switch on `dataMode()` like the chat and agent pages, the mock `*View` untouched. `/schedules` reads the Workspace index live and each entry's `Schedule.get` as its own live read (a firing or a switch re-renders that row), on the workspace's `settings.timeZone` (AST-07: `nextRunLabel`, `whenLabel`, the DST footer); the switch is `Schedule.enable` / `disable`; "New schedule" (topbar → `newScheduleRequest`) is `Workspace.createSchedule` then `Schedule.create(newScheduleSpec(input, tz))` — a reminder at a wall time typed on the workspace clock (`wallToInstant` resolves it through `Intl` like `recur.ts`: a spring-gap time lands after the gap, a repeated fall time takes its first occurrence), or a cron recurrence, plain or as an agent task with an environment (`useEnvironmentDirectory`: the paired machines' `Machine.get().environments`, also the Settings default-environment select; an offline machine shows the policy line). `/plugins` reads the Registry's `list`, `connectors` and `secrets` (names only) live, `dependents(id)` per plugin for "Used by"; switching off asks `dependents` first and the dialog lists them by name (`dependentNames`: agents with how, schedules through their agent), the confirm is `Registry.disable`, whose answer the card states ("Still referenced by …; new use is refused, running work finishes" — AC-13 through the page). `/settings` edits a draft of `Workspace.get().settings` (time zone from `Intl.supportedValuesOf`, default environment, the workspace-wide inbox / push flags, retention in days) and Save is `Workspace.updateSettings(settingsPatch(draft))`; keys are the Registry's secret names; "Export workspace" is `Workspace.exportAll` with the record's outcome shown (`opStatus`), "Delete workspace" is `Workspace.deleteAll` behind a dialog that needs the workspace's name typed back. Tests: `__tests__/pages/ops-live.test.tsx` on the live harness (which now takes extra `actors` and host `defaults`; a reminder created on the page fires from the entry's own reminder into the Inbox, the plugin dialog and card, the settings round trip, an export into a fake sink with its manifest, delete-all refusing the wrong name), `ops-live-model.test.ts`, `__tests__/workers/ops-pages.test.ts` (the browser stubs against the Schedule and Registry objects; an alarm run early re-arms).
- Failure distinction (OPS-04) from four signals: client socket status, `Machine.online`, environment `authStatus`, adapter `error` events, `Task.status` — each rendered by its own `FailureCard` kind (`kit/states`), never a generic error. Recovery (OPS-05): interrupted turns are marked, never auto-replayed; "Resume" issues a new prompt over the intact transcript. Wired by #46 (`apps/web/src/components/status`): `failureOf(signals)` is the one pure mapping to exactly one state — the socket (`clientConnection`, fed by `watchTransport` around the actor transport and the browser's online events), then `Machine.online` / a `disconnected` session, then `authStatus` missing / expired or an auth-shaped doctor error, then the interrupted marker (the Session's `process_exited` turn end, the Task's `waiting {input, resume:…}`), then a non-recoverable adapter error, then `Task.status: failed` by its code (`taskFailureKind`: `environment-offline` → machine, `auth*` → auth, the adapter codes and `turn-error` / `prompt-*` → runtime, the rest — `session-open` / `no-api-key` included — → task). `FailureNotice` draws it with the one action; the shell strip (`LiveConnection`) and the offline banner read the socket, the chat (`chatFailure`: feeds first, then the `task-failed` / `interrupted:` status rows the router and the Session publish, #128), session (`sessionSignals`) and task (`LiveTask`) pages each map what they have. Resume is `Routing.resume(taskId)` → `Session.resume()` (a new `{turnId}:resume` prompt with the cut turn's input) and a `resumed` transition on the Task; the route parks as `interrupted` meanwhile; interrupted work reads uncertain on the card, the node and the transition until then.
- Live plumbing (#150): `ActorDefs` also names `Memory` (a stub in `clientDefs()`, the registry's object in `platformDefs()`; key `memoryKeyOf(ws, scope)`). `__tests__/actors-keys.test.ts` pins `memoryKeyOf`, `taskIndexKeyOf`, `registryKeyOf` and `ledgerKeyOf` / `ledgerMonthOf` against the platform's helpers and holds the browser stubs and the SSR definitions to the same actor types, name by name. Times on the live pages: `src/time.ts` — `zoneFormat(zone)` (`time` / `dateTime` / `age`, built once per IANA zone, an unknown zone formats as UTC), `useWorkspaceZone(defs, viewer)` over a live `Workspace.get().settings.timeZone`, and `clockNow()` (the real clock in live mode, the mock workspace's frozen one otherwise). A formatter is passed into the pure adapters — there is no module-level zone, because two server renders share the module. `Age` takes `zone` and defaults `now` to `clockNow()`. The mock workspace keeps its own zone and clock.
- Chat on live data (#152, `apps/web/src/pages/chat`): everything the live chat pages print comes from an actor or from this device. **Tasks in this chat** — one live `TaskIndex.list()` folded by `chatTasks(rows, chatId)`: a root is a row whose `chatId` is this chat, delegated children (no `chatId`) hang off it by `parentId`; depth-first, a chain that still runs before a settled one, capped at `CHAT_TASKS_CAP`. **Stop task chain** — `Task.cancel('user')` on `stopTargets` (the highest task of each chain that has not settled; the cancel takes the subtree, COL-12); `notStoppedLine` names what a report could not stop on the page's error line; the button and the dialog list only what has not settled. **Waiting** — `openRequests(entries)` pairs `request` / `request-resolved` status entries by `ref`; an agent with one open reads WAITING in the members panel and the topbar summary, and its chat's list row carries the amber pill. **Unread** — device-local by decision: `read-marks.ts` keeps the chat's `seq` at the moment this device last had it open, per workspace, in `localStorage` (guarded; SSR and a refusing store count nothing); `unreadOf` counts agent messages at or past it; a chat first seen on a device starts at its present end; the open chat never counts. Chat state is a replayed entry log, so a marker there would need a core entry kind and spend the window on non-content — read state that follows the user is #157. **The list is live** — after the batch read for the first paint, one renderless `ChatWatch` per chat holds live reads of `Chat.get` and the newest `LIST_TAIL` (30) entries (the `MachineWatch` pattern; the `$live` channel multiplexes them), so a reply elsewhere moves the row, its count and its pill without a reload; a row's last line is the newest message (`lastOf`), not the session bookkeeping after it. **Search and settings** — the topbar's two buttons raise `chatSearchRequest` / `chatSettingsRequest` (`head.ts`, the `newChatRequest` seam) in live mode and stay inert in mock mode: a panel over `Chat.search(q, 20)` (hits are listed with who and when; the Thread's rows carry no DOM id to jump to), and a dialog whose `settingsChange` names only what changed — members leave first (`removeAgent`; a leaving coordinator takes the role with it), then `setCoordinator`, then `rename`. The chat list's search box filters rows by title and last line in both modes. The user reads as "You" (it was the mock workspace's person) and every time on the page is `zoneFormat(useWorkspaceZone()).time` (#150).
- Session page without literals (#154): `liveSessionView` takes the open time from `spec.retrieval.at` — the instant `Session.open` stamped, present when the platform has its learning ports; absent, the header says only where the session was opened from — and the working dir from the environment's first `cwdRoots` entry in the `MachineView` the page already reads (what the router hands the daemon); a platform session has none and the row is left out. The header's time is printed in the workspace zone (`SessionView`'s `time` prop ← `zoneFormat(useWorkspaceZone())`). The current tool call shows a duration only when the view carries one (`current.meta`, the design workspace's): agent events have no timestamps, so the platform reports none. Session grants are listed and never revocable (`SessionInfo.grants`), so `SessionView` draws Revoke only for an `onRevoke` nobody passes today, under the note "Grants end with the session."
- Shell and inbox on live data (#151): the sidebar's Home badge is the "Needs you" count — `App` reads `useNeedsSource()().useRows()` and passes the length to `NAV_GROUPS(badge)`, so the badge and Home's "N open" are one number from one source (not `Inbox.unread()`, which counts every kind and misses interrupted work); the mock shell keeps the design workspace's three. Interrupted work (OPS-05) is a row: `liveNeedsSource` adds the router's routes parked `interrupted` (`interruptedRows` over a live `Routing.get()` — the agent by the route's frozen config, the chat the task came from, else the task), and the row's Resume is `NeedsSource.resume(row)` → `Routing.resume(taskId)`; the row leaves with the live read once the route runs again (`memoryNeedsSource` drops it in memory). Ages print through `zoneFormat(zone).age(at, clockNow())` with the zone from `useWorkspaceZone`. A signed-in user gets a plain `<form method="post" action="/auth/logout">` in the shell's user slot, live mode only.
- Agent pages on live data (#153, `apps/web/src/pages/agent`): `activity.ts` holds the reads, `live.ts` the pure folds (`profileOf(view, versions, index, activity)`). Presence and the Sessions tab come from the workspace's `TaskIndex` — one live `list()` shared by the roster and the agent page and filtered by assignee in the browser (a live read's arguments are part of its key and must be JSON primitives, so the query object stays out): `presenceOf` (waiting on an approval or an input beats active; nothing in flight is idle), `sessionRowsOf` (the rows that carry a `sessionId`, one per session — every session is opened by the router for a task). `activeOnOlder` (AGT-07) is `Task.get().configVersion` of the tasks in flight against the current version. The Memory tab lists the agent's private scope (`Memory.exportPage`, re-read whenever `Memory.stats().rev` moves, so a write from anywhere reaches an open tab) and acts through a `MemoryTabStore` prop (`correct` → `Memory.update` as stated by the user, `retire` → `Memory.retire`, `remove?` → `Memory.delete`, `setLearning` → `Agent.update({ memoryPolicy })`, a config version); Delete is drawn only when the store has `remove` (the AC-15 rule), and without a store the tab mutates a local copy (the mock page). Counters: `Ledger.corrections(agent, isoWeek)` and the same for `wrong`, summed over the month ledgers the week has touched (`weekMonths`; a correction is tallied in the month it happened in); the roster's cards read their own `Memory.stats().live` and corrections (`CardStats`). The oldest pending instruction proposal is the rail's NEEDS REVIEW item (`proposedVersion`); `ConfigStore.review` accepts or rejects it through `Agent.reviewProposal`, and on an accept the form restarts from the config the actor holds, so a later save cannot undo the patch. The pickers take a `catalog` (`catalog.ts`): tools are `PLATFORM_TOOLS` (a mirror of `@agentic/runtimes`' `PLATFORM_TOOL_NAMES`, pinned by a test — the browser does not import the runtimes) plus what the Registry's connectors discovered, connectors are `Registry.connectors()`, skills and shared scopes only what the agent already carries (there is no catalogue of either), and `environments` is left to the machines wiring (#144). Times print in the workspace's zone against the real clock (`format.ts`, `useWorkspaceZone`).
- Working folders in the web app (#193, part of #185; AGT-05, EXE-02, EXE-06):
  - **`pages/workdir/`:** `WorkdirPicker` drives `@agentic/ui`'s `WorkdirDialog` (#191).
    - Live: a listing is `Machine.fsRequest(env, { kind: 'list', path })`, then `fsResult(requestId)` read live until the daemon's answer lands (#189). The last listing stays on screen while the next loads, and the picker gives up after `CLIENT_TIMEOUT_MS`.
    - A worktree is the same round trip with `worktree`, and the picker then opens the new folder.
    - Recent folders are `Workspace.recentWorkdirs()`. On mock data the folders come from `mock/fs.ts`.
    - `WorkdirInput` is the field-plus-picker the forms use. `workdirEnvironmentOf` builds the environments (`machine / environment`, roots, the machine's OS, and why one is unavailable: offline, no roots, sign-in) from the environment directory.
  - **Entry points:**
    - **Chat:** the context panel shows a folder chip per daemon member, which calls `Chat.setWorkdir`. `activationContract` copies the member's folder into the task as `environmentId` + `workdir`, and the folder note renders by name. **Effective folder (#333):** the chip shows `member.workdir ?? projectFolderFor(project, member's environment)` (`effectiveWorkdir`), marked "from project" when inherited; Clear appears only for an override, and clearing goes back to the project's folder. `activationContract` copies the chat's `projectId` into the task, so the router resolves the project's folder wherever the member has none.
    - **`/chats/new?env=&path=&origin=` (#336, `pages/chat/{NewChatEntry,new-chat-prefill}`):** where `agentic-daemon open` sends the browser (§5b). The route (declared before `/chats/:id`, crumb Chats) renders the chats page and opens the New chat dialog on a prefill — `newChatPrefillOf(route.query)`: `env` and `path` required, `origin` optional, a malformed query is the plain dialog. `NewChatDialog.prefill`: with an `origin` that a project's git feature names (`projectForOrigin` over `features['agentic.feature.git'].origin`, core's `sameOrigin`) that project is preselected over the last used one and, when it has no folder on that environment, a checked line offers to save this one; with an origin no project names the dialog starts on "No project" and offers **Just this chat** (the default) or **Create project from this folder**; without an origin the last used project stays. `create` then carries `workdir: { environmentId, path, saveToProject }` and `createChatFrom` (`LiveChats`) runs `Workspace.upsertProject({ id, folders: { [env]: path } })` before `createChat` when saving, else `Chat.setWorkdir` on every picked member whose environment is that one (as "Start task" does for one agent) unless the project's folder there is already this path. **Create project** goes to `/projects/new?name=<folder name>&env=&path=&origin=` (`newProjectLink`), where `NewProject` reads the query (`projectPrefillOf`) into `ProjectForm.initial`: the name and one folder row whose badge carries the origin, so Find on the other rows and `detect` work at once. Whatever the dialog does, the `/chats/new` URL is replaced (the new chat, the form, or `/chats` on cancel), so the link never re-fires.
    - **"Start task":** on Tasks (topbar) and an agent's page. It opens a chat with the agent titled by the objective, sets the folder there and posts the objective, so the task has a user origin to follow up in and core needed no new origin kind.
    - **Agent Config tab:** the "Default working folder" fills `AgentForm`'s new `workdir` slot. It sets `execution.defaultEnvironmentId` + `defaultWorkdir`, and another environment clears the folder.
    - **New schedule:** "Working folder" beside Environment, as `ScheduleSpec.workdir`; "Project" (#333) as a third, exclusive option — choosing one clears and disables Environment and Working folder, and `newScheduleSpec` emits `projectId` alone.
  - **Session page:** "Working dir" is the record's `spec.cwd`.
  - **History:** files `workdir.worktree-created` under "Environments and folders".

## 11. Zero feedback loop

This app is the dogfood consumer for `@sigx/zero`. Friction (a missing part, a state the anatomy cannot express, a control that fights the model binding, a recipe the validator rejects) is filed on andtii/zero-wip labelled `from:agentic` as it is found, linked from the sub-issue here, with the smallest workaround under `packages/ui/src/_zero-gaps/`. Zero is never patched or vendored in this repo.

## 12. Risks and chosen defaults

| Risk | Default |
|---|---|
| Durable Object eviction during long model calls | task-based driver, append per event, "interrupted, resume?" |
| `CLAUDE_CONFIG_DIR` isolation on macOS Keychain | Windows only in v1; EXE-07 conformance test before enabling macOS |
| Node 22 global WebSocket in the daemon | assume it works; fall back to `ws` |
| Chat log growth | 200-entry window in state, rest in the log, compaction on save |
| Session record growth (a live chat session runs for weeks) | window paged out to `SessionPage`s, index without prompts and capped to `INDEX_TURNS`, replied commands without inputs, transcript bounded (#198, #391); history off the record entirely is #397 |
| Web Push on Workers | VAPID ES256 over WebCrypto, keys per workspace through the Web Push plugin (#244); contentless push, so the notification is generic; `aes128gcm` payload encryption is a follow-up |
| Collaborator policy (open decision 4) | `'all'` within the workspace, depth 3, concurrency 3 |
| Learning review policy (6) | memory automatic, instructions reviewed |
| Billing (7) | user-provided keys |
| Search (CHT-10) | substring per chat; full-text later |
| `@sigx/actors` pre-1.0 churn | pin `^0.9.2`, accept minor churn |
| `@sigx/cli` `actors` scaffold layer throws | scaffold `--target cloudflare`, add the DO class by hand |
